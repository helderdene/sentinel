---
phase: 08-implement-operator-role-and-intake-layer-ui
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - resources/js/pages/intake/IntakeStation.vue
  - resources/js/components/intake/DispatchQueuePanel.vue
  - resources/js/components/intake/QueueRow.vue
  - resources/js/components/intake/SessionMetrics.vue
  - resources/js/components/intake/PriorityBreakdown.vue
  - resources/js/components/intake/SessionLog.vue
  - app/Http/Controllers/IntakeStationController.php
  - app/Http/Controllers/IncidentController.php
  - routes/web.php
  - resources/js/pages/incidents/Queue.vue
autonomous: false
requirements: [OP-09, OP-10, OP-11]

must_haves:
  truths:
    - "Right panel shows triaged incidents ordered by priority then FIFO"
    - "New triaged incidents appear in queue with slideIn animation"
    - "Session metrics show Received, Triaged, Pending, Avg Handle Time"
    - "Priority breakdown bar chart shows distribution of incidents by priority"
    - "Supervisor/admin see Override Priority button on queue rows"
    - "Supervisor/admin see Recall button on queue rows"
    - "Supervisor/admin see Session Log section at bottom of right panel"
    - "Operator does NOT see override, recall, or session log"
    - "Override changes incident priority and creates timeline entry"
    - "Recall removes incident from queue and creates timeline entry"
  artifacts:
    - path: "resources/js/components/intake/DispatchQueuePanel.vue"
      provides: "Right panel: queue header + scrollable queue + session metrics + priority breakdown + session log"
      min_lines: 60
    - path: "resources/js/components/intake/QueueRow.vue"
      provides: "Single queued incident row with priority border and supervisor actions"
      min_lines: 40
    - path: "resources/js/components/intake/SessionMetrics.vue"
      provides: "2x2 stat grid showing session metrics"
      min_lines: 20
    - path: "resources/js/components/intake/PriorityBreakdown.vue"
      provides: "CSS horizontal bar chart by priority level"
      min_lines: 20
    - path: "resources/js/components/intake/SessionLog.vue"
      provides: "Activity log for supervisor/admin"
      min_lines: 20
  key_links:
    - from: "resources/js/pages/intake/IntakeStation.vue"
      to: "DispatchQueuePanel.vue"
      via: "component in right column"
      pattern: "DispatchQueuePanel"
    - from: "resources/js/components/intake/QueueRow.vue"
      to: "IntakeStationController"
      via: "override/recall actions"
      pattern: "intake\\.(override|recall)"
    - from: "resources/js/components/intake/SessionLog.vue"
      to: "auth.user.can"
      via: "gate-based conditional rendering"
      pattern: "can\\.view_session_log"
---

<objective>
Build the right panel of the intake station: dispatch queue showing triaged incidents, session metrics, priority breakdown chart, and supervisor/admin features (priority override, recall, session log). This completes the full intake station UI.

Purpose: Completes the intake workflow -- triaged incidents appear in the dispatch queue for dispatchers to pick up. Supervisors get oversight tools. Session metrics provide operational awareness.
Output: Fully functional intake station with all three panels, supervisor features, and visual verification checkpoint.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-CONTEXT.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-RESEARCH.md
@docs/IRMS-Intake-Design-System.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-01-SUMMARY.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-02-SUMMARY.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
IntakeStationController::show() props:
- incidentTypes, channels, priorities, pendingIncidents, triagedIncidents, priorityConfig

IntakeStationController::triage() -- already implemented in Plan 01

Gates available (via Inertia shared auth.user.can):
- triage_incidents, manual_entry, submit_dispatch (operator + supervisor + admin)
- override_priority, recall_incident, view_session_log (supervisor + admin only)

<!-- From Plan 03 -->
From resources/js/composables/useIntakeFeed.ts:
```typescript
// triagedIncidents reactive list, updated when triage form submitted
// IncidentStatusChanged WebSocket handler moves incidents to triaged list
```

From resources/js/composables/useIntakeSession.ts:
```typescript
export function useIntakeSession() {
    // received, triaged, pending (computed), avgHandleTime (computed)
    // recordReceived(), recordTriaged(handleTimeMs)
}
```

From resources/js/components/intake/PriBadge.vue:
```typescript
// Props: p (1-4), size ('sm' | 'lg')
```

From Inertia shared props (auth.ts):
```typescript
export type UserPermissions = {
    // ... existing gates
    triage_incidents: boolean;
    manual_entry: boolean;
    submit_dispatch: boolean;
    override_priority: boolean;
    recall_incident: boolean;
    view_session_log: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DispatchQueuePanel, QueueRow, SessionMetrics, PriorityBreakdown, and backend endpoints</name>
  <files>
    resources/js/components/intake/DispatchQueuePanel.vue
    resources/js/components/intake/QueueRow.vue
    resources/js/components/intake/SessionMetrics.vue
    resources/js/components/intake/PriorityBreakdown.vue
    app/Http/Controllers/IntakeStationController.php
    routes/web.php
  </files>
  <action>
    1. Add two new methods to IntakeStationController:
       - overridePriority(Request, Incident): Validates new priority (required, in P1-P4). Updates incident priority. Creates 'priority_override' timeline entry with old/new values and actor. Dispatches IncidentStatusChanged event. Returns back(). Gate check: 'override-priority'.
       - recall(Incident): Transitions incident status back to PENDING from TRIAGED. Creates 'incident_recalled' timeline entry. Dispatches IncidentStatusChanged event. Returns back(). Gate check: 'recall-incident'.

    2. Add routes in web.php within the intake middleware group:
       - POST /intake/{incident}/override-priority -> IntakeStationController::overridePriority, name: intake.override-priority
       - POST /intake/{incident}/recall -> IntakeStationController::recall, name: intake.recall

    3. Build DispatchQueuePanel.vue (right panel, 304px):
       - Structure (top to bottom):
         a. Header (fixed): "DISPATCH QUEUE" label (Space Mono 9px uppercase text-t-text-faint letter-spacing 2) + count badge (accent bg, white text, Space Mono 10px bold, rounded-full, min-w 20px)
         b. Queue list (scrollable, flex-1): QueueRow components for each triaged incident, ordered by priority (P1 first) then FIFO. Use TransitionGroup for slideIn animation when new incidents are triaged.
         c. SessionMetrics component (fixed)
         d. PriorityBreakdown component (fixed, conditional: only show if there are triaged incidents)
       - Accept props: triagedIncidents (from useIntakeFeed), sessionMetrics (from useIntakeSession), userCan (permissions object)

    4. Build QueueRow.vue (per design system "Queued Incident Row"):
       - Props: incident, canOverride (boolean), canRecall (boolean)
       - Background: bg-t-surface
       - Border-left: 3px solid priority color
       - Border-radius: 7px
       - Shadow: 0 1px 3px rgba(0,0,0,0.04)
       - Padding: 10px 12px
       - Content:
         - Top row: PriBadge (sm) + incident_no (Space Mono 10px text-t-text-faint) + relative timestamp
         - Middle: Incident type name (DM Sans 13px weight 500) + location (DM Sans 11px text-t-text-dim with pin icon)
         - Bottom row (conditional, supervisor/admin only):
           - Override Priority button: IntakeIconOverride + "Override" text. Styled per design system: rgba(124,58,237,0.07) bg, rgba(124,58,237,0.25) border, #7c3aed text. On click: expand inline 4-cell priority picker (current priority disabled at 40% opacity). Selecting new priority submits via Wayfinder action to intake.override-priority.
           - Recall button: IntakeIconRecall + "Recall" text. Styled: rgba(220,38,38,0.06) bg, rgba(220,38,38,0.2) border, #dc2626 text. On click: immediately recalls (no confirmation per design system -- speed-optimized for ops context). Submits via Wayfinder action to intake.recall.

    5. Build SessionMetrics.vue:
       - 2x2 grid of stat cards
       - Each card: Number (Space Mono 20px bold) + Label (DM Sans 11px text-t-text-dim)
       - Cards: Received (count), Triaged (count, text-t-online), Pending (count, text-t-accent), Avg Handle (seconds, text-t-text)
       - Background: bg-t-surface-alt, border border-t-border, rounded-lg
       - Accept props: received, triaged, pending, avgHandleTime

    6. Build PriorityBreakdown.vue:
       - "PRIORITY BREAKDOWN" label (Space Mono 9px uppercase text-t-text-faint letter-spacing 2)
       - 4 horizontal bars (P1 through P4):
         - Label: "P{n}" (Space Mono 10px bold, priority color) + level name (DM Sans 10px text-t-text-dim)
         - Bar: priority color background, width proportional to count/total, min 4px if count > 0
         - Count: Space Mono 10px bold, right-aligned
       - Pure CSS implementation (no chart library). Bar widths via inline style percentage.
       - Accept props: triagedIncidents (compute counts from priority field)
  </action>
  <verify>
    <automated>npm run build && php artisan test --compact tests/Feature/Intake/ -x</automated>
  </verify>
  <done>
    Right panel shows triaged incidents in priority order. Session metrics display 4 stats. Priority breakdown shows distribution bars. Override and recall endpoints work. Build passes. Intake tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: SessionLog, supervisor gate rendering, IntakeStation wiring, and existing Queue.vue update</name>
  <files>
    resources/js/components/intake/SessionLog.vue
    resources/js/pages/intake/IntakeStation.vue
    resources/js/pages/incidents/Queue.vue
    app/Http/Controllers/IncidentController.php
  </files>
  <action>
    1. Build SessionLog.vue (supervisor/admin only):
       - "SESSION LOG" label (Space Mono 9px uppercase text-t-text-faint letter-spacing 2)
       - Scrollable list of activity entries, most recent first
       - Each entry: timestamp (Space Mono 9.5px text-t-text-faint) + action description (DM Sans 11px text-t-text-mid) + optional priority badge
       - Entry types:
         - "Triaged INC-YYYY-NNNNN as P{n}": shown when triage form submitted
         - "Override INC-YYYY-NNNNN priority P{old} -> P{new}": shown on priority override
         - "Recalled INC-YYYY-NNNNN from queue": shown on recall
       - Activity log is maintained in-memory (local reactive array), populated from WebSocket events and local form submissions
       - Only rendered when usePage().props.auth.user.can.view_session_log is true
       - Max 50 entries (oldest trimmed)

    2. Wire DispatchQueuePanel into IntakeStation.vue:
       - Replace the right column placeholder div with DispatchQueuePanel component
       - Pass triagedIncidents from useIntakeFeed
       - Pass session metrics from useIntakeSession
       - Pass user permissions from usePage().props.auth.user.can
       - Wire SessionLog component inside DispatchQueuePanel (only rendered for supervisor/admin via v-if on view_session_log permission)
       - Ensure override and recall actions update the local state:
         - Override: update priority in triagedIncidents array, re-sort, add session log entry
         - Recall: remove from triagedIncidents, add back to pending feed, add session log entry

    3. Update existing Queue.vue (used by dispatchers):
       - Update the WebSocket handler to also listen for IncidentStatusChanged where new_status is TRIAGED (incidents entering the dispatch queue after operator triage)
       - This is a small but critical change to complete the PENDING -> TRIAGED -> DISPATCHED workflow

    4. Update IncidentController::queue() in the backend:
       - Change `->where('status', IncidentStatus::Pending)` to `->where('status', IncidentStatus::Triaged)`
       - This ensures the dispatcher's queue shows triaged incidents (ready for dispatch), not pending ones (still need triage)
       - Also update the channelCounts query in HandleInertiaRequests and IncidentController to count TRIAGED for the dispatch queue context
  </action>
  <verify>
    <automated>php artisan test --compact && npm run build && npm run types:check</automated>
  </verify>
  <done>
    SessionLog renders for supervisor/admin only, hidden for operator. Override and recall actions update local state and session log. IntakeStation fully wired with all three panels. Existing Queue.vue and IncidentController::queue() updated to show TRIAGED incidents. All tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete intake station</name>
  <files>n/a</files>
  <action>
    Human verifies the complete intake station UI across operator, supervisor, and admin roles. Tests the full intake workflow: feed -> triage -> queue, plus supervisor features (override, recall, session log), dark mode, and font rendering on existing pages.
  </action>
  <verify>
    <automated>php artisan test --compact && npm run build</automated>
  </verify>
  <done>
    User confirms: three-column layout renders correctly, live feed works via WebSocket, triage form submits and transitions status, right panel shows queue with metrics, supervisor features are role-gated, dark mode works, existing pages unaffected by font change. Type "approved" or describe issues.
  </done>
</task>

</tasks>

<verification>
```bash
# Full test suite passes
php artisan test --compact

# Frontend builds
npm run build

# TypeScript clean
npm run types:check

# Lint clean
npm run lint && npm run format

# PHP formatted
vendor/bin/pint --dirty --format agent
```
</verification>

<success_criteria>
- Right panel shows triaged incidents ordered by priority then FIFO
- Session metrics (Received, Triaged, Pending, Avg Handle) update in real-time
- Priority breakdown bar chart shows correct distribution
- Supervisor/admin see Override and Recall buttons; operator does not
- Session Log visible only for supervisor/admin
- Override changes priority and re-sorts queue
- Recall removes incident from queue
- Existing Queue.vue shows TRIAGED incidents (not PENDING)
- All three panels compose correctly in IntakeStation
- Full test suite green, build passes, types clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-04-SUMMARY.md`
</output>
