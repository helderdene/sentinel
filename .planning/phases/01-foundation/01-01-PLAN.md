---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env
  - .env.testing
  - phpunit.xml
  - config/fortify.php
  - app/Providers/FortifyServiceProvider.php
  - composer.json
  - app/Enums/UserRole.php
  - app/Enums/IncidentStatus.php
  - app/Enums/IncidentPriority.php
  - app/Enums/UnitStatus.php
  - app/Enums/UnitType.php
  - database/migrations/2026_03_12_000001_enable_postgis_extension.php
  - database/migrations/2026_03_12_000002_add_role_fields_to_users_table.php
  - database/migrations/2026_03_12_000003_create_units_table.php
  - database/migrations/2026_03_12_000004_create_barangays_table.php
  - database/migrations/2026_03_12_000005_create_incident_types_table.php
  - database/migrations/2026_03_12_000006_create_incidents_table.php
  - database/migrations/2026_03_12_000007_create_incident_timeline_table.php
  - database/migrations/2026_03_12_000008_create_incident_messages_table.php
  - app/Models/User.php
  - app/Models/Incident.php
  - app/Models/Unit.php
  - app/Models/Barangay.php
  - app/Models/IncidentType.php
  - app/Models/IncidentTimeline.php
  - app/Models/IncidentMessage.php
  - app/Http/Middleware/EnsureUserHasRole.php
  - app/Providers/AppServiceProvider.php
  - bootstrap/app.php
  - database/factories/UserFactory.php
  - database/factories/IncidentFactory.php
  - database/factories/UnitFactory.php
  - database/factories/BarangayFactory.php
  - database/factories/IncidentTypeFactory.php
  - database/factories/IncidentTimelineFactory.php
  - database/factories/IncidentMessageFactory.php
  - database/seeders/DatabaseSeeder.php
  - database/seeders/AdminUserSeeder.php
  - database/seeders/IncidentTypeSeeder.php
  - database/seeders/UnitSeeder.php
  - database/seeders/BarangaySeeder.php
  - database/data/butuan-barangays.geojson
  - tests/Feature/Foundation/PostgisSetupTest.php
  - tests/Feature/Foundation/BarangaySpatialTest.php
  - tests/Feature/Foundation/RoleAccessTest.php
  - tests/Feature/Foundation/UserUnitTest.php
  - tests/Feature/Foundation/IncidentModelTest.php
  - tests/Feature/Foundation/UnitModelTest.php
  - tests/Feature/Foundation/IncidentTimelineTest.php
  - tests/Feature/Foundation/IncidentMessageTest.php
autonomous: true
requirements:
  - FNDTN-01
  - FNDTN-02
  - FNDTN-03
  - FNDTN-04
  - FNDTN-05
  - FNDTN-06
  - FNDTN-07
  - FNDTN-08

must_haves:
  truths:
    - "PostgreSQL with PostGIS is the application database and spatial queries execute successfully"
    - "A responder user is linked to a specific unit and that association is queryable"
    - "The 86 Butuan City barangay boundary polygons are seeded and a point-in-polygon query correctly identifies a barangay"
    - "An unauthorized role is blocked from accessing role-restricted routes with a 403 response"
    - "An admin can create user accounts for each of the four roles via the seeded admin account"
    - "Creating an incident populates lifecycle fields and coordinates geography"
    - "Incident timeline entries append correctly as an immutable audit log"
    - "Dispatch and responder can exchange messages on an incident"
  artifacts:
    - path: "app/Enums/UserRole.php"
      provides: "Backed enum for 4 roles"
      contains: "enum UserRole: string"
    - path: "app/Models/Incident.php"
      provides: "Incident model with all fields from spec"
      min_lines: 40
    - path: "app/Models/Barangay.php"
      provides: "Barangay model with geography polygon cast"
      contains: "boundary"
    - path: "app/Http/Middleware/EnsureUserHasRole.php"
      provides: "Route-level role checking middleware"
      contains: "handle"
    - path: "database/data/butuan-barangays.geojson"
      provides: "86 barangay boundary polygons for Butuan City"
      min_lines: 50
    - path: "tests/Feature/Foundation/PostgisSetupTest.php"
      provides: "PostGIS extension and spatial migration tests"
      min_lines: 10
    - path: "tests/Feature/Foundation/RoleAccessTest.php"
      provides: "RBAC middleware and gate enforcement tests"
      min_lines: 20
  key_links:
    - from: "app/Models/User.php"
      to: "app/Enums/UserRole.php"
      via: "casts() method"
      pattern: "UserRole::class"
    - from: "app/Models/Incident.php"
      to: "app/Models/IncidentTimeline.php"
      via: "hasMany relationship"
      pattern: "timeline"
    - from: "app/Models/User.php"
      to: "app/Models/Unit.php"
      via: "belongsTo relationship"
      pattern: "unit"
    - from: "bootstrap/app.php"
      to: "app/Http/Middleware/EnsureUserHasRole.php"
      via: "middleware alias registration"
      pattern: "role.*EnsureUserHasRole"

user_setup:
  - service: postgresql
    why: "PostGIS spatial queries require PostgreSQL with PostGIS extension installed"
    env_vars:
      - name: DB_PASSWORD
        source: "Your local PostgreSQL superuser password (set during PostgreSQL 18 installation)"
    dashboard_config:
      - task: "Ensure PostGIS extension is available"
        location: "Run: /Library/PostgreSQL/18/bin/psql -U postgres -c 'SELECT PostGIS_Version();' -- if fails, install PostGIS via EDB StackBuilder or brew install postgis"
      - task: "Create application databases"
        location: "Run: /Library/PostgreSQL/18/bin/createdb -U postgres irms && /Library/PostgreSQL/18/bin/createdb -U postgres irms_testing"
---

<objective>
Create the complete database foundation for IRMS: switch from SQLite to PostgreSQL with PostGIS, create all migrations for the 7 data models (incidents, units, barangays, incident_types, incident_timeline, incident_messages, plus users extension), implement Eloquent models with spatial casts via clickbar/laravel-magellan, create enums for all status/type fields, build factories and seeders (including 86 real barangay boundaries), and implement RBAC middleware + Laravel Gates. All 8 foundation test files are created alongside their corresponding implementation.

Purpose: This is the data layer that all subsequent phases depend on. Without correct geography types, spatial indexes, and role enforcement, nothing else can function.
Output: Working PostgreSQL database with PostGIS, 7+ models, factories, seeders, RBAC middleware, 8 foundation test files, and a green test suite.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@docs/IRMS-Specification.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From app/Models/User.php:
```php
class User extends Authenticatable
{
    use HasFactory, Notifiable, TwoFactorAuthenticatable;

    protected $fillable = ['name', 'email', 'password'];
    protected $hidden = ['password', 'two_factor_secret', 'two_factor_recovery_codes', 'remember_token'];

    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
            'two_factor_confirmed_at' => 'datetime',
        ];
    }
}
```

From bootstrap/app.php:
```php
->withMiddleware(function (Middleware $middleware): void {
    $middleware->encryptCookies(except: ['appearance', 'sidebar_state']);
    $middleware->web(append: [
        HandleAppearance::class,
        HandleInertiaRequests::class,
        AddLinkHeadersForPreloadedAssets::class,
    ]);
})
```

From config/fortify.php features array (line 146-155):
```php
'features' => [
    Features::registration(),      // MUST BE REMOVED per locked decision
    Features::resetPasswords(),
    Features::emailVerification(),
    Features::twoFactorAuthentication([...]),
],
```

From app/Providers/FortifyServiceProvider.php:
```php
// Line 69: Fortify::registerView(fn () => Inertia::render('auth/Register'));
// This registerView call should be removed alongside disabling registration in fortify.php
// Also line 53: 'canRegister' => Features::enabled(Features::registration()) -- will auto-resolve to false
```

From phpunit.xml (lines 20-35) -- currently SQLite:
```xml
<env name="DB_CONNECTION" value="sqlite"/>
<env name="DB_DATABASE" value=":memory:"/>
<env name="DB_URL" value=""/>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PostgreSQL/PostGIS setup, Fortify config, enums, and all 8 migrations</name>
  <files>
    .env,
    .env.testing,
    phpunit.xml,
    config/fortify.php,
    app/Providers/FortifyServiceProvider.php,
    composer.json,
    app/Enums/UserRole.php,
    app/Enums/IncidentStatus.php,
    app/Enums/IncidentPriority.php,
    app/Enums/UnitStatus.php,
    app/Enums/UnitType.php,
    database/migrations/2026_03_12_000001_enable_postgis_extension.php,
    database/migrations/2026_03_12_000002_add_role_fields_to_users_table.php,
    database/migrations/2026_03_12_000003_create_units_table.php,
    database/migrations/2026_03_12_000004_create_barangays_table.php,
    database/migrations/2026_03_12_000005_create_incident_types_table.php,
    database/migrations/2026_03_12_000006_create_incidents_table.php,
    database/migrations/2026_03_12_000007_create_incident_timeline_table.php,
    database/migrations/2026_03_12_000008_create_incident_messages_table.php,
    tests/Feature/Foundation/PostgisSetupTest.php
  </files>
  <action>
    **Step 1: Install clickbar/laravel-magellan and switch to PostgreSQL**

    Run `composer require clickbar/laravel-magellan` to install PostGIS model casts. Update `.env` to use PostgreSQL:
    - DB_CONNECTION=pgsql
    - DB_HOST=127.0.0.1
    - DB_PORT=5432
    - DB_DATABASE=irms
    - DB_USERNAME=postgres
    - DB_PASSWORD=(user's local password)

    Create `.env.testing` with same PostgreSQL config but DB_DATABASE=irms_testing. Update `phpunit.xml` to REMOVE the three SQLite override lines (DB_CONNECTION, DB_DATABASE, DB_URL) so `.env.testing` takes effect. All tests now run on PostgreSQL.

    **Step 2: Disable Fortify registration**

    Remove `Features::registration()` from `config/fortify.php` features array per locked decision. Also remove the `Fortify::registerView(...)` call from `app/Providers/FortifyServiceProvider.php` (line 69) since the registration feature is disabled and this view is no longer reachable.

    **Step 3: Create 5 PHP backed enums**

    Use `php artisan make:class` for each enum in `app/Enums/`:
    - `UserRole`: Admin, Dispatcher, Responder, Supervisor (string-backed)
    - `IncidentStatus`: Pending, Dispatched, Acknowledged, EnRoute, OnScene, Resolving, Resolved
    - `IncidentPriority`: P1, P2, P3, P4 (with label() method returning human-readable names, and color() method)
    - `UnitStatus`: Available, Dispatched, EnRoute, OnScene, Offline
    - `UnitType`: Ambulance, Fire, Rescue, Police, Boat

    **Step 4: Create 8 migrations in order**

    Use `php artisan make:migration` for each. The exact timestamp prefix matters for ordering but use sequential numbers within the same date:

    1. `enable_postgis_extension` -- `DB::statement('CREATE EXTENSION IF NOT EXISTS postgis;')`
    2. `add_role_fields_to_users_table` -- Add `role` string column (default 'dispatcher'), `unit_id` nullable FK to units, `badge_number` nullable string, `phone` nullable string. NOTE: units table doesn't exist yet; add `unit_id` as nullable unsignedBigInteger without FK constraint here, then add the FK in the units migration or a separate migration.
    3. `create_units_table` -- id (string primary 'AMB-01' style), callsign, type (string), agency (string), crew_capacity (integer), status (string default 'AVAILABLE'), geography('coordinates', point, 4326) nullable, shift (string nullable), notes (text nullable), timestamps. Add spatialIndex on coordinates. Add FK constraint for users.unit_id -> units.id.
    4. `create_barangays_table` -- id, name (string 100), district (string 50 nullable), city (string 50 default 'Butuan City'), geography('boundary', polygon, 4326) nullable, population (integer nullable), risk_level (string 20 nullable), timestamps. Add spatialIndex on boundary.
    5. `create_incident_types_table` -- id, category (string 50), name (string 100), code (string 20 unique), default_priority (string 2), description (text nullable), is_active (boolean default true), sort_order (integer default 0), timestamps. Index on category.
    6. `create_incidents_table` -- uuid primary key, incident_no (string 20 unique), incident_type_id FK, priority (string 2), status (string 30 default 'PENDING'), channel (string 20), location_text (text nullable), geography('coordinates', point, 4326) nullable, barangay_id nullable FK, caller_name (string 100 nullable), caller_contact (string 30 nullable), raw_message (text nullable), notes (text nullable), assigned_unit (string 20 nullable), dispatched_at through resolved_at timestamps nullable, outcome (string 50 nullable), hospital (string 100 nullable), scene_time_sec (integer nullable), checklist_pct (smallInteger nullable), vitals (jsonb nullable), assessment_tags (text nullable -- PostgreSQL array via cast), closure_notes (text nullable), report_pdf_url (string 255 nullable), created_by nullable FK to users, timestamps. Add spatialIndex on coordinates, indexes on priority, status, created_at.
    7. `create_incident_timeline_table` -- id, incident_id FK (uuid) cascading delete, event_type (string 50), event_data (jsonb nullable), actor_type (string 50 nullable), actor_id (unsignedBigInteger nullable), notes (text nullable), timestamps. Index on incident_id + created_at.
    8. `create_incident_messages_table` -- id, incident_id FK (uuid) cascading delete, sender_type (string 50), sender_id (unsignedBigInteger), body (text), message_type (string 20 default 'text'), is_quick_reply (boolean default false), read_at (timestamp nullable), timestamps. Index on incident_id + created_at.

    **Step 5: Create PostGIS setup test and run migrations**

    Create `tests/Feature/Foundation/PostgisSetupTest.php` using `php artisan make:test --pest Foundation/PostgisSetupTest`:
    - Test: PostGIS extension is enabled (`DB::select("SELECT PostGIS_Version()")` succeeds)
    - Test: All 8 migrations run without error (migration is already handled by RefreshDatabase trait, verify table existence via `Schema::hasTable()` for each of the 7 tables + users columns)
    - Test: Geography columns exist on incidents, units, and barangays tables
    - Test: Fortify registration route is disabled (GET /register returns 404 or redirect)

    Run `php artisan migrate` to verify migrations work, then run the test.

    Run `vendor/bin/pint --dirty --format agent` after all PHP file changes.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=PostgisSetup</automated>
  </verify>
  <done>
    - PostgreSQL with PostGIS is the application database; `php artisan migrate` runs cleanly
    - All 8 migrations create correct tables with geography columns and spatial indexes
    - 5 enums created with correct cases
    - Fortify registration is disabled in config and FortifyServiceProvider registerView removed
    - PostgisSetupTest passes confirming extension, tables, and registration disabled
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Eloquent models, RBAC middleware, gates, and model tests</name>
  <files>
    app/Models/User.php,
    app/Models/Incident.php,
    app/Models/Unit.php,
    app/Models/Barangay.php,
    app/Models/IncidentType.php,
    app/Models/IncidentTimeline.php,
    app/Models/IncidentMessage.php,
    app/Http/Middleware/EnsureUserHasRole.php,
    app/Providers/AppServiceProvider.php,
    bootstrap/app.php,
    tests/Feature/Foundation/RoleAccessTest.php,
    tests/Feature/Foundation/UserUnitTest.php,
    tests/Feature/Foundation/IncidentModelTest.php,
    tests/Feature/Foundation/UnitModelTest.php,
    tests/Feature/Foundation/IncidentTimelineTest.php,
    tests/Feature/Foundation/IncidentMessageTest.php
  </files>
  <behavior>
    - Test: User can be created with each of the 4 roles via factory states (admin, dispatcher, responder, supervisor)
    - Test: Responder user has unit_id FK and unit() belongsTo relationship returns a Unit
    - Test: Incident creates with UUID primary key, auto-generated INC-YYYY-NNNNN number, geography coordinates, JSONB vitals, and all lifecycle timestamps
    - Test: Unit creates with geography point coordinates and GiST spatial index
    - Test: IncidentTimeline entry appends to incident (hasMany), stores event_type and event_data JSONB
    - Test: IncidentMessage creates with polymorphic sender, belongs to incident
    - Test: IncidentType belongs to category, has default_priority
    - Test: EnsureUserHasRole middleware returns 403 for wrong role, passes for correct role
    - Test: Laravel Gates (manage-users, create-incidents, view-analytics) enforce correct role permissions
  </behavior>
  <action>
    **Step 1: Create 7 Eloquent models**

    Use `php artisan make:model` for new models. Each model must have:
    - Proper fillable/guarded attributes
    - `casts()` method (not $casts property) following existing User model convention
    - Relationships with return type hints
    - For geography columns: use Clickbar\Magellan casts (Point::class for coordinates, Polygon::class for boundary)
    - For Incident: use `HasUuids` trait, custom `incident_no` generation via boot method or observer

    Specific model details:
    - **User.php**: Add `role`, `unit_id`, `badge_number`, `phone` to fillable, add `UserRole::class` cast, add `unit()` belongsTo(Unit::class), add `isAdmin()`, `isDispatcher()`, `isResponder()`, `isSupervisor()` helper methods
    - **Incident.php**: HasUuids, boot method to auto-generate `incident_no` (INC-YYYY-NNNNN format using DB sequence or max+1), relationships: incidentType(), barangay(), createdBy(), timeline(), messages(), assignedUnit(). Cast vitals as array, assessment_tags as array, coordinates as Point::class.
    - **Unit.php**: Primary key is string (not auto-increment). Cast coordinates as Point::class. Relationships: users() hasMany, currentIncidents().
    - **Barangay.php**: Cast boundary as Polygon::class (Magellan). Relationships: incidents().
    - **IncidentType.php**: Relationships: incidents(). Scopes: active(), byCategory().
    - **IncidentTimeline.php**: Belongs to incident (uuid FK). Cast event_data as array. Morphs to actor.
    - **IncidentMessage.php**: Belongs to incident (uuid FK). Morphs to sender. Cast read_at as datetime.

    **Step 2: Create EnsureUserHasRole middleware + register it**

    Create `app/Http/Middleware/EnsureUserHasRole.php` that accepts variadic role strings, checks `$request->user()->role->value` against them, aborts 403 if no match. Register in `bootstrap/app.php` as alias `'role' => EnsureUserHasRole::class`.

    **Step 3: Define Laravel Gates in AppServiceProvider**

    In `AppServiceProvider::boot()`, define gates per spec Section 9 permissions matrix:
    - `manage-users`: Admin only
    - `manage-incident-types`: Admin only
    - `manage-barangays`: Admin only
    - `create-incidents`: Dispatcher, Supervisor, Admin
    - `dispatch-units`: Dispatcher, Supervisor, Admin
    - `respond-incidents`: Responder
    - `view-analytics`: Supervisor, Admin
    - `view-all-incidents`: Dispatcher, Supervisor, Admin
    - `manage-system`: Admin only

    **Step 4: Create 6 test files alongside models**

    Create test files using `php artisan make:test --pest`:

    - `tests/Feature/Foundation/RoleAccessTest.php` -- Test middleware returns 403 for wrong role, passes for correct role. Test each gate (manage-users, create-incidents, view-analytics, respond-incidents) enforces the correct roles. Register a temporary test route with role middleware for testing.
    - `tests/Feature/Foundation/UserUnitTest.php` -- Test responder has unit_id and unit() relationship returns a Unit instance. Test non-responder roles have null unit_id.
    - `tests/Feature/Foundation/IncidentModelTest.php` -- Test incident creates with UUID, auto-generates INC-YYYY-NNNNN, stores geography coordinates via Magellan Point cast, stores JSONB vitals, has correct relationships (incidentType, barangay, createdBy, timeline, messages).
    - `tests/Feature/Foundation/UnitModelTest.php` -- Test unit creates with string primary key, stores geography point coordinates, has users() relationship.
    - `tests/Feature/Foundation/IncidentTimelineTest.php` -- Test timeline entry belongs to incident, stores event_type and event_data JSONB, morphs to actor.
    - `tests/Feature/Foundation/IncidentMessageTest.php` -- Test message belongs to incident, morphs to sender, has read_at datetime cast.

    Each test uses factories (which will be created in Task 3). Write tests first (RED), then verify models satisfy them (GREEN).

    Run `vendor/bin/pint --dirty --format agent` after all PHP file changes.
  </action>
  <verify>
    <automated>php artisan test --compact --filter="RoleAccess|UserUnit|IncidentModel|UnitModel|IncidentTimeline|IncidentMessage"</automated>
  </verify>
  <done>
    - All 7 models have correct relationships, casts, and spatial columns
    - Role middleware blocks/allows correctly for all 4 roles
    - Laravel Gates enforce the permissions matrix from spec Section 9
    - All 6 foundation model/RBAC test files pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Factories, seeders, barangay GeoJSON, and spatial test</name>
  <files>
    database/factories/UserFactory.php,
    database/factories/IncidentFactory.php,
    database/factories/UnitFactory.php,
    database/factories/BarangayFactory.php,
    database/factories/IncidentTypeFactory.php,
    database/factories/IncidentTimelineFactory.php,
    database/factories/IncidentMessageFactory.php,
    database/seeders/DatabaseSeeder.php,
    database/seeders/AdminUserSeeder.php,
    database/seeders/IncidentTypeSeeder.php,
    database/seeders/UnitSeeder.php,
    database/seeders/BarangaySeeder.php,
    database/data/butuan-barangays.geojson,
    tests/Feature/Foundation/BarangaySpatialTest.php
  </files>
  <action>
    **Step 1: Create/update factories for all models**

    Update existing `UserFactory` with role states: `->admin()`, `->dispatcher()`, `->responder()`, `->supervisor()`. Add `role` default of `UserRole::Dispatcher` to definition. Add `badge_number` and `phone` to definition.

    Create new factories using `php artisan make:factory`:
    - `IncidentFactory` -- generates valid incident_no (INC-YYYY-NNNNN), random coordinates near Butuan 8.95/125.54 via Magellan Point::makeGeodetic, random status and priority
    - `UnitFactory` -- generates unit IDs like AMB-01 with string PK, coordinates near Butuan
    - `BarangayFactory` -- generates polygon near Butuan via Magellan Polygon
    - `IncidentTypeFactory` -- generates category, name, code, default_priority
    - `IncidentTimelineFactory` -- generates event_type, event_data JSONB
    - `IncidentMessageFactory` -- generates body, message_type, polymorphic sender

    **Step 2: Create barangay GeoJSON data file**

    Create `database/data/butuan-barangays.geojson` -- obtain from faeldon/philippines-json-maps GitHub repo or create a comprehensive GeoJSON from available sources. If the exact 86-barangay file is not obtainable, create a synthetic GeoJSON with real barangay names from PSA data with approximate polygons around Butuan City center (lat ~8.95, lng ~125.54). Must contain 86 Feature entries with polygon geometry and properties including name.

    **Step 3: Create 4 seeders**

    - `AdminUserSeeder`: Create admin@irms.test with admin role, name "System Administrator"
    - `IncidentTypeSeeder`: Seed 8 categories with 40+ types from IRMS spec Section 8 (Medical: Cardiac Arrest -> P1, Stroke -> P1, Severe Bleeding -> P1, Difficulty Breathing -> P2, Seizure -> P2, Allergic Reaction -> P2, Minor Injury -> P3, etc. Fire: Structure Fire -> P1, Vehicle Fire -> P1, Brush/Grass Fire -> P2, etc. Natural Disaster: Earthquake -> P1, Flood -> P1, Landslide -> P1, Typhoon -> P1, etc. Vehicular: Multi-Vehicle Collision -> P1, Vehicle vs Pedestrian -> P1, Single Vehicle Accident -> P2, etc. Crime/Security: Active Shooter -> P1, Bomb Threat -> P1, Assault -> P2, etc. Hazmat: Chemical Spill -> P1, Gas Leak -> P1, etc. Water Rescue: Drowning -> P1, Boat Capsized -> P1, Flood Rescue -> P1, etc. Public Disturbance: Riot -> P2, Noise Complaint -> P4, etc.)
    - `UnitSeeder`: Seed 10 sample units (AMB-01 through AMB-03, RESCUE-01 through RESCUE-02, FIRE-01 through FIRE-02, POLICE-01 through POLICE-02, BOAT-01) with coordinates near Butuan City Hall area
    - `BarangaySeeder`: Parse `database/data/butuan-barangays.geojson`. Parse GeoJSON features, insert each with name, district (map from known district assignments), population (from PSA data where available), risk_level (default 'moderate', override for known flood-prone: Baan, Banza, Libertad, Dagatan -> 'high'; known high-ground: Ampayon, Bonbon -> 'low').

    Wire all seeders into `DatabaseSeeder.php` in order: AdminUser, IncidentType, Unit, Barangay.

    **Step 4: Create BarangaySpatialTest**

    Create `tests/Feature/Foundation/BarangaySpatialTest.php` using `php artisan make:test --pest Foundation/BarangaySpatialTest`:
    - Test: BarangaySeeder inserts 86 barangays (run seeder in test, assert count)
    - Test: A point-in-polygon query (ST_Contains) for a coordinate known to be within a specific barangay returns the correct barangay
    - Test: Barangay boundary is stored as geography polygon and can be retrieved via Magellan cast

    Run `vendor/bin/pint --dirty --format agent` after all PHP file changes.
  </action>
  <verify>
    <automated>php artisan migrate:fresh --seed --no-interaction && php artisan test --compact --filter=BarangaySpatial && php artisan test --compact</automated>
  </verify>
  <done>
    - All 7 factories produce valid model instances
    - UserFactory has admin(), dispatcher(), responder(), supervisor() states
    - `php artisan db:seed` populates admin user, 40+ incident types, 10 units, and 86 barangays
    - Point-in-polygon spatial query correctly identifies a barangay from coordinates
    - Full test suite passes on PostgreSQL (existing auth tests + all 8 new foundation tests)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `php artisan migrate:fresh --seed` runs without errors
2. `php artisan test --compact` passes all tests (existing + 8 new foundation tests)
3. `php artisan tinker --execute "App\Models\Barangay::count()"` returns 86
4. `php artisan tinker --execute "App\Models\IncidentType::count()"` returns 40+
5. `php artisan tinker --execute "App\Models\User::where('role', 'admin')->count()"` returns 1
6. PostGIS spatial query works: `php artisan tinker --execute "DB::select(\"SELECT PostGIS_Version()\")"`
</verification>

<success_criteria>
- PostgreSQL with PostGIS is the only database for dev and test
- All 8 migrations run cleanly, all 7 models work with spatial casts
- 86 barangays seeded, point-in-polygon query works
- RBAC middleware and gates enforce the 4-role permissions matrix
- Full test suite green (existing auth tests still pass, all 8 new foundation tests pass)
- Factories produce valid test data for all models with role variants
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
