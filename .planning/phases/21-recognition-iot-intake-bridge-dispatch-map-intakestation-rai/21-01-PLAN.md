---
phase: 21
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Fras/FrasIncidentFactoryTest.php
  - tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php
  - tests/Feature/Fras/EscalateToP1Test.php
  - tests/Feature/Fras/IntakeStationFrasRailTest.php
  - app/Events/RecognitionAlertReceived.php
  - config/fras.php
  - routes/channels.php
  - database/seeders/PersonOfInterestIncidentTypeSeeder.php
  - database/seeders/DatabaseSeeder.php
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - RECOGNITION-01
  - RECOGNITION-05
  - RECOGNITION-07
  - RECOGNITION-08
  - INTEGRATION-01
must_haves:
  truths:
    - "Wave 0 test files exist for all new Phase 21 code and fail with MISSING_CLASS / MISSING_ROUTE until Plan 02+ ships"
    - "config/fras.php has a 'recognition' section with confidence_threshold, dedup_window_seconds, pulse_duration_seconds, priority_map keys"
    - "routes/channels.php authorizes 'fras.alerts' for operator, dispatcher, supervisor, admin roles only (responder denied)"
    - "App\\Events\\RecognitionAlertReceived exists, implements ShouldBroadcast + ShouldDispatchAfterCommit, broadcasts on PrivateChannel('fras.alerts')"
    - "IncidentType with code='person_of_interest' is idempotently seeded by PersonOfInterestIncidentTypeSeeder"
    - "ROADMAP §Phase 21 SC1 says 'block-list, missing, or lost-child personnel' (not just 'block-list'); SC6 says '6th channel rail' (not '4th')"
    - "REQUIREMENTS RECOGNITION-02 says 'BOLO personnel (block/missing/lost_child categories)'; INTEGRATION-03 says '6th channel rail'"
  artifacts:
    - path: "tests/Feature/Fras/FrasIncidentFactoryTest.php"
      provides: "Nyquist test scaffold locking FrasIncidentFactory gate contract"
      min_lines: 80
    - path: "tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php"
      provides: "Broadcast event payload + channel auth coverage"
      min_lines: 60
    - path: "tests/Feature/Fras/EscalateToP1Test.php"
      provides: "Escalate-to-P1 route + audit trigger coverage"
      min_lines: 50
    - path: "tests/Feature/Fras/IntakeStationFrasRailTest.php"
      provides: "IntakeStation recentFrasEvents prop + 6th rail coverage"
      min_lines: 40
    - path: "app/Events/RecognitionAlertReceived.php"
      provides: "Broadcast event on fras.alerts with full denorm payload (D-12)"
      contains: "ShouldDispatchAfterCommit"
    - path: "config/fras.php"
      provides: "recognition section with thresholds + priority_map (D-05)"
      contains: "'recognition' =>"
    - path: "routes/channels.php"
      provides: "fras.alerts private channel auth (D-11)"
      contains: "fras.alerts"
    - path: "database/seeders/PersonOfInterestIncidentTypeSeeder.php"
      provides: "Idempotent IncidentType seeder for person_of_interest (D-02)"
      contains: "person_of_interest"
  key_links:
    - from: "routes/channels.php"
      to: "fras.alerts private channel"
      via: "Broadcast::channel('fras.alerts', ...)"
      pattern: "Broadcast::channel\\('fras\\.alerts'"
    - from: "app/Events/RecognitionAlertReceived.php"
      to: "fras.alerts"
      via: "PrivateChannel('fras.alerts')"
      pattern: "PrivateChannel\\('fras\\.alerts'\\)"
    - from: "database/seeders/DatabaseSeeder.php"
      to: "PersonOfInterestIncidentTypeSeeder"
      via: "call(PersonOfInterestIncidentTypeSeeder::class)"
      pattern: "PersonOfInterestIncidentTypeSeeder"
---

<objective>
Wave 1 of Phase 21: ship every contract that downstream code compiles against — failing test files (Nyquist gate), broadcast event class, new channel auth, config section, IncidentType seeder, and ROADMAP/REQUIREMENTS text amendments (D-25/D-26/D-27). Nothing functional yet; this plan unblocks Wave 2 parallel execution by making every interface the executors depend on concrete.

Purpose: Lock the test contract BEFORE implementation (Nyquist validation), land all "contracts" (event class, channel, config, seeder) as pure additions so Wave 2 implementers can reference final shapes without speculation, and align the spec text with locked user decisions D-01/D-17.

Output: 4 failing-spec test files (5 gates × 2 methods + payload + escalate + rail), 1 new event class, 1 new channel auth line, 1 new config section, 1 new seeder + registration, 2 documentation files amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-CONTEXT.md
@.planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-RESEARCH.md
@.planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-PATTERNS.md
@.planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-VALIDATION.md
@app/Events/CameraStatusChanged.php
@app/Events/IncidentCreated.php
@routes/channels.php
@config/fras.php
@database/seeders/IncidentTypeSeeder.php
@database/seeders/DatabaseSeeder.php
@tests/Feature/Intake/IoTWebhookTest.php
@tests/Feature/Fras/BroadcastAuthorizationTest.php
@tests/Feature/Intake/IntakeStationTest.php

<interfaces>
<!-- Executors use these exact shapes — no codebase exploration needed. -->

CameraStatusChanged event boilerplate (app/Events/CameraStatusChanged.php — verbatim model):
```php
<?php
namespace App\Events;

use Illuminate\Broadcasting\InteractsWithSockets;
use Illuminate\Broadcasting\PrivateChannel;
use Illuminate\Contracts\Broadcasting\ShouldBroadcast;
use Illuminate\Contracts\Events\ShouldDispatchAfterCommit;
use Illuminate\Foundation\Events\Dispatchable;
use Illuminate\Queue\SerializesModels;

final class CameraStatusChanged implements ShouldBroadcast, ShouldDispatchAfterCommit
{
    use Dispatchable, InteractsWithSockets, SerializesModels;

    public function __construct(public Camera $camera) {}

    public function broadcastOn(): array
    {
        return [new PrivateChannel('fras.cameras')];
    }

    public function broadcastWith(): array
    {
        return [ /* flat denorm */ ];
    }
}
```

routes/channels.php shape — $dispatchRoles already declared at top of file for [Operator, Dispatcher, Supervisor, Admin]. Existing fras.cameras block:
```php
Broadcast::channel('fras.cameras', function (User $user) use ($dispatchRoles): bool {
    return in_array($user->role, $dispatchRoles);
});
```

config/fras.php existing section shape:
```php
'cameras' => [
    'degraded_gap_s' => (int) env('FRAS_CAMERA_DEGRADED_GAP_S', 30),
    'offline_gap_s' => (int) env('FRAS_CAMERA_OFFLINE_GAP_S', 90),
],
```

IncidentTypeSeeder.php updateOrCreate pattern (D-02):
```php
IncidentType::updateOrCreate(
    ['code' => $code],
    [ /* attrs */ ]
);
```

RecognitionAlertReceived payload shape (D-12) — the 11 keys tests must assert:
event_id, camera_id, camera_id_display, camera_location ([lng, lat]), severity, personnel_id, personnel_name, personnel_category, confidence, captured_at, incident_id

Severity enum values: 'critical' | 'warning' | 'info' (lowercase backed values per Phase 18 D-64)
PersonnelCategory enum values: 'block' | 'missing' | 'lost_child' | 'allow'

UserRole enum cases: Admin, Supervisor, Operator, Dispatcher, Responder (5 cases, no Citizen).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship test scaffolding (Nyquist Wave 0) + new IncidentType seeder + DatabaseSeeder registration</name>
  <files>
    - tests/Feature/Fras/FrasIncidentFactoryTest.php (NEW)
    - tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php (NEW)
    - tests/Feature/Fras/EscalateToP1Test.php (NEW)
    - tests/Feature/Fras/IntakeStationFrasRailTest.php (NEW)
    - database/seeders/PersonOfInterestIncidentTypeSeeder.php (NEW)
    - database/seeders/DatabaseSeeder.php (MOD)
  </files>
  <read_first>
    - .planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-CONTEXT.md (D-01..D-23 — test cases derive from each decision)
    - .planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-RESEARCH.md §Pattern 6 Pest broadcast testing + §Wave 0 Requirements
    - .planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-PATTERNS.md §tests/Feature/Fras/ sections (pattern analogs with line numbers)
    - tests/Feature/Intake/IoTWebhookTest.php (Event::fake shape lines 10–12, assertion idiom)
    - tests/Feature/Fras/BroadcastAuthorizationTest.php (authAttempt helper lines 17–27, fras channel group pattern line 7)
    - tests/Feature/Intake/IntakeStationTest.php (Inertia assertInertia idiom lines 58–72)
    - tests/Feature/Intake/TriageIncidentTest.php (route-POST + assertDatabaseHas pattern lines 17–34)
    - database/seeders/IncidentTypeSeeder.php (updateOrCreate idempotent pattern lines 58–71)
    - database/seeders/DatabaseSeeder.php (call chain pattern)
    - app/Enums/RecognitionSeverity.php (enum values: critical/warning/info)
    - app/Enums/PersonnelCategory.php (enum values: block/missing/lost_child/allow)
    - app/Enums/UserRole.php (5 cases)
    - database/factories/RecognitionEventFactory.php (verify `similarity` and `severity` settable via ->create([...]))
    - database/factories/PersonnelFactory.php (verify `category` settable)
    - database/factories/CameraFactory.php (verify Camera factory exists)
  </read_first>
  <behavior>
    All 4 test files MUST run and FAIL with "class not found" / "route not found" / "prop missing" errors until Wave 2+ ships implementation. Each test exercises one contract:
    - FrasIncidentFactoryTest: 10 it() blocks covering the 5 gates × 2 methods + full payload (list below)
    - RecognitionAlertReceivedBroadcastTest: payload-shape assertion + channel auth matrix (4 allowed roles, 1 denied)
    - EscalateToP1Test: 3 it() blocks — FRAS incident escalate succeeds, non-FRAS incident hides button render path, dispatcher gate denies (403)
    - IntakeStationFrasRailTest: recentFrasEvents prop present + shape + 6th rail rendered

    PersonOfInterestIncidentTypeSeeder.php idempotency: calling twice results in ONE incident_types row with code='person_of_interest'.
  </behavior>
  <action>
    1. Create `tests/Feature/Fras/FrasIncidentFactoryTest.php`:
       - Starts with `<?php` + Pest `uses(RefreshDatabase::class);` + `pest()->group('fras');` (mirrors BroadcastAuthorizationTest.php line 7 convention)
       - Imports: `App\Enums\PersonnelCategory`, `App\Enums\RecognitionSeverity`, `App\Enums\IncidentChannel`, `App\Enums\IncidentPriority`, `App\Events\IncidentCreated`, `App\Events\RecognitionAlertReceived`, `App\Models\Camera`, `App\Models\Incident`, `App\Models\IncidentType`, `App\Models\Personnel`, `App\Models\RecognitionEvent`, `App\Services\FrasIncidentFactory`, `Illuminate\Support\Facades\Cache`, `Illuminate\Support\Facades\Event`
       - `beforeEach(fn () => Event::fake([IncidentCreated::class, RecognitionAlertReceived::class]))`
       - 10 `it()` blocks (one per gate/boundary, matching D-07):
         * `it('returns null and stays silent for Info severity')`
         * `it('broadcasts RecognitionAlertReceived then returns null for Warning severity with incident_id null')`
         * `it('returns null without broadcast for confidence below threshold')` — sets similarity=0.70 with threshold=0.75
         * `it('returns null for allow-category personnel')`
         * `it('returns null for unknown/null personnel_id')`
         * `it('returns null when dedup key already present within window')` — `Cache::add(...)` first then call factory
         * `it('creates P2 Incident for Critical × block-list personnel')` — asserts channel=IoT, priority=P2, incident_type->code='person_of_interest', timeline event_data.source='fras_recognition', recognition_event.incident_id==incident->id
         * `it('creates P1 Incident for Critical × lost_child personnel')`
         * `it('creates P2 Incident for Critical × missing personnel')`
         * `it('respects config overrides for confidence_threshold and dedup_window_seconds')` — sets `config(['fras.recognition.confidence_threshold' => 0.90])` and verifies a 0.85 similarity now fails the gate
       - Each success-path `it()` calls `app(FrasIncidentFactory::class)->createFromRecognition($event)` and asserts returned Incident + DB row + Event::assertDispatched on both IncidentCreated and RecognitionAlertReceived.
       - Factory setup uses `Camera::factory()->create()`, `Personnel::factory()->create(['category' => PersonnelCategory::Block])`, `RecognitionEvent::factory()->for($camera)->for($personnel)->create(['severity' => ..., 'similarity' => ...])`.
       - Add a `it('createFromSensor writes incident via factory with iot_sensor source')` covering D-06 sensor method (setup: `$incidentType = IncidentType::factory()->create()`, mapping array, validated payload; assert incident created + timeline event_data.source=='iot_sensor').
       - **This file WILL FAIL with "Class App\\Services\\FrasIncidentFactory not found" until Plan 02 ships. That failure is the Nyquist RED state.**

    2. Create `tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php`:
       - Headers: `<?php`, `uses(RefreshDatabase::class)`, `pest()->group('fras')`
       - Imports: `App\Enums\PersonnelCategory`, `App\Enums\RecognitionSeverity`, `App\Enums\UserRole`, `App\Events\RecognitionAlertReceived`, `App\Models\Camera`, `App\Models\Personnel`, `App\Models\RecognitionEvent`, `App\Models\User`
       - Helper `authAttempt(UserRole $role, string $channelName): TestResponse` copied verbatim from `tests/Feature/Fras/BroadcastAuthorizationTest.php` lines 17–27
       - `it('broadcasts on private fras.alerts channel with full denorm payload')`:
         * Creates Camera (set `location` via factory), Personnel (category=Block), RecognitionEvent (severity=Critical, similarity=0.85)
         * Instantiates `new RecognitionAlertReceived($event, null)` directly
         * Asserts `$event->broadcastOn()[0]->name === 'private-fras.alerts'`
         * Asserts `$event->broadcastWith()` has all 11 keys: event_id, camera_id, camera_id_display, camera_location ([lng, lat] array), severity ('critical'), personnel_id, personnel_name, personnel_category ('block'), confidence, captured_at (ISO-8601 string), incident_id (null because none passed)
       - `it('broadcasts with incident_id populated when incident passed')` — same shape, asserts incident_id === passed incident's id
       - `describe('fras.alerts channel auth', function() {...})` with `foreach ([Operator, Dispatcher, Supervisor, Admin] as $role)` each calling `authAttempt($role, 'private-fras.alerts')` and `expect(...)->getStatusCode())->toBeIn([200, 201])`; one `it('denies responder')` asserting 403.
       - **FAILS with "Class App\\Events\\RecognitionAlertReceived not found" OR "Unauthorized channel fras.alerts" — passes after Task 2 + 3 land.**

    3. Create `tests/Feature/Fras/EscalateToP1Test.php`:
       - Headers: `<?php`, `uses(RefreshDatabase::class)`, `pest()->group('fras')`
       - Imports: `App\Enums\IncidentPriority`, `App\Enums\IncidentChannel`, `App\Enums\UserRole`, `App\Models\Incident`, `App\Models\IncidentTimeline`, `App\Models\User`
       - `it('escalates FRAS-originated incident to P1 with fras_escalate_button trigger')`:
         * Creates supervisor user (`User::factory()->create(['role' => UserRole::Supervisor])`)
         * Creates incident (channel=IoT, priority=P2) + a seed IncidentTimeline row with event_type='incident_created' and event_data containing `'source' => 'fras_recognition'`
         * POSTs `route('intake.override-priority', $incident)` with `['priority' => 'P1', 'trigger' => 'fras_escalate_button']`
         * Asserts redirect + `$incident->fresh()->priority === IncidentPriority::P1` + a new IncidentTimeline row with event_type='priority_override' AND event_data.trigger='fras_escalate_button'
       - `it('defaults event_data.trigger to manual_override when trigger field absent')`:
         * Posts with only `['priority' => 'P1']` (no trigger)
         * Asserts new timeline event_data.trigger === 'manual_override' (v1.0 audit preservation)
       - `it('rejects invalid trigger values with 422')`:
         * Posts `['priority' => 'P1', 'trigger' => 'hacker_value']` — asserts 422 + validation error on `trigger`
       - `it('denies dispatcher on override-priority route')`:
         * Acts as Dispatcher role, POSTs same payload, asserts 403
       - **FAILS with 422 on `trigger` field until Plan 03 extends IntakeStationController::overridePriority() validation.**

    4. Create `tests/Feature/Fras/IntakeStationFrasRailTest.php`:
       - Headers: `<?php`, `uses(RefreshDatabase::class)`, `pest()->group('fras')`
       - Imports: `App\Enums\PersonnelCategory`, `App\Enums\RecognitionSeverity`, `App\Enums\UserRole`, `App\Models\Camera`, `App\Models\Personnel`, `App\Models\RecognitionEvent`, `App\Models\User`
       - `it('renders IntakeStation with recentFrasEvents prop carrying shaped payload')`:
         * Creates Camera + Personnel (block) + 2 RecognitionEvents (1 Critical, 1 Warning)
         * Also creates 1 Info severity event (MUST be excluded from the prop)
         * Acts as operator, GETs `route('intake.station')`
         * `assertInertia(fn ($page) => $page->component('intake/IntakeStation')->has('recentFrasEvents', 2)->has('recentFrasEvents.0.event_id')->has('recentFrasEvents.0.severity')->has('recentFrasEvents.0.camera_label')->has('recentFrasEvents.0.personnel_name')->has('recentFrasEvents.0.personnel_category')->has('recentFrasEvents.0.confidence')->has('recentFrasEvents.0.captured_at')->has('recentFrasEvents.0.incident_id')->has('recentFrasEvents.0.face_image_path'))`
       - `it('renders recentFrasEvents as empty array when no recognition events exist')`:
         * Acts as operator, GETs route, asserts `recentFrasEvents` array is empty + `assertInertia(...->has('frasConfig.pulseDurationSeconds'))`
       - `it('shares frasConfig Inertia prop with pulseDurationSeconds')`:
         * Acts as authenticated user, asserts any page response has `frasConfig.pulseDurationSeconds` shared (integer)
       - **FAILS with "Missing Inertia prop 'recentFrasEvents'" until Plan 03 adds prop + Plan 03 shares frasConfig.**

    5. Create `database/seeders/PersonOfInterestIncidentTypeSeeder.php` (D-02):
       ```php
       <?php

       namespace Database\Seeders;

       use App\Models\IncidentCategory;
       use App\Models\IncidentType;
       use Illuminate\Database\Seeder;

       final class PersonOfInterestIncidentTypeSeeder extends Seeder
       {
           public function run(): void
           {
               $categoryId = IncidentCategory::query()
                   ->where('name', 'Crime')
                   ->value('id');

               IncidentType::updateOrCreate(
                   ['code' => 'person_of_interest'],
                   [
                       'incident_category_id' => $categoryId,
                       'category' => 'Crime',
                       'name' => 'Person of Interest',
                       'default_priority' => 'P2',
                       'is_active' => true,
                       'show_in_public_app' => false,
                       'sort_order' => (int) (IncidentType::max('sort_order') ?? 0) + 1,
                   ]
               );
           }
       }
       ```
       Check the real column names/enum case values by reading `database/seeders/IncidentTypeSeeder.php` first — adapt attribute keys (`category` vs `incident_category_id`, `is_active` vs `active`) to match the shipped shape. Use `updateOrCreate` keyed on `code` for idempotency (D-02).

    6. Modify `database/seeders/DatabaseSeeder.php` — append a `$this->call(PersonOfInterestIncidentTypeSeeder::class);` line AFTER the existing `IncidentTypeSeeder::class` call so the category exists first.

    7. Run Pint: `vendor/bin/pint --dirty --format agent`

    8. Confirm test files are DISCOVERED (not yet passing):
       `php artisan test --compact --filter=Fras 2>&1 | head -40` — should show the 4 new Fras test files being run. Expected state: compile errors or missing-class errors. Record this RED state in the summary.
  </action>
  <verify>
    <automated>test -f tests/Feature/Fras/FrasIncidentFactoryTest.php && test -f tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php && test -f tests/Feature/Fras/EscalateToP1Test.php && test -f tests/Feature/Fras/IntakeStationFrasRailTest.php && test -f database/seeders/PersonOfInterestIncidentTypeSeeder.php && grep -q "PersonOfInterestIncidentTypeSeeder" database/seeders/DatabaseSeeder.php && grep -q "pest()->group('fras')" tests/Feature/Fras/FrasIncidentFactoryTest.php && grep -q "private-fras.alerts" tests/Feature/Fras/RecognitionAlertReceivedBroadcastTest.php && grep -q "fras_escalate_button" tests/Feature/Fras/EscalateToP1Test.php && grep -q "recentFrasEvents" tests/Feature/Fras/IntakeStationFrasRailTest.php && grep -q "person_of_interest" database/seeders/PersonOfInterestIncidentTypeSeeder.php</automated>
  </verify>
  <done>
    Four Wave 0 test files exist under `tests/Feature/Fras/`, each declared with `pest()->group('fras')`. PersonOfInterestIncidentTypeSeeder uses `updateOrCreate(['code' => 'person_of_interest'], ...)` and is registered in DatabaseSeeder. `php artisan migrate:fresh --seed --env=testing` completes green and seeds exactly one `person_of_interest` row (re-running does not create duplicates). Pint runs clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship RecognitionAlertReceived event + fras.alerts channel auth + config/fras.php recognition section + ROADMAP/REQUIREMENTS doc amendments</name>
  <files>
    - app/Events/RecognitionAlertReceived.php (NEW)
    - config/fras.php (MOD)
    - routes/channels.php (MOD)
    - .planning/ROADMAP.md (MOD)
    - .planning/REQUIREMENTS.md (MOD)
  </files>
  <read_first>
    - app/Events/CameraStatusChanged.php (event boilerplate — copy verbatim, change class name + channel + payload shape)
    - app/Events/IncidentCreated.php (eager-loaded denorm payload pattern)
    - routes/channels.php (verify `$dispatchRoles` var declared at top; add fras.alerts line after existing fras.cameras block per 21-PATTERNS.md §routes/channels.php)
    - config/fras.php (existing cameras/enrollment/photo section shape)
    - .planning/ROADMAP.md §Phase 21 (SC1 text + SC6 text to amend per D-25/D-27)
    - .planning/REQUIREMENTS.md §RECOGNITION (RECOGNITION-02 text to amend per D-26) and §INTEGRATION (INTEGRATION-03 text to amend per D-27)
    - .planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-CONTEXT.md §decisions D-05, D-11, D-12, D-25, D-26, D-27
    - .planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-RESEARCH.md §Pattern 2 ShouldDispatchAfterCommit + §Code Examples (Channel authorization + Config addition)
  </read_first>
  <action>
    1. Create `app/Events/RecognitionAlertReceived.php` (D-12) — copy `CameraStatusChanged.php` shape verbatim; structural differences:
       - Namespace `App\Events`
       - Imports: `App\Models\Incident`, `App\Models\RecognitionEvent`, `Illuminate\Broadcasting\InteractsWithSockets`, `Illuminate\Broadcasting\PrivateChannel`, `Illuminate\Contracts\Broadcasting\ShouldBroadcast`, `Illuminate\Contracts\Events\ShouldDispatchAfterCommit`, `Illuminate\Foundation\Events\Dispatchable`, `Illuminate\Queue\SerializesModels`
       - `final class RecognitionAlertReceived implements ShouldBroadcast, ShouldDispatchAfterCommit { use Dispatchable, InteractsWithSockets, SerializesModels; ... }`
       - Constructor signature: `public function __construct(public RecognitionEvent $event, public ?Incident $incident = null) {}`
       - `public function broadcastOn(): array { return [new PrivateChannel('fras.alerts')]; }`
       - `public function broadcastWith(): array { ... }` returning EXACTLY these 11 keys with these types (D-12):
         ```php
         $camera = $this->event->camera;
         $personnel = $this->event->personnel;
         $coords = $camera?->location;
         return [
             'event_id' => $this->event->id,
             'camera_id' => $this->event->camera_id,
             'camera_id_display' => $camera?->camera_id_display,
             'camera_location' => $coords ? [$coords->getLongitude(), $coords->getLatitude()] : null,
             'severity' => $this->event->severity->value,
             'personnel_id' => $this->event->personnel_id,
             'personnel_name' => $personnel?->name,
             'personnel_category' => $personnel?->category?->value,
             'confidence' => $this->event->similarity,
             'captured_at' => $this->event->captured_at->toIso8601String(),
             'incident_id' => $this->incident?->id,
         ];
         ```
       - Do NOT define `broadcastAs()` — let Laravel use default class basename (Pitfall 3).

    2. Modify `config/fras.php` — append a `'recognition'` section AFTER existing sections (verbatim from D-05):
       ```php
       'recognition' => [
           'confidence_threshold' => (float) env('FRAS_CONFIDENCE_THRESHOLD', 0.75),
           'dedup_window_seconds' => (int) env('FRAS_DEDUP_WINDOW_SECONDS', 60),
           'pulse_duration_seconds' => (int) env('FRAS_PULSE_DURATION_SECONDS', 3),
           'priority_map' => [
               'critical' => [
                   'block' => env('FRAS_PRIORITY_CRITICAL_BLOCK', 'P2'),
                   'missing' => env('FRAS_PRIORITY_CRITICAL_MISSING', 'P2'),
                   'lost_child' => env('FRAS_PRIORITY_CRITICAL_LOST_CHILD', 'P1'),
               ],
           ],
       ],
       ```

    3. Modify `routes/channels.php` — add a new channel authorization line AFTER the existing `fras.cameras` block (D-11), using the already-declared `$dispatchRoles` variable:
       ```php
       Broadcast::channel('fras.alerts', function (User $user) use ($dispatchRoles): bool {
           return in_array($user->role, $dispatchRoles);
       });
       ```
       Maintain same shape as fras.cameras line; do not alter $dispatchRoles declaration; responders stay excluded (not in the role set).

    4. Modify `.planning/ROADMAP.md` §Phase 21:
       - **SC1** (D-25): replace `"Critical-severity recognition event at ≥ 0.75 confidence against a block-list personnel"` with `"Critical-severity recognition event at ≥ 0.75 confidence against a block-list, missing, or lost-child personnel"`.
       - **SC6** (D-27): replace `"4th channel rail"` with `"6th channel rail"`.
       - Also fix the ROADMAP §Phase 21 Plans block (currently mis-copied from Phase 18 — lines 150-156). Replace those placeholder plan lines with this phase's actual plan list; leave placeholders as `[ ]` (unchecked) with filenames `21-01-PLAN.md` through `21-05-PLAN.md` and one-line objectives matching plan headers. (Note: the Plans block will be regenerated by the planner's `update_roadmap` step automatically; this edit is belt-and-suspenders.)

    5. Modify `.planning/REQUIREMENTS.md`:
       - **RECOGNITION-02** (D-26): replace the phrase `"block-list personnel"` with `"BOLO personnel (block / missing / lost_child categories; \`allow\` excluded)"` inside the RECOGNITION-02 acceptance criteria.
       - **INTEGRATION-03** (D-27): replace `"4th channel rail"` with `"6th channel rail"`.

    6. Run Pint: `vendor/bin/pint --dirty --format agent`.

    7. Re-run Fras test filter to confirm Task 1's broadcast/channel-auth tests now make progress (they should still FAIL on FrasIncidentFactory-dependent assertions but PASS on the RecognitionAlertReceived payload-shape test + channel-auth matrix):
       `php artisan test --compact --filter=RecognitionAlertReceivedBroadcastTest 2>&1 | tail -20`
       Expected: channel-auth matrix + direct `new RecognitionAlertReceived(...)` payload shape tests PASS; factory-invocation tests remain RED (Plan 02 gate).
  </action>
  <verify>
    <automated>test -f app/Events/RecognitionAlertReceived.php && grep -q "PrivateChannel('fras.alerts')" app/Events/RecognitionAlertReceived.php && grep -q "ShouldDispatchAfterCommit" app/Events/RecognitionAlertReceived.php && grep -q "'recognition' =>" config/fras.php && grep -q "FRAS_CONFIDENCE_THRESHOLD" config/fras.php && grep -q "FRAS_DEDUP_WINDOW_SECONDS" config/fras.php && grep -q "fras.alerts" routes/channels.php && grep -Eq "block-list,? missing,? or lost-child" .planning/ROADMAP.md && grep -q "6th channel rail" .planning/ROADMAP.md && grep -q "BOLO personnel" .planning/REQUIREMENTS.md && grep -q "6th channel rail" .planning/REQUIREMENTS.md && php artisan test --compact --filter=RecognitionAlertReceivedBroadcastTest 2>&1 | grep -E "(Tests:|PASS|authorizes|denies)" | head -20</automated>
  </verify>
  <done>
    RecognitionAlertReceived event class exists, implements ShouldBroadcast + ShouldDispatchAfterCommit, broadcasts on PrivateChannel('fras.alerts'), payload has exactly the 11 D-12 keys. `config/fras.php` has a 'recognition' section with the 4 keys. `routes/channels.php` authorizes fras.alerts for the 4 dispatch roles (using the existing `$dispatchRoles` var). ROADMAP SC1+SC6 and REQUIREMENTS RECOGNITION-02+INTEGRATION-03 text reflect D-25/D-26/D-27 amendments. The payload-shape test and channel-auth matrix in `RecognitionAlertReceivedBroadcastTest.php` pass (factory tests still RED — will close in Plan 02). Pint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT broker → Laravel listener | Raw recognition payloads cross here; Phase 19 already validates camera registration + schema; Phase 21 inherits |
| Laravel → Reverb → browser | Broadcast payload crosses here via `fras.alerts` private channel — denorm includes personnel PII (name, category) |
| Browser → Laravel | Broadcast auth request (`/broadcasting/auth`) validates user role against channel gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01-01 | Information Disclosure | `fras.alerts` private channel payload contains personnel_name, category | mitigate | Channel auth restricts to Operator/Dispatcher/Supervisor/Admin via `$dispatchRoles` in `routes/channels.php` — responders denied per D-11. Verified by EscalateToP1Test + RecognitionAlertReceivedBroadcastTest channel-auth matrix (Task 1 test scaffolding) |
| T-21-01-02 | Tampering | Seeder runs idempotently, could be hijacked to create rogue IncidentType | accept | Seeders run under admin context via `php artisan db:seed`; updateOrCreate keyed on immutable `code` column; no user input |
| T-21-01-03 | Repudiation | Missing audit trail for new recognition path | mitigate | Factory writes IncidentTimeline with `event_data.source='fras_recognition'` including recognition_event_id for full trace (D-03); covered by FrasIncidentFactoryTest gate tests in Task 1 |
</threat_model>

<verification>
- `vendor/bin/pint --test --format agent` clean
- `php artisan test --compact --filter=RecognitionAlertReceivedBroadcastTest` passes payload-shape + channel-auth tests
- `php artisan migrate:fresh --seed --env=testing` green; `SELECT COUNT(*) FROM incident_types WHERE code='person_of_interest'` == 1
- `php artisan config:show fras.recognition.confidence_threshold` returns `0.75`
- `grep -c "6th channel rail" .planning/ROADMAP.md .planning/REQUIREMENTS.md` >= 2
- `grep -q "fras.alerts" routes/channels.php` returns 0
</verification>

<success_criteria>
- Four Wave 0 test files exist under `tests/Feature/Fras/` declaring `pest()->group('fras')`
- `App\Events\RecognitionAlertReceived` class present with D-12 payload shape
- `config/fras.php` has `recognition` section with `confidence_threshold=0.75`, `dedup_window_seconds=60`, `pulse_duration_seconds=3`, `priority_map.critical.{block,missing,lost_child}` keys
- `routes/channels.php` authorizes `fras.alerts` for 4 dispatch roles; responder denied
- `PersonOfInterestIncidentTypeSeeder` idempotent on re-run (`migrate:fresh --seed` green; re-seed produces no duplicate)
- ROADMAP SC1 contains "block-list, missing, or lost-child"; SC6 + INTEGRATION-03 contain "6th channel rail"; REQUIREMENTS RECOGNITION-02 mentions "BOLO personnel (block / missing / lost_child)"
- Test state after Plan 01: factory-invocation tests RED (expected — closes in Plan 02); payload-shape + channel-auth tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/21-recognition-iot-intake-bridge-dispatch-map-intakestation-rai/21-01-SUMMARY.md` capturing:
- Exact grep count from final RED test failures (should list `FrasIncidentFactory not found` and similar — the expected RED gate for Plan 02)
- Registered seeder call order in DatabaseSeeder
- Any discretionary calls (e.g., whether IncidentCategory lookup used `where('name', 'Crime')` or alternative)
- Lines changed in ROADMAP.md and REQUIREMENTS.md
</output>
