---
phase: 05-responder-workflow
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - resources/js/components/responder/SceneTab.vue
  - resources/js/components/responder/ChecklistSection.vue
  - resources/js/components/responder/VitalsForm.vue
  - resources/js/components/responder/AssessmentTags.vue
  - resources/js/components/responder/ChatTab.vue
  - resources/js/components/responder/MessageBanner.vue
  - resources/js/pages/responder/Station.vue
autonomous: true
requirements: [RSPDR-05, RSPDR-06, RSPDR-07, RSPDR-08]

must_haves:
  truths:
    - "Responder can see and interact with contextual arrival checklists organized by incident type"
    - "Checklist completion percentage updates in real-time and broadcasts to dispatch"
    - "Responder can record patient vitals (BP, HR, SpO2, GCS) with validated input ranges"
    - "Responder can toggle assessment tags and each toggle auto-saves immediately"
    - "Responder can send quick-reply chips with a single tap (no confirmation)"
    - "Responder can type and send free-text messages to dispatch"
    - "Incoming dispatch messages show as a slide-down banner when on another tab"
    - "Unread message count updates on the Chat tab badge"
  artifacts:
    - path: "resources/js/components/responder/SceneTab.vue"
      provides: "Accordion container with Checklist, Vitals, and Assessment Tags sections"
    - path: "resources/js/components/responder/ChecklistSection.vue"
      provides: "Animated checkboxes with per-incident-type templates and progress bar"
    - path: "resources/js/components/responder/VitalsForm.vue"
      provides: "BP, HR, SpO2, GCS inputs with validation ranges and placeholders"
    - path: "resources/js/components/responder/AssessmentTags.vue"
      provides: "11 toggle chips with auto-save on each toggle"
    - path: "resources/js/components/responder/ChatTab.vue"
      provides: "Quick-reply chips + free text input + message history"
    - path: "resources/js/components/responder/MessageBanner.vue"
      provides: "Slide-down notification for incoming messages"
  key_links:
    - from: "ChecklistSection"
      to: "responder.update-checklist endpoint"
      via: "PATCH on each checkbox toggle"
      pattern: "updateChecklist|checklist_pct"
    - from: "AssessmentTags"
      to: "responder.update-assessment-tags endpoint"
      via: "PATCH on each tag toggle (auto-save)"
      pattern: "updateAssessmentTags|assessment_tags"
    - from: "VitalsForm"
      to: "responder.update-vitals endpoint"
      via: "PATCH on save button"
      pattern: "updateVitals|vitals"
    - from: "ChatTab"
      to: "responder.send-message endpoint"
      via: "POST on quick-reply tap or free text submit"
      pattern: "sendMessage|is_quick_reply"
    - from: "MessageBanner"
      to: "useResponderSession.messages"
      via: "Watch for new messages when not on chat tab"
      pattern: "unreadCount|activeTab.*chat"
---

<objective>
Scene documentation and messaging: SceneTab with accordion sections (Checklist, Vitals, Assessment Tags), ChatTab with quick-reply chips and free text, and MessageBanner for incoming message notifications.

Purpose: Build the two remaining tab content areas that enable responders to document scene data and communicate with dispatch. These are the core field-work features that capture incident data.

Output: Working SceneTab and ChatTab components wired to backend endpoints via Wayfinder, with auto-save behavior and real-time WebSocket message reception.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-responder-workflow/05-CONTEXT.md
@.planning/phases/05-responder-workflow/05-RESEARCH.md
@.planning/phases/05-responder-workflow/05-01-SUMMARY.md
@.planning/phases/05-responder-workflow/05-02-SUMMARY.md

<interfaces>
<!-- Types and composables from Plan 02 that this plan consumes -->

From resources/js/types/responder.ts (created in Plan 02):
```typescript
export interface ResponderIncident {
    id: number; incident_no: string; priority: IncidentPriority; status: IncidentStatus;
    incident_type: { id: number; name: string; code: string; category: string };
    vitals: VitalsData | null; assessment_tags: string[];
    checklist_data: Record<string, boolean> | null; checklist_pct: number;
    // ... other fields
}

export interface VitalsData {
    systolic_bp: number | null; diastolic_bp: number | null;
    heart_rate: number | null; spo2: number | null; gcs: number | null;
}

export interface ChecklistTemplate {
    id: string; label: string;
    items: Array<{ key: string; label: string }>;
}

export interface IncidentMessageItem {
    id: number; body: string; is_quick_reply: boolean;
    sender: { id: number; name: string; role: string } | null;
    sender_type: string; sender_id: number; created_at: string;
}
```

From resources/js/composables/useResponderSession.ts (created in Plan 02):
```typescript
export function useResponderSession(initialIncident, initialUnit, initialMessages, userId): {
    activeIncident: Ref<ResponderIncident | null>;
    currentStatus: ComputedRef<IncidentStatus | null>;
    messages: Ref<IncidentMessageItem[]>;
    unreadCount: Ref<number>;
    activeTab: Ref<ResponderTab>;
    // ... other state and methods
}
```

From app/Http/Controllers/ResponderController.php (created in Plan 01):
```
// Endpoints consumed by this plan:
// PATCH /responder/{incident}/checklist — updateChecklist (items: Record<string,boolean>)
// PATCH /responder/{incident}/vitals — updateVitals (systolic_bp, diastolic_bp, heart_rate, spo2, gcs)
// PATCH /responder/{incident}/assessment-tags — updateAssessmentTags (assessment_tags: string[])
// POST /responder/{incident}/message — sendMessage (body: string, is_quick_reply: boolean)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SceneTab with ChecklistSection, VitalsForm, and AssessmentTags accordion sections</name>
  <files>
    resources/js/components/responder/SceneTab.vue,
    resources/js/components/responder/ChecklistSection.vue,
    resources/js/components/responder/VitalsForm.vue,
    resources/js/components/responder/AssessmentTags.vue
  </files>
  <action>
    1. Create `SceneTab.vue` — accordion container:
    - Three collapsible sections: Checklist, Vitals, Assessment Tags
    - Only one section open at a time (clicking one closes the other)
    - Each section header shows: icon + label + progress indicator
      - Checklist: "Checklist 3/7" (completed/total)
      - Vitals: "Vitals 0/4" or "Vitals 3/4" (number of non-null fields out of 4 vitals: BP counts as 1 if either systolic or diastolic filled)
      - Assessment: "Assessment 2/11" (number of toggled tags out of 11)
    - Accordion animation: CSS transition max-height or grid-template-rows approach (smooth expand/collapse)
    - Props: receive incident data from useResponderSession (via inject or props from Station.vue)
    - Scrollable content area within each section

    2. Create `ChecklistSection.vue` — contextual checklists:
    - 4 hardcoded checklist templates (define inline or in a separate data file):
      - **Cardiac**: Scene secured, Patient responsive check, ABC assessment, Vital signs taken, AED/monitor attached, IV access established, Medication administered
      - **Road Accident**: Scene secured, Traffic control established, Vehicle stability assessed, Patient extrication (if needed), Spinal immobilization applied, Bleeding controlled, Patient assessed
      - **Structure Fire**: Scene secured, Fire suppression confirmed, Search completed, Hazmat assessment, Ventilation status, Patient triage, Decontamination (if needed)
      - **Default**: Scene secured, Area assessment complete, Hazards identified, Patient contacted, Initial assessment, Treatment provided, Documentation complete
    - Template selection logic: match incident_type.code or category to template. If category matches "fire" -> structure fire template, if category matches "medical" and code contains "cardiac"/"heart" -> cardiac, if code contains "accident"/"collision"/"vehicular" -> road accident, else default
    - Each item: animated checkbox (CSS transition on check state), item label
    - On checkbox toggle: update local state immediately, then PATCH to `responder.update-checklist` endpoint with full items object. Use direct fetch() with Wayfinder URL (not useForm — no page visit needed)
    - Progress bar at top: filled percentage based on completed/total
    - Props: `incident: ResponderIncident`
    - Initialize checklist state from `incident.checklist_data` if it exists, otherwise start all unchecked

    3. Create `VitalsForm.vue` — patient vitals input:
    - Fields: Systolic BP (mmHg), Diastolic BP (mmHg), Heart Rate (bpm), SpO2 (%), GCS (3-15)
    - Each field: number input with placeholder showing normal range, validation range shown as helper text
    - Validation ranges (client-side): systolic 50-300, diastolic 20-200, heart_rate 20-300, spo2 0-100, gcs 3-15
    - Show validation errors inline (red border + error text below input)
    - "Save Vitals" button at bottom: PATCH to `responder.update-vitals` endpoint
    - All fields nullable — responder fills what they can
    - Large number inputs with `inputmode="numeric"` for mobile keyboard
    - Initialize from `incident.vitals` if it exists
    - 44px min height on all inputs for touch targets
    - Props: `incident: ResponderIncident`

    4. Create `AssessmentTags.vue` — toggle chip grid:
    - 11 assessment tags: Conscious, Breathing, Bleeding, Unresponsive, Fracture, Burns, Shock, Chest Pain, Head Trauma, Airway Compromised, Anaphylaxis
    - Each tag: toggle chip (44px min height). Active state: filled accent color. Inactive: outlined/muted
    - Single-tap toggles the tag immediately
    - On toggle: update local state, then PATCH to `responder.update-assessment-tags` endpoint with full tags array. Fire-and-forget (no loading state needed). Use direct fetch() with Wayfinder URL
    - Grid layout: 2-3 columns depending on screen width (responsive)
    - Initialize from `incident.assessment_tags` array
    - color-mix() for tag background following Phase 8 design system pattern
    - Props: `incident: ResponderIncident`

    5. Run `npm run lint`, `npm run format`
  </action>
  <verify>
    <automated>npm run types:check && npm run build</automated>
  </verify>
  <done>
    - SceneTab renders accordion with 3 collapsible sections and progress indicators
    - ChecklistSection shows contextual checklist based on incident type with animated checkboxes
    - Each checkbox toggle sends PATCH to backend
    - VitalsForm shows 5 validated numeric inputs with mobile-friendly keyboard
    - AssessmentTags shows 11 toggle chips in responsive grid with auto-save
    - All components compile and build clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ChatTab with quick-reply chips and free text, MessageBanner for incoming messages, wire into Station.vue</name>
  <files>
    resources/js/components/responder/ChatTab.vue,
    resources/js/components/responder/MessageBanner.vue,
    resources/js/pages/responder/Station.vue
  </files>
  <action>
    1. Create `ChatTab.vue` — messaging interface:
    - Message history list: scrollable area showing all messages for the current incident, newest at bottom, auto-scroll on new message
    - Each message: sender name + role badge, body text, timestamp (Space Mono). Differentiate responder messages (right-aligned, accent color) from dispatch messages (left-aligned, muted background)
    - Quick-reply chips section (fixed above text input): 8 preset chips in a horizontally scrollable or wrapping layout:
      "On scene", "Need backup", "Patient stable", "Transporting", "All clear", "Copy that", "Stand by", "Negative"
    - Single tap on chip = message sent immediately (no confirmation). POST to `responder.send-message` with `{body: chipText, is_quick_reply: true}`. Use direct fetch() with Wayfinder URL
    - Free text input below chips: text input with send button (Send/Arrow icon). On submit: POST to `responder.send-message` with `{body: text, is_quick_reply: false}`. Clear input after send
    - 44px min height on all chips and input
    - On mount: mark all messages as read (set unreadCount to 0 in session)
    - When tab becomes active (watch activeTab): mark messages read
    - Props: `messages: IncidentMessageItem[]`, `incidentId: number`, `currentUserId: number`
    - Emits: `messages-read` (to update unread count in parent)

    2. Create `MessageBanner.vue` — slide-down notification for incoming messages:
    - Positioned fixed at top of content area (below topbar), z-50
    - Shows when: new message arrives AND responder is NOT on the chat tab
    - Content: sender name + truncated message body
    - Auto-dismisses after 4 seconds
    - Tap on banner: switches to chat tab (emits `go-to-chat`)
    - CSS slide-down animation: translateY(-100%) -> translateY(0) transition
    - Props: `message: IncidentMessageItem | null`, `isVisible: boolean`
    - Emits: `dismiss`, `go-to-chat`

    3. Update `Station.vue` to wire SceneTab and ChatTab:
    - Replace placeholder divs for 'scene' and 'chat' tabs with actual components
    - Pass correct props from useResponderSession to SceneTab and ChatTab
    - Add MessageBanner component: watch messages array for new additions, show banner when activeTab !== 'chat'
    - Wire MessageBanner go-to-chat to switch activeTab to 'chat'
    - Wire ChatTab messages-read to reset unreadCount

    4. Run `npm run lint`, `npm run format`, `npm run types:check`, `npm run build`
  </action>
  <verify>
    <automated>npm run types:check && npm run build</automated>
  </verify>
  <done>
    - ChatTab shows message history with sender differentiation
    - 8 quick-reply chips send messages with single tap
    - Free text input sends custom messages
    - MessageBanner slides down for incoming messages when not on chat tab
    - Station.vue renders SceneTab and ChatTab in their respective tabs
    - Unread count badge updates correctly
    - All TypeScript compiles and Vite builds clean
  </done>
</task>

</tasks>

<verification>
- `npm run types:check` — TypeScript compiles
- `npm run build` — Vite builds clean
- `npm run lint` — ESLint passes
- `php artisan test --compact --filter=Responder` — backend tests still pass (no regressions)
</verification>

<success_criteria>
- SceneTab accordion with Checklist, Vitals, and Assessment Tags sections
- Checklist auto-selects template based on incident type (4 templates)
- Vitals form validates ranges and saves via PATCH
- Assessment tags auto-save on each toggle via PATCH
- ChatTab with 8 quick-reply chips + free text messaging
- MessageBanner for cross-tab incoming message notifications
- Station.vue renders correct tab content for scene and chat tabs
</success_criteria>

<output>
After completion, create `.planning/phases/05-responder-workflow/05-03-SUMMARY.md`
</output>
