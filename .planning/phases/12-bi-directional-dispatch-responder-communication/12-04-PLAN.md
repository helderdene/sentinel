---
phase: 12-bi-directional-dispatch-responder-communication
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/components/dispatch/DispatchMessagesSection.vue
  - resources/js/components/responder/ChatTab.vue
autonomous: true
gap_closure: true
requirements: [COMM-06, COMM-12]

must_haves:
  truths:
    - "Dispatch Messages section header is visually prominent and easily discoverable in the incident detail panel"
    - "Responder Chat tab free text input and quick-reply chips are fully visible and not obscured by the StatusButton overlay"
  artifacts:
    - path: "resources/js/components/dispatch/DispatchMessagesSection.vue"
      provides: "Visually prominent collapsible Messages header"
      contains: "MESSAGES"
    - path: "resources/js/components/responder/ChatTab.vue"
      provides: "Chat input with bottom padding clearing StatusButton overlay"
      contains: "pb-"
  key_links:
    - from: "resources/js/components/dispatch/DispatchMessagesSection.vue"
      to: "resources/js/components/dispatch/IncidentDetailPanel.vue"
      via: "component rendering"
      pattern: "DispatchMessagesSection"
    - from: "resources/js/components/responder/ChatTab.vue"
      to: "resources/js/components/responder/StatusButton.vue"
      via: "visual layering (StatusButton fixed overlay must not cover ChatTab input)"
      pattern: "pb-\\["
---

<objective>
Fix two UAT-reported visual issues from Phase 12 testing:
1. Dispatch Messages section header too small (text-[9px]) to be noticed by users
2. Responder ChatTab free text input hidden behind the fixed-position StatusButton overlay

Purpose: Close UAT gaps so dispatch messaging and responder chat are fully usable
Output: Two patched Vue components with improved visual prominence and layout clearance
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bi-directional-dispatch-responder-communication/12-02-SUMMARY.md
@.planning/phases/12-bi-directional-dispatch-responder-communication/12-03-SUMMARY.md
@.planning/debug/dispatch-messages-not-visible.md
@.planning/debug/chat-input-hidden-by-status-btn.md

<interfaces>
<!-- Existing components being patched - no interface changes, only CSS/template modifications -->

From resources/js/components/dispatch/DispatchMessagesSection.vue:
- Props: incidentId, messages, currentUserId, expanded, unreadCount
- Emits: toggle, send
- Issue: Header text is text-[9px] font-mono -- too small to notice between Available Units and Timeline sections

From resources/js/components/responder/ChatTab.vue:
- Props: messages, incidentId, currentUserId
- Emits: messages-read
- Issue: No bottom padding; free text input sits at flex bottom where StatusButton overlaps ~96px

From resources/js/components/responder/StatusButton.vue (reference only, not modified):
- Uses `fixed inset-x-0 bottom-[80px]` positioning
- Total overlay height: ~96px (pt-8 + 52px button + pb-3)
- Only visible when currentStatus is DISPATCHED, ACKNOWLEDGED, EN_ROUTE, or ON_SCENE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make dispatch Messages header visually prominent</name>
  <files>resources/js/components/dispatch/DispatchMessagesSection.vue</files>
  <action>
    In DispatchMessagesSection.vue, update the collapsible header button to be more visually discoverable:

    1. Increase the header text size from `text-[9px]` to `text-[11px]` to match the TIMELINE section header sizing pattern used elsewhere in IncidentDetailPanel
    2. Add a small chat/message icon (inline SVG, 12x12) before the "MESSAGES" text label to provide a visual anchor. Use a simple speech bubble icon matching the design system style (stroke-based, currentColor).
    3. Add a top border `border-t border-t-border` to create a visual separator above the Messages section, distinguishing it from the Available Units section above it
    4. Change the text color from `text-t-text-faint` to `text-t-text-dim` for slightly more contrast while keeping the uppercase mono style

    Do NOT change the collapsed/expanded behavior, grid-template-rows animation, message list, quick-reply chips, or input area. Only the header button styling changes.

    Gap reason: User reported "There is no message section" -- debug confirmed the section renders but the 9px header is too small to notice.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Messages header is visually distinct with larger text (11px), chat icon, top border separator, and improved text contrast -- easily discoverable between Available Units and Timeline sections</done>
</task>

<task type="auto">
  <name>Task 2: Add bottom padding to ChatTab for StatusButton clearance</name>
  <files>resources/js/components/responder/ChatTab.vue</files>
  <action>
    In ChatTab.vue, add bottom padding to prevent the fixed-position StatusButton from covering the free text input area:

    1. Add `pb-[100px]` to the root div of ChatTab (the `flex flex-1 flex-col overflow-hidden` element at line 114). This provides ~100px of bottom clearance matching the StatusButton's ~96px visual overlay height (pt-8=32 + min-h-[52px] + pb-3=12 = 96px, rounded up to 100px for comfortable spacing).

    This is the correct fix location because:
    - ChatTab is the only tab with interactive content (text input) at the very bottom
    - Other tabs (AssignmentTab, NavTab, SceneTab) scroll their content and the StatusButton doesn't block critical interactive elements
    - Adding padding to ChatTab specifically (not ResponderLayout slot) avoids affecting other tabs unnecessarily
    - The padding provides a safe zone below the input so the StatusButton's gradient fade sits over empty space

    Do NOT modify StatusButton.vue positioning, ResponderLayout.vue structure, or Station.vue wrapper. Only add bottom padding to ChatTab's root element.

    Gap reason: StatusButton uses position:fixed at bottom-[80px] overlapping ~96px of content area. ChatTab places free text input flush at the bottom with no padding.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ChatTab free text input and quick-reply chips are fully visible above the StatusButton overlay with 100px bottom padding clearance</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run types:check` passes (no TypeScript errors introduced)
3. Visual: Dispatch console incident detail panel shows a clearly visible "MESSAGES" header with icon and border separator
4. Visual: Responder Chat tab input field and quick-reply chips are fully visible and not hidden behind the status transition button
</verification>

<success_criteria>
- DispatchMessagesSection header text is 11px with chat icon and top border -- visually prominent in the incident detail panel
- ChatTab has 100px bottom padding preventing StatusButton from covering the free text input
- Both components build without errors
- No regressions to existing messaging functionality (send, receive, quick-reply, collapse/expand)
</success_criteria>

<output>
After completion, create `.planning/phases/12-bi-directional-dispatch-responder-communication/12-04-SUMMARY.md`
</output>
