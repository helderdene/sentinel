---
phase: 09-create-a-public-facing-reporting-app
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - report-app/src/views/HomeView.vue
  - report-app/src/views/ReportTypeView.vue
  - report-app/src/views/ReportDetailsView.vue
  - report-app/src/views/ReportConfirmView.vue
  - report-app/src/views/MyReportsView.vue
  - report-app/src/views/TrackReportView.vue
  - report-app/src/views/AboutView.vue
  - report-app/src/App.vue
autonomous: false
requirements:
  - CITIZEN-01
  - CITIZEN-02
  - CITIZEN-03
  - CITIZEN-04
  - CITIZEN-05
  - CITIZEN-06
  - CITIZEN-07
  - CITIZEN-08
  - CITIZEN-09
  - CITIZEN-10

must_haves:
  truths:
    - "Citizen can see home screen with hero, CTA button, quick tips, recent reports, and emergency hotline"
    - "Citizen can select an incident type from a 2-column visual card grid"
    - "Citizen can provide location via GPS auto-detect or manual barangay dropdown"
    - "Citizen can fill out details (description, contact, name) and submit a report"
    - "After submission, citizen sees confirmation screen with tracking token and status pipeline"
    - "Citizen can view all their reports in My Reports tab (from localStorage)"
    - "Citizen can track any report by entering its token"
    - "About page shows CDRRMO info, data privacy, and app description"
  artifacts:
    - path: "report-app/src/views/HomeView.vue"
      provides: "Home screen matching prototype"
      min_lines: 80
    - path: "report-app/src/views/ReportTypeView.vue"
      provides: "Step 1: incident type selection grid"
      min_lines: 40
    - path: "report-app/src/views/ReportDetailsView.vue"
      provides: "Step 2: location + description form"
      min_lines: 80
    - path: "report-app/src/views/ReportConfirmView.vue"
      provides: "Step 3: confirmation with status pipeline"
      min_lines: 60
    - path: "report-app/src/views/MyReportsView.vue"
      provides: "Report list + track by ID"
      min_lines: 60
    - path: "report-app/src/views/TrackReportView.vue"
      provides: "Single report tracking view"
      min_lines: 40
  key_links:
    - from: "ReportDetailsView.vue"
      to: "useApi.post"
      via: "Form submit -> POST /api/v1/citizen/reports"
      pattern: "api/v1/citizen/reports"
    - from: "ReportTypeView.vue"
      to: "useApi.get"
      via: "Fetch incident types on mount"
      pattern: "api/v1/citizen/incident-types"
    - from: "ReportDetailsView.vue"
      to: "useGeolocation"
      via: "GPS auto-detect on mount"
      pattern: "useGeolocation"
    - from: "ReportConfirmView.vue"
      to: "useReportStorage.addReport"
      via: "Save to localStorage after submission"
      pattern: "addReport"
    - from: "MyReportsView.vue"
      to: "useReportStorage.getReports"
      via: "Load from localStorage"
      pattern: "getReports"
    - from: "TrackReportView.vue"
      to: "useApi.get"
      via: "GET /api/v1/citizen/reports/{token}"
      pattern: "api/v1/citizen/reports/"
---

<objective>
Build all 7 view components for the citizen report app, implementing the complete reporting flow (home -> type selection -> details -> confirmation), My Reports, Track by ID, and About -- all matching the HTML prototype design.

Purpose: Delivers the complete citizen-facing UI. This is the plan that makes the app usable end-to-end.
Output: Fully functional citizen reporting SPA with all screens matching the prototype at docs/irms-report-app.html.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-create-a-public-facing-reporting-app/09-CONTEXT.md
@.planning/phases/09-create-a-public-facing-reporting-app/09-RESEARCH.md
@.planning/phases/09-create-a-public-facing-reporting-app/09-01-SUMMARY.md
@.planning/phases/09-create-a-public-facing-reporting-app/09-02-SUMMARY.md
@docs/irms-report-app.html

<interfaces>
<!-- From Plan 01 (backend API) -->
POST /api/v1/citizen/reports
  Request: { incident_type_id, description, caller_contact, caller_name?, location_text?, latitude?, longitude?, barangay_id? }
  Response 201: { data: { tracking_token, type, category, priority, status, barangay, location_text, description, submitted_at } }

GET /api/v1/citizen/reports/{token}
  Response 200: { data: { tracking_token, type, category, priority, status, barangay, location_text, description, submitted_at } }

GET /api/v1/citizen/incident-types
  Response 200: { data: [{ id, name, category, code, default_priority, description }] }

GET /api/v1/citizen/barangays
  Response 200: { data: [{ id, name }] }

<!-- From Plan 02 (SPA scaffold) -->
Types: IncidentType, StoredReport, CitizenReport, Barangay, CITIZEN_STATUS_MAP, STATUS_SEQUENCE, STATUS_COLORS, PRIORITY_COLORS, PRIORITY_LABELS, PRIORITY_BG
Composables: useApi (get, post), useGeolocation (requestLocation, latitude, longitude, status), useReportStorage (getReports, addReport, updateReportStatus)
Components: BottomNav, PriorityBadge, StatusBadge, StatusPipeline, TypeCard, StepIndicator
Router: /, /report/type, /report/details, /report/confirm, /reports, /track/:token, /about
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Home, type selection, details form, and confirmation views (report flow)</name>
  <files>
    report-app/src/views/HomeView.vue
    report-app/src/views/ReportTypeView.vue
    report-app/src/views/ReportDetailsView.vue
    report-app/src/views/ReportConfirmView.vue
    report-app/src/App.vue
  </files>
  <action>
    IMPORTANT: Follow the prototype at docs/irms-report-app.html closely for visual design, layout, spacing, colors, and interactions. Use the design tokens and components from Plan 02.

    1. HomeView.vue (matches prototype HomeScreen):
       - Hero section: dark brand background (#1e3a6e), decorative rings (absolute positioned circles with white borders at low opacity), shield icon + "CDRRMO Butuan City" + "EMERGENCY REPORTING" label, large heading "Report an Emergency", subtitle text
       - Red CTA button: "Report Emergency Now" with alert triangle icon, full-width, rounded-xl, red shadow, navigates to /report/type
       - Quick tips section: "QUICK TIPS" label (Space Mono uppercase), 3 tip cards in a row ("Stay calm", "Be specific", "Stay safe")
       - Recent reports: "RECENT REPORTS" label, list of up to 3 recent reports from useReportStorage, each with: type icon placeholder (priority-colored bg), type name, barangay + elapsed time, StatusBadge
       - Emergency hotline card: red-tinted bg, phone icon, "Emergency Hotline", phone number in Space Mono
       - Scrollable content area

    2. ReportTypeView.vue (matches prototype TypeScreen):
       - Header bar: back button (chevron-left in bordered circle), "What happened?" title, "Select the type of emergency" subtitle
       - StepIndicator showing step 1 of 3
       - Scrollable 2-column grid of TypeCard components
       - Fetch incident types on mount via useApi.get('/api/v1/citizen/incident-types')
       - On card select: store selected type in route state (router.push with state) or a shared composable, navigate to /report/details after 200ms delay
       - Show loading skeleton while fetching types
       - No BottomNav on this screen (report flow screens hide bottom nav)

    3. ReportDetailsView.vue (matches prototype DetailScreen):
       - Header bar: back button -> /report/type, "Details" title, "Provide location & info" subtitle
       - StepIndicator showing step 2 of 3
       - Location section:
         a. On mount, call useGeolocation.requestLocation()
         b. If granted: show green "Location detected" pill with coordinates, auto-lookup barangay by sending lat/lng to backend (or show detected barangay name)
         c. If denied: show "Location unavailable" orange pill, show manual barangay dropdown (fetched from /api/v1/citizen/barangays) and address text input
         d. Manual address input always visible for additional context
       - Description textarea: "Describe the emergency" placeholder, min 10 chars, character counter
       - Contact number input: required, tel type, "09XX-XXX-XXXX" placeholder
       - Name input: optional, "Your name (optional)"
       - Submit button: full-width, blue accent, "Submit Report" with loading state
       - On submit:
         a. Validate all fields client-side (show inline errors)
         b. POST to /api/v1/citizen/reports with all data
         c. On success: save to localStorage via useReportStorage.addReport(), navigate to /report/confirm with the response data in route state
         d. On 422: show server validation errors
         e. On 429: show rate limit message

    4. ReportConfirmView.vue (matches prototype SubmittedScreen):
       - Success animation: checkmark circle with draw animation (SVG stroke-dasharray animation matching prototype)
       - "Report Submitted" heading
       - Tracking token displayed prominently: "YOUR TRACKING ID" label (Space Mono), large token text in blue with monospace font
       - Report summary card: type, priority badge, location, description
       - StatusPipeline showing current status (Received -- step 1 of 4)
       - "What happens next?" info section with 3 steps
       - Two buttons: "Track This Report" -> /track/{token}, "Report Another" -> /report/type
       - "Done" button at bottom -> / (home)
       - No BottomNav on this screen

    5. Update App.vue:
       - Add route-based transition animations matching prototype (slide left/right)
       - Hide BottomNav when route path starts with /report/ (report flow screens)
       - Show BottomNav on /, /reports, /track/*, /about
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS/report-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Home screen renders with hero, CTA, tips, recent reports, hotline card. Type selection shows grid of API-fetched types. Details form collects location (GPS or manual), description, contact. Confirmation shows token, status pipeline. Report flow navigates correctly between all 4 screens. Reports saved to localStorage on submission.</done>
</task>

<task type="auto">
  <name>Task 2: My Reports, Track by ID, and About views</name>
  <files>
    report-app/src/views/MyReportsView.vue
    report-app/src/views/TrackReportView.vue
    report-app/src/views/AboutView.vue
  </files>
  <action>
    IMPORTANT: Follow the prototype at docs/irms-report-app.html closely for visual design.

    1. MyReportsView.vue (matches prototype MyReportsScreen):
       - Header: "My Reports" title
       - "Track by ID" input section at top: text input for tracking token + "Track" button
         - On track: navigate to /track/{token}
       - Reports list from useReportStorage.getReports():
         - Each report card: priority-colored icon area, type name, barangay + elapsed time, StatusBadge
         - Tapping a report navigates to /track/{token}
         - On visit, refresh status for each stored report by calling GET /api/v1/citizen/reports/{token} for each (poll on visit per decision -- update localStorage with fresh status)
       - Empty state: illustration/icon + "No reports yet" + "Reports you submit will appear here" + CTA to report
       - Loading state while refreshing statuses

    2. TrackReportView.vue:
       - Accepts :token route param
       - On mount: fetch report via useApi.get('/api/v1/citizen/reports/{token}')
       - Header: back button, "Track Report" title
       - If found:
         - Tracking token displayed (Space Mono, blue)
         - Report type + PriorityBadge
         - StatusPipeline showing current status position
         - Detail card: barangay, location text, description, submitted time
         - "Refresh Status" button to re-fetch
       - If not found (404):
         - "Report not found" message with suggestion to check token
       - Loading state while fetching

    3. AboutView.vue (matches prototype AboutScreen):
       - Header: "About" title
       - App info card:
         - IRMS Report App name + version (v1.0)
         - "CDRRMO Butuan City Emergency Reporting System" description
       - "How It Works" section with 3 numbered steps:
         1. Select emergency type
         2. Provide location & details
         3. Track your report status
       - "Data Privacy" section:
         - Brief privacy statement: contact info used only for emergency response, no account required, reports processed by CDRRMO
       - "Emergency Contacts" section:
         - CDRRMO Hotline number
         - National Emergency Hotline (911)
       - Footer: "Powered by IRMS" + year

    Verify the build still succeeds.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS/report-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>My Reports shows localStorage reports with refreshed statuses on visit. Track by ID fetches and displays single report with status pipeline. About shows CDRRMO info, how it works, privacy, contacts. All views match prototype visual style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete citizen report app</name>
  <files>report-app/src/views/*.vue</files>
  <action>
    Human verifies the complete citizen report app by running both servers and testing the full flow in a browser. All automated work was completed in Tasks 1 and 2.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS/report-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms all screens match prototype, full report flow works end-to-end, mobile responsive at 390px, dark mode works</done>
  <what-built>Complete citizen report app SPA with 7 views (Home, Type Selection, Details, Confirmation, My Reports, Track Report, About) matching the HTML prototype design, consuming the Laravel API at /api/v1/citizen/*</what-built>
  <how-to-verify>
    1. Start the report app dev server: `cd report-app && npm run dev`
    2. Start the main Laravel app: `composer run dev` (in another terminal)
    3. Open http://localhost:5174 in your browser

    Test the full flow:
    a. Home screen: verify hero section, red CTA button, quick tips, emergency hotline card
    b. Tap "Report Emergency Now" -> type selection grid appears with incident types from API
    c. Select a type (e.g., "Road Accident") -> details form appears
    d. Allow/deny GPS location -> verify location detection or manual barangay dropdown
    e. Fill in description (10+ chars), contact number, submit
    f. Confirmation screen shows tracking token and "Received" status in pipeline
    g. Navigate to "My Reports" tab -> submitted report appears in list
    h. Tap a report -> tracking view shows report details and status pipeline
    i. Enter a tracking token manually in "Track by ID" field
    j. Check "About" page for CDRRMO info
    k. Verify dark mode if system is set to dark (prefers-color-scheme)
    l. Test mobile responsiveness (Chrome DevTools mobile mode, 390px width)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd report-app && npm run build` completes without errors
- All 7 views render correctly and match prototype
- Full report submission flow works end-to-end (creates incident via API)
- Report tracking works via token lookup
- My Reports loads from localStorage and refreshes statuses on visit
- BottomNav hidden during report flow, visible on other screens
- Mobile-first layout works at 390px width
</verification>

<success_criteria>
- Complete citizen report flow: Home -> Type -> Details -> Confirm
- GPS auto-detect with manual barangay fallback
- Report creates Incident via API (visible in operator intake feed)
- 8-char tracking token returned and displayed
- StatusPipeline shows Received -> Verified -> Dispatched -> Resolved progression
- My Reports lists localStorage reports with refreshed statuses
- Track by ID lookup works for any valid token
- About page shows CDRRMO info and privacy notice
- Visual design matches HTML prototype (colors, fonts, spacing, layout)
- Mobile-first, system dark mode
</success_criteria>

<output>
After completion, create `.planning/phases/09-create-a-public-facing-reporting-app/09-03-SUMMARY.md`
</output>
