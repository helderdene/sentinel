---
phase: 10-update-all-pages-design-to-match-irms-intake-design-system
plan: 04
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - resources/js/pages/analytics/Dashboard.vue
  - resources/js/pages/analytics/Heatmap.vue
  - resources/js/pages/analytics/Reports.vue
  - resources/js/pages/dispatch/Console.vue
  - resources/js/pages/responder/Station.vue
  - resources/js/layouts/ResponderLayout.vue
autonomous: false
requirements:
  - DS-10
  - DS-11
  - DS-12

must_haves:
  truths:
    - "Analytics pages use design system tokens for cards, filters, typography"
    - "Dispatch Console uses design system color/font tokens but layout structure is unchanged"
    - "Responder Station uses design system color/font tokens but layout structure is unchanged"
    - "All pages across the entire app use consistent design system visual language"
  artifacts:
    - path: "resources/js/pages/analytics/Dashboard.vue"
      provides: "Analytics dashboard with design system tokens"
      min_lines: 30
    - path: "resources/js/pages/dispatch/Console.vue"
      provides: "Dispatch console with token alignment"
      min_lines: 50
  key_links:
    - from: "resources/css/app.css"
      to: "all pages"
      via: "CSS variable cascade"
      pattern: "var\\(--t-"
---

<objective>
Restyle analytics pages with full design system treatment, align dispatch console and responder station to design system tokens (colors/fonts only), and verify the complete visual consistency across the entire application.

Purpose: Analytics pages get the full design system treatment (cards, filters, chart styling). Dispatch Console and Responder Station are specialized environments that keep their purpose-built layouts but swap to design system color/font tokens for visual consistency. This completes the design system migration across all pages.

Output: 3 analytics pages restyled, 2 specialized pages token-aligned, visual verification of entire app.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-CONTEXT.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-RESEARCH.md
@docs/IRMS-Intake-Design-System.md
@resources/css/app.css
@resources/js/pages/analytics/Dashboard.vue
@resources/js/pages/dispatch/Console.vue
@resources/js/pages/responder/Station.vue

<interfaces>
<!-- Analytics pages use Chart.js for sparklines and line charts -->
<!-- Chart colors are currently hardcoded hex -- keep as-is per research recommendation -->
<!-- Analytics filter bar uses Shadcn Select, Input components (inherit CSS variables) -->

<!-- Dispatch Console uses MapLibre GL JS, custom WebGL layers -->
<!-- Do NOT change map styling, layer definitions, or panel layout -->
<!-- Only change color/font tokens in panel Chrome (headers, borders, text) -->

<!-- Responder Station uses custom touch-optimized layout -->
<!-- Do NOT change touch targets, tab structure, or mobile layout -->
<!-- Only change color/font tokens in component chrome -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle analytics pages with full design system treatment</name>
  <files>resources/js/pages/analytics/Dashboard.vue, resources/js/pages/analytics/Heatmap.vue, resources/js/pages/analytics/Reports.vue</files>
  <action>
**Read each analytics page file first** to understand current class usage, then apply design system tokens.

**For analytics/Dashboard.vue (KPI dashboard):**
- KPI cards: Use `bg-card rounded-[var(--radius)] border border-border shadow-[var(--shadow-1)]` pattern
- KPI labels (metric names): Use `font-mono text-[9px] font-bold tracking-[2px] text-t-text-faint uppercase` (Space Mono section label pattern)
- KPI values: Use `text-foreground text-2xl font-semibold` (DM Sans)
- KPI change indicators: green for positive (text-t-online), red for negative (text-t-p1)
- Filter bar: Shadcn Select/Input components inherit CSS variables. Ensure the filter container has design system border/background
- Chart containers: Wrap in card pattern. Chart.js colors stay as hardcoded hex values (they already match the design system palette per research findings)
- Replace ALL `text-neutral-*`, `bg-neutral-*`, `border-neutral-*` with design system equivalents
- Sparkline/chart wrapper: `bg-card rounded-[var(--radius)] border border-border p-4 shadow-[var(--shadow-1)]`

**For analytics/Heatmap.vue (choropleth map):**
- Map container: Keep MapLibre map styling as-is
- Filter controls: Apply design system tokens to the filter bar/sidebar
- Legend: Use design system priority/density colors with `font-mono text-[10px]` labels
- Export button: Should use Shadcn Button which inherits design system primary color
- Replace any hardcoded color classes

**For analytics/Reports.vue (report download center):**
- Report cards/list: Use `bg-card rounded-[var(--radius)] border border-border shadow-[var(--shadow-1)]` pattern
- Report type labels: `font-mono text-[9px] font-bold tracking-[2px] text-t-text-faint uppercase`
- Download buttons: Shadcn Button inherits design system styling
- Date/status info: Use `text-muted-foreground` and `font-mono` for dates/IDs
- If there's a table of generated reports, apply the same data table pattern as Plan 03

For all pages: Replace ALL instances of `text-neutral-*`, `bg-neutral-*`, `border-neutral-*`, `text-zinc-*`, `bg-zinc-*` with design system equivalents.

Run `npm run lint` and `npm run format` after all changes.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Analytics Dashboard uses design system cards with Space Mono KPI labels. Heatmap uses design system filter styling. Reports page uses design system card/table patterns. All hardcoded color classes replaced with design system tokens. Frontend builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Token-align Dispatch Console and Responder Station</name>
  <files>resources/js/pages/dispatch/Console.vue, resources/js/pages/responder/Station.vue, resources/js/layouts/ResponderLayout.vue</files>
  <action>
**CRITICAL: Token alignment ONLY. Do NOT change layout structure, custom UX patterns, or purpose-built interfaces. These are specialized operational environments.**

**For dispatch/Console.vue:**
Read the file first. The dispatch console was built in Phase 4 with its own DispatchLayout. It likely already uses many design system tokens from Phase 8's token definitions. The goal is to verify and fill gaps:

1. **Scan for hardcoded color classes:** Replace any remaining `text-neutral-*`, `bg-neutral-*`, `border-neutral-*`, `text-zinc-*`, `bg-zinc-*`, `text-gray-*`, `bg-gray-*` with design system equivalents
2. **Panel chrome (headers, section titles):** If panel headers use generic font styles, update to Space Mono for section labels and DM Sans for content
3. **Borders:** Replace any `border-gray-*`, `border-neutral-*` with `border-border` or `border-t-border`
4. **Do NOT touch:** Map styling, WebGL layer definitions, marker configurations, layout flex structure, panel sizes, audio alert logic, assignment workflows
5. **Priority/status colors:** Verify they already use t-p1/t-p2/t-p3/t-p4 and t-unit-* tokens. If not, swap to design system tokens.

**For responder/Station.vue and ResponderLayout.vue:**
Read both files first. The responder station was built in Phase 5 with mobile-optimized touch targets. Token alignment only:

1. **Scan for hardcoded color classes** in Station.vue and ResponderLayout.vue
2. **Replace:** `text-neutral-*`, `bg-neutral-*`, `border-neutral-*`, `text-zinc-*`, `bg-zinc-*` with design system equivalents
3. **Font tokens:** Verify DM Sans for content, Space Mono for data/labels/timestamps
4. **Do NOT touch:** Tab structure, touch targets (44px min), status transition buttons, GPS tracking, WebSocket event handlers, mobile layout breakpoints
5. **Priority/status colors:** Verify they use t-p* and design system status tokens

Also check these responder component files for hardcoded colors and update if found:
- `resources/js/components/responder/AssignmentTab.vue`
- `resources/js/components/responder/ChatTab.vue`
- `resources/js/components/responder/NavTab.vue`
- `resources/js/components/responder/OutcomeSheet.vue`
- `resources/js/components/responder/ClosureSummary.vue`
- `resources/js/components/responder/ResourceRequestModal.vue`

For each responder component: ONLY replace hardcoded neutral-*/zinc-*/gray-* classes with design system tokens. Do NOT change component structure, props, event handlers, or layout.

Run `npm run lint` and `npm run format` after all changes.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5 && php artisan test --compact 2>&1 | tail -10</automated>
  </verify>
  <done>Dispatch Console and Responder Station use design system color/font tokens. No layout or UX changes made to either specialized environment. All remaining hardcoded neutral-*/zinc-* classes replaced. Full test suite passes. Frontend builds cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete design system migration</name>
  <what-built>Complete design system migration across all IRMS pages: CSS variable foundation, auth pages with CDRRMO branding, sidebar with IRMS branding, dashboard, admin data tables, settings, incidents, analytics, dispatch console (tokens only), responder station (tokens only)</what-built>
  <how-to-verify>
    1. Visit the login page -- verify CDRRMO branding (shield icon, "CDRRMO Butuan City", subtitle), centered card with shadow, fadeUp animation
    2. Log in as admin -- verify sidebar shows CDRRMO icon + "IRMS" (not "Laravel Starter Kit"), section labels in Space Mono uppercase
    3. Check Dashboard -- verify cards use design system elevation and colors (no generic gray), content area has light background
    4. Check Admin > Users -- verify table has Space Mono column headers, role badges use design system role colors, Level 1 shadow
    5. Check Settings > Profile -- verify design system typography and card patterns
    6. Check Incidents list -- verify design system table pattern, priority badges use P1-P4 colors
    7. Check Analytics Dashboard -- verify KPI cards with Space Mono labels
    8. Check Dispatch Console -- verify visual consistency (colors/fonts) while layout remains unchanged
    9. Check Responder Station (if accessible) -- verify visual consistency while mobile layout remains unchanged
    10. Toggle dark mode on any page -- verify dark theme tokens are correct (no missing or invisible elements)
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any visual issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `php artisan test --compact` -- full test suite passes
3. `npm run types:check` -- TypeScript compiles cleanly
4. `npm run lint` -- ESLint passes
5. Visual verification across all page groups (auth, sidebar, dashboard, admin, settings, incidents, analytics, dispatch, responder)
6. Dark mode verification on at least 3 page groups
</verification>

<success_criteria>
- Analytics pages use design system cards, Space Mono labels, chart containers
- Dispatch Console uses design system tokens but layout/UX unchanged
- Responder Station uses design system tokens but layout/UX unchanged
- Full test suite passes
- Frontend builds with no errors
- Visual consistency verified by human across all page groups
- No remaining neutral-*/zinc-* hardcoded color classes anywhere in the affected pages
</success_criteria>

<output>
After completion, create `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-04-SUMMARY.md`
</output>
