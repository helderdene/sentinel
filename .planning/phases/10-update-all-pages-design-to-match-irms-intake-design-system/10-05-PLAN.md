---
phase: 10-update-all-pages-design-to-match-irms-intake-design-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/js/components/TextLink.vue
  - resources/js/pages/auth/TwoFactorChallenge.vue
  - resources/js/components/analytics/ReportRow.vue
  - resources/js/components/incidents/PrioritySelector.vue
  - resources/js/components/incidents/IncidentTimeline.vue
autonomous: true
gap_closure: true
requirements: [DS-01, DS-04, DS-09, DS-10]

must_haves:
  truths:
    - "All auth pages render with zero hardcoded neutral-* classes"
    - "ReportRow.vue type and status badges use color-mix() with design system tokens"
    - "PrioritySelector.vue uses t-p1 through t-p4 tokens instead of hardcoded red/orange/amber/green"
    - "IncidentTimeline.vue uses design system text classes instead of text-neutral-*"
  artifacts:
    - path: "resources/js/components/TextLink.vue"
      provides: "Design system underline decoration"
      contains: "decoration-muted-foreground"
    - path: "resources/js/pages/auth/TwoFactorChallenge.vue"
      provides: "Design system underline decoration on toggle buttons"
      contains: "decoration-muted-foreground"
    - path: "resources/js/components/analytics/ReportRow.vue"
      provides: "Design system color-mix() badges for report types and statuses"
      contains: "color-mix"
    - path: "resources/js/components/incidents/PrioritySelector.vue"
      provides: "Design system priority token colors"
      contains: "t-p1"
    - path: "resources/js/components/incidents/IncidentTimeline.vue"
      provides: "Design system text colors"
      contains: "text-foreground"
  key_links:
    - from: "resources/js/components/TextLink.vue"
      to: "resources/js/pages/auth/Login.vue"
      via: "component import"
      pattern: "TextLink"
    - from: "resources/js/components/analytics/ReportRow.vue"
      to: "resources/js/pages/analytics/Reports.vue"
      via: "component import"
      pattern: "ReportRow"
---

<objective>
Close 2 verification gaps and 2 anti-patterns from Phase 10 verification: residual hardcoded neutral-*/color-* classes in 5 component files that should use design system tokens.

Purpose: Achieve 12/12 truth verification for Phase 10 design system alignment. All gaps are style-only -- replacing hardcoded Tailwind color classes with design system CSS variable tokens.

Output: 5 files updated with zero hardcoded neutral-*/blue-*/purple-*/green-*/amber-*/yellow-*/red-*/orange-* color classes.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md

<interfaces>
<!-- Design system color-mix() badge pattern (established in plans 02-03, used throughout) -->
<!-- From resources/js/pages/incidents/Index.vue: -->
const priorityBadgeClass: Record<string, string> = {
    P1: 'bg-[color-mix(in_srgb,var(--t-p1)_12%,transparent)] text-t-p1',
    P2: 'bg-[color-mix(in_srgb,var(--t-p2)_12%,transparent)] text-t-p2',
    P3: 'bg-[color-mix(in_srgb,var(--t-p3)_12%,transparent)] text-t-p3',
    P4: 'bg-[color-mix(in_srgb,var(--t-p4)_12%,transparent)] text-t-p4',
};

<!-- Available design system tokens from resources/css/app.css: -->
<!-- Priority: --t-p1 (#dc2626), --t-p2 (#ea580c), --t-p3 (#ca8a04), --t-p4 (#16a34a) -->
<!-- Semantic: --t-accent (#2563eb), --t-online (#16a34a) -->
<!-- Roles: --t-role-supervisor (#7c3aed), --t-role-admin (#0f766e) -->
<!-- Shadcn: text-foreground, text-muted-foreground (already mapped to design system via CSS cascade) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix auth decoration classes and IncidentTimeline neutral classes</name>
  <files>resources/js/components/TextLink.vue, resources/js/pages/auth/TwoFactorChallenge.vue, resources/js/components/incidents/IncidentTimeline.vue</files>
  <action>
**TextLink.vue (line 21):** Replace `decoration-neutral-300 ... dark:decoration-neutral-500` with `decoration-muted-foreground`. The `muted-foreground` Shadcn variable is already mapped to design system tokens in app.css. Remove the dark: variant since muted-foreground handles both modes via CSS cascade. The full class string becomes:
`class="text-foreground underline decoration-muted-foreground underline-offset-4 transition-colors duration-300 ease-out hover:decoration-current!"`

**TwoFactorChallenge.vue (lines 91 and 123):** Apply the exact same replacement on both inline `<button>` elements. Replace `decoration-neutral-300 underline-offset-4 transition-colors duration-300 ease-out hover:decoration-current! dark:decoration-neutral-500` with `decoration-muted-foreground underline-offset-4 transition-colors duration-300 ease-out hover:decoration-current!` (remove the dark: variant, add decoration-muted-foreground).

**IncidentTimeline.vue:** Replace 3 instances of hardcoded neutral-* text colors:
- Line 100: `text-neutral-900 dark:text-neutral-100` -> `text-foreground` (event label text)
- Line 111: `text-neutral-600 dark:text-neutral-400` -> `text-muted-foreground` (event data text)
- Line 118: `text-neutral-500 dark:text-neutral-500` -> `text-muted-foreground` (notes text -- same semantic level as event data)

These Shadcn semantic classes are already mapped to design system tokens via the CSS cascade in app.css, so they correctly handle both light and dark modes.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && grep -rn "neutral-" resources/js/components/TextLink.vue resources/js/pages/auth/TwoFactorChallenge.vue resources/js/components/incidents/IncidentTimeline.vue; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero grep matches for "neutral-" in all three files. TextLink and TwoFactorChallenge use decoration-muted-foreground. IncidentTimeline uses text-foreground and text-muted-foreground.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ReportRow hardcoded badge colors and PrioritySelector hardcoded priority colors with design system tokens</name>
  <files>resources/js/components/analytics/ReportRow.vue, resources/js/components/incidents/PrioritySelector.vue</files>
  <action>
**ReportRow.vue:** Replace TYPE_BADGES and STATUS_BADGES class maps with color-mix() pattern using design system tokens. This is the exact same pattern used in Users.vue role badges, Index.vue priority badges, and Barangays.vue risk badges.

Replace TYPE_BADGES (lines 11-28):
```typescript
const TYPE_BADGES: Record<string, { label: string; class: string }> = {
    quarterly: {
        label: 'Quarterly',
        class: 'bg-[color-mix(in_srgb,var(--t-accent)_12%,transparent)] text-t-accent',
    },
    annual: {
        label: 'Annual',
        class: 'bg-[color-mix(in_srgb,var(--t-role-supervisor)_12%,transparent)] text-t-role-supervisor',
    },
    dilg_monthly: {
        label: 'DILG',
        class: 'bg-[color-mix(in_srgb,var(--t-online)_12%,transparent)] text-t-online',
    },
    ndrrmc_sitrep: {
        label: 'NDRRMC',
        class: 'bg-[color-mix(in_srgb,var(--t-p2)_12%,transparent)] text-t-p2',
    },
};
```

Replace STATUS_BADGES (lines 30-43):
```typescript
const STATUS_BADGES: Record<string, { label: string; class: string }> = {
    generating: {
        label: 'Generating',
        class: 'bg-[color-mix(in_srgb,var(--t-p3)_12%,transparent)] text-t-p3',
    },
    ready: {
        label: 'Ready',
        class: 'bg-[color-mix(in_srgb,var(--t-online)_12%,transparent)] text-t-online',
    },
    failed: {
        label: 'Failed',
        class: 'bg-[color-mix(in_srgb,var(--t-p1)_12%,transparent)] text-t-p1',
    },
};
```

Token mapping rationale:
- quarterly -> t-accent (blue, matching accent color)
- annual -> t-role-supervisor (purple, distinguishes from quarterly)
- dilg_monthly -> t-online (green, operational report)
- ndrrmc_sitrep -> t-p2 (orange, emergency context)
- generating -> t-p3 (amber/yellow, in-progress)
- ready -> t-online (green, success)
- failed -> t-p1 (red, error)

**PrioritySelector.vue:** Replace hardcoded red/orange/amber/green Tailwind classes with design system tokens. Replace the `priorities` array (lines 18-47):

```typescript
const priorities: {
    value: IncidentPriority;
    label: string;
    activeClass: string;
    inactiveClass: string;
}[] = [
    {
        value: 'P1',
        label: 'P1',
        activeClass: 'bg-t-p1 text-white border-t-p1',
        inactiveClass:
            'border-[color-mix(in_srgb,var(--t-p1)_40%,transparent)] text-t-p1 hover:bg-[color-mix(in_srgb,var(--t-p1)_8%,transparent)]',
    },
    {
        value: 'P2',
        label: 'P2',
        activeClass: 'bg-t-p2 text-white border-t-p2',
        inactiveClass:
            'border-[color-mix(in_srgb,var(--t-p2)_40%,transparent)] text-t-p2 hover:bg-[color-mix(in_srgb,var(--t-p2)_8%,transparent)]',
    },
    {
        value: 'P3',
        label: 'P3',
        activeClass: 'bg-t-p3 text-white border-t-p3',
        inactiveClass:
            'border-[color-mix(in_srgb,var(--t-p3)_40%,transparent)] text-t-p3 hover:bg-[color-mix(in_srgb,var(--t-p3)_8%,transparent)]',
    },
    {
        value: 'P4',
        label: 'P4',
        activeClass: 'bg-t-p4 text-white border-t-p4',
        inactiveClass:
            'border-[color-mix(in_srgb,var(--t-p4)_40%,transparent)] text-t-p4 hover:bg-[color-mix(in_srgb,var(--t-p4)_8%,transparent)]',
    },
];
```

This eliminates all dark: variant overrides (design system tokens handle dark mode via CSS cascade) and uses the same color-mix() pattern established throughout the codebase.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && grep -rn "bg-blue-\|bg-purple-\|bg-green-\|bg-amber-\|bg-yellow-\|bg-red-\|bg-orange-\|text-blue-\|text-purple-\|text-green-\|text-amber-\|text-yellow-\|text-red-\|text-orange-\|border-red-\|border-orange-\|border-amber-\|border-green-" resources/js/components/analytics/ReportRow.vue resources/js/components/incidents/PrioritySelector.vue; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero grep matches for hardcoded color-N00 classes in both files. ReportRow uses color-mix() with t-accent, t-role-supervisor, t-online, t-p2, t-p3, t-p1 tokens. PrioritySelector uses bg-t-p1 through bg-t-p4 active classes and color-mix() inactive classes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "neutral-\|bg-blue-\|bg-purple-\|bg-green-\|bg-amber-\|bg-yellow-\|bg-red-\|bg-orange-\|text-blue-\|text-purple-\|text-green-\|text-amber-\|text-yellow-\|text-red-\|text-orange-\|border-red-\|border-orange-\|border-amber-\|border-green-" resources/js/components/TextLink.vue resources/js/pages/auth/TwoFactorChallenge.vue resources/js/components/analytics/ReportRow.vue resources/js/components/incidents/PrioritySelector.vue resources/js/components/incidents/IncidentTimeline.vue` returns zero matches
2. `npm run build` completes without errors
3. `npm run types:check` passes (no TypeScript errors from class string changes)
</verification>

<success_criteria>
- All 5 files use design system tokens exclusively (zero hardcoded neutral-*/color-N00 classes)
- Frontend builds successfully
- Phase 10 verification would score 12/12 truths (all gaps closed)
</success_criteria>

<output>
After completion, create `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-05-SUMMARY.md`
</output>
