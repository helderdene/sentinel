---
phase: 16
plan: 02
plan_number: 02
title: REQUIREMENTS.md traceability backfill (OP-01..15 + REBRAND-01..06)
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
requirements_addressed: []

must_haves:
  truths:
    - "REQUIREMENTS.md has an `### Operator Role` section between `### Intake` and `### Dispatch` listing OP-01 through OP-15 with full statements"
    - "REQUIREMENTS.md has a `### Sentinel Rebrand` section between `### PWA & Push Notifications` and `## v2 Requirements` listing REBRAND-01 through REBRAND-06 with full statements"
    - "REQUIREMENTS.md traceability table (after the MOBILE-02 row) contains 21 new rows: 15 OP-* → Phase 8 + 6 REBRAND-* → Phase 14 (all Complete)"
    - "REQUIREMENTS.md coverage footer shows `v1 requirements: 123 total` (was 102)"
    - "REQUIREMENTS.md `Last updated:` footer line reflects the Phase 16 backfill"
    - "Phase ordering is preserved: requirement sections appear in phase-chronological order"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Full OP-01..15 + REBRAND-01..06 definitions and traceability rows"
      contains: "### Operator Role"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Sentinel Rebrand requirement statements"
      contains: "### Sentinel Rebrand"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Complete traceability coverage (123 total)"
      contains: "v1 requirements: 123 total"
  key_links:
    - from: ".planning/REQUIREMENTS.md §Operator Role"
      to: ".planning/REQUIREMENTS.md §Traceability (OP-* rows)"
      via: "In-document cross-reference by ID"
      pattern: "OP-(01|02|03|04|05|06|07|08|09|10|11|12|13|14|15)"
    - from: ".planning/REQUIREMENTS.md §Sentinel Rebrand"
      to: ".planning/REQUIREMENTS.md §Traceability (REBRAND-* rows)"
      via: "In-document cross-reference by ID"
      pattern: "REBRAND-(01|02|03|04|05|06)"
    - from: ".planning/REQUIREMENTS.md §Coverage footer"
      to: "All v1 requirement IDs across all sections"
      via: "Summed count"
      pattern: "123 total"
---

<objective>
Backfill `.planning/REQUIREMENTS.md` so that OP-01 through OP-15 (Phase 8 operator role requirements) and REBRAND-01 through REBRAND-06 (Phase 14 Sentinel rebrand requirements) are registered as first-class v1 requirements with full statements AND corresponding traceability rows.

Purpose: Closes v1.0 Milestone Audit Tech Debt items "OP-01..15 missing from REQUIREMENTS.md traceability table" (Phase 8, audit line 208) and "REBRAND-01..06 missing from REQUIREMENTS.md traceability table" (Phase 14, audit line 223). Addresses frontmatter decisions D-01, D-02, D-03, D-04, D-05.

Output: Single-file edit to `.planning/REQUIREMENTS.md` adding 21 new requirement definitions, 21 new traceability rows, and an updated coverage footer (102 → 123).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md
@.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-VERIFICATION.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VERIFICATION.md
</context>

<threat_model>
## Trust Boundaries

Not applicable. This plan edits a planning document (`.planning/REQUIREMENTS.md`) — no code, no routes, no user input path, no authentication boundary. Document lives in-repo and is read by humans + planning tooling only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | N/A | `.planning/REQUIREMENTS.md` (documentation) | accept | Pure documentation edit. No runtime surface. Git history preserves the prior state for audit. No authentication, authorization, input validation, or data integrity properties involved. |

**Net assessment:** Docs-only edit. Zero security threat surface. ASVS not applicable.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert `### Operator Role` section with OP-01..15 full statements</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — target of the edit)
    - .planning/phases/08-implement-operator-role-and-intake-layer-ui/08-VERIFICATION.md (source of OP-01..15 descriptions — `## Requirements Coverage` table starting at line 131)
    - .planning/phases/08-implement-operator-role-and-intake-layer-ui/08-RESEARCH.md lines 74-88 (cross-reference for work descriptions per ID)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md §4 (canonical section format: `### {Name}` + `- [x] **{ID}**: {statement}` with code backticks for technical terms)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md D-01, D-02, D-04 (decisions locking the format and insertion point)
    - ROADMAP.md §Phase 8 Success Criteria (lines 155-163 — reference only, NOT the source of text per D-02)
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Locate the `### Intake` section (starts at line 22) and the `### Dispatch` section (starts at line 34). The new `### Operator Role` section must be inserted BETWEEN them — immediately after the last INTK-09 line and before the `### Dispatch` header (preserving a blank line above and below, matching the existing section spacing).

    Insert EXACTLY the following block (15 requirement lines, using `[x]` because Phase 8 is complete, OP-10 marked Complete per 16-PATTERNS.md §4 "OP-10 status exception" — the OP-10 gap was resolved in commit 66b8a52 per 08-VERIFICATION.md frontmatter):

    ```markdown
    ### Operator Role

    - [x] **OP-01**: Operator case exists in `UserRole` enum with value `'operator'` as the 5th role (alongside dispatcher, responder, supervisor, admin)
    - [x] **OP-02**: Triaged case exists in `IncidentStatus` enum with value `'TRIAGED'` at position 2, between `PENDING` and `DISPATCHED`
    - [x] **OP-03**: 6 intake gates defined (`access-intake-station`, `create-incident-intake`, `triage-incident`, `override-priority`, `recall-incident`, `view-session-log`) with correct role matrix (operator/supervisor/admin access where appropriate)
    - [x] **OP-04**: Operator login redirects to `/intake` (not `/dashboard`) via custom Fortify `LoginResponse` singleton bound in `FortifyServiceProvider`
    - [x] **OP-05**: Design system tokens adopted as Tailwind utilities — DM Sans + Space Mono fonts, 27 `--color-t-*` tokens with `:root` light values and `.dark` overrides, used across intake components
    - [x] **OP-06**: Intake station route (`intake.station`) accessible to operator/supervisor/admin; forbidden to dispatcher/responder via `role:operator,supervisor,admin` middleware
    - [x] **OP-07**: Three-column intake station with live WebSocket feed (left channel feed at 296px, center triage form, right 304px dispatch queue) and filter tabs (All/Pending/Triaged) via `useIntakeFeed` composable subscribing to `dispatch.incidents`
    - [x] **OP-08**: Triage action transitions incidents from `PENDING` to `TRIAGED` via `IntakeStationController::triage`; manual entry (`intake.store-and-triage`) creates TRIAGED incidents directly with dual timeline entries (created + triaged)
    - [x] **OP-09**: Dispatch queue right panel renders priority-ordered TRIAGED incidents with per-row priority border and session metrics (SessionMetrics, PriorityBreakdown)
    - [x] **OP-10**: Topbar stat pills (Incoming, Pending, Triaged, Avg Resp) update in real-time via WebSocket through `inject('topbarStats')` + `inject('tickerEvents')` chain from `IntakeStation.vue` to `IntakeTopbar.vue`
    - [x] **OP-11**: Override Priority and Recall actions available to supervisor/admin only (via `v-if="canOverride"` / `v-if="canRecall"`); both write timeline entries and broadcast WebSocket events
    - [x] **OP-12**: `IntakeLayout.vue` full-screen shell with 56px topbar (`IntakeTopbar`) and statusbar (`IntakeStatusbar`) wrapping the intake station body
    - [x] **OP-13**: 14 custom SVG icon components under `resources/js/components/intake/icons/` (Sms, App, Voice, Iot, Walkin, Pin, User, Check, Intake, Logout, Shield, Recall, Override, Activity)
    - [x] **OP-14**: Intake badge components (`PriBadge` P1–P4, `ChBadge` channel, `RoleBadge`, `UserChip` with avatar and dropdown) styled with design system tokens and used across intake surfaces
    - [x] **OP-15**: Operator role included in `dispatch.incidents` channel authorization (`routes/channels.php` `$dispatchRoles`) so operators receive real-time incident events

    ```

    Insertion rules:
    - Preserve the exact formatting shown: `### Operator Role` as an H3 header with title case and spaces (not kebab-case)
    - Each requirement starts with `- [x] **OP-NN**: ` followed by the statement
    - Use code backticks around class names, file paths, route names, column names, and channel names (matches the style of existing sections like `### Bi-directional Communication`)
    - Exactly one blank line above `### Operator Role` (separating it from the INTK-09 line)
    - Exactly one blank line below the OP-15 line (separating it from `### Dispatch`)
    - Do not renumber any other IDs
    - Do not modify any line in `### Intake` or `### Dispatch`

    Why this section placement (per D-04): Phase-chronological reading order — Phase 2 Intake → Phase 8 Operator Role → Phase 4 Dispatch. The pragmatic grouping is by section-level relevance, which places Operator between Intake (its upstream) and Dispatch (the next operator-adjacent concern).

    Source authority (per D-02): Descriptions are derived from `08-VERIFICATION.md` §Requirements Coverage (lines 131-147). Enrichment with `08-RESEARCH.md` lines 74-88 adds technical-term precision (e.g., route name `intake.station`, channel `dispatch.incidents`). ROADMAP.md abbreviated success criteria are NOT used as source text.

    OP-10 status resolution (per 16-PATTERNS.md §4): `08-VERIFICATION.md:7-9` records the resolved gap via commit 66b8a52. The `### Operator Role` entry uses `[x]` (Complete). Traceability row in Task 2 also uses `Complete`.
  </action>
  <behavior>
    - `.planning/REQUIREMENTS.md` reads as: `### Intake` section → (blank line) → `### Operator Role` section with 15 `[x]` items → (blank line) → `### Dispatch` section (unchanged)
    - Every OP-NN has its full-sentence statement (no abbreviations, no "TODO")
    - Formatting matches sibling sections — H3 header, `- [x]` checkbox, `**ID**:` bold, technical terms in backticks
  </behavior>
  <verify>
    <automated>grep -qE '^### Operator Role$' .planning/REQUIREMENTS.md && grep -cE '^- \[x\] \*\*OP-(0[1-9]|1[0-5])\*\*:' .planning/REQUIREMENTS.md | grep -q '^15$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the exact line `### Operator Role` (grep-checkable)
    - File contains 15 lines matching the regex `^- \[x\] \*\*OP-(0[1-9]|1[0-5])\*\*:` (one per OP-01..15)
    - Each OP-NN line contains the statement text verbatim as written in this action
    - File still contains `### Intake` (grep-checkable — unchanged)
    - File still contains `### Dispatch` (grep-checkable — unchanged)
    - File still contains `- [x] **INTK-09**:` as the last item before `### Operator Role` (grep-checkable)
    - OP-10 uses `[x]` (not `[ ]`) — status resolved per commit 66b8a52
  </acceptance_criteria>
  <done>`### Operator Role` section inserted with 15 full OP-NN requirement statements, formatting matches sibling sections, Intake and Dispatch sections untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert `### Sentinel Rebrand` section + append 21 traceability rows + bump coverage count</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — reflects Task 1's additions)
    - .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VERIFICATION.md (source of REBRAND-01..06 descriptions — `## Requirements Coverage` table starting at line 98)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md §4 (canonical traceability row format + coverage footer update shape)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md D-01, D-02, D-03, D-05 (decisions locking Rebrand format, source, row ordering, and coverage count)
  </read_first>
  <action>
    Make three edits to `.planning/REQUIREMENTS.md` in the following order.

    **Edit 1 — Insert `### Sentinel Rebrand` section.**

    Locate the end of the `### PWA & Push Notifications` section (the MOBILE-02 `- [x]` line — currently line 141) and the `## v2 Requirements` H2 header (currently line 143). Insert the new section BETWEEN them, preserving the blank line separation.

    Insert EXACTLY:

    ```markdown
    ### Sentinel Rebrand

    - [x] **REBRAND-01**: CSS token migration — Sentinel navy/blue palette in `--t-*` tokens (`--t-bg: #eff3fa` light, `#05101e` dark), both `:root` and `.dark` blocks; Shadcn components inherit via cascade (`--background: var(--t-bg)`)
    - [x] **REBRAND-02**: Font migration — DM Mono replaces Space Mono as `--font-mono`; Bebas Neue added as `--font-display` for auth page title; Google Fonts URL loads all three (DM Sans + DM Mono + Bebas Neue)
    - [x] **REBRAND-03**: SVG shield identity — full animated Sentinel shield on auth page (radar rings, crosshairs, eye, pulseRing + sweep animations); simplified 26x30 shield in sidebar (`AppLogo.vue`) and favicon (`public/favicon.svg`)
    - [x] **REBRAND-04**: User-facing string rename — all "IRMS" / "CDRRMO Butuan City" application-code references replaced with "Sentinel"; `APP_NAME="Sentinel"` in `.env`; documented non-user-facing residuals (FHIR URNs, BFP API identifiers, CDRRMO agency name in UnitForm, `irms.test` Herd hostname, PHPDoc comments) retained as INFO-severity
    - [x] **REBRAND-05**: PWA and push rebrand — `vite.config.ts` manifest `name: 'Sentinel - Incident Response Management System'`, `theme_color: '#042C53'`; 4 PNG icons regenerated on `#042C53` background (192, 512, maskable-512, apple-touch-180); `sw.ts` push notifications default to `'Sentinel'` title and tag
    - [x] **REBRAND-06**: Hardcoded priority/status hex color sweep — MapLibre `useDispatchMap.ts` and `useAnalyticsMap.ts`, Chart.js `Dashboard.vue`, responder and intake components (`PriBadge`, `ChBadge`) all use Sentinel palette values (P1 `#E24B4A`, P2 `#EF9F27`, P3 `#1D9E75`, P4 `#378ADD`); zero instances of old palette (`#dc2626`, `#ea580c`, `#ca8a04`, `#16a34a`, `#2563eb`) in `resources/js/`

    ```

    Formatting rules (identical to Task 1):
    - H3 `### Sentinel Rebrand` header (title case, spaced words)
    - Each line: `- [x] **REBRAND-NN**: {statement}`
    - Technical terms in backticks (file paths, hex values, env vars, token names)
    - One blank line above and below the section

    **Edit 2 — Append 21 traceability rows to the traceability table.**

    Locate the traceability table (starts at line 172 `| Requirement | Phase | Status |`) and its last row, `| MOBILE-02 | Phase 13 | Complete |` (currently line 275). Append 21 new rows IMMEDIATELY AFTER the MOBILE-02 row, grouped by prefix (all OP-* first, then all REBRAND-* — preserves phase ordering per D-05).

    Insert EXACTLY:

    ```markdown
    | OP-01 | Phase 8 | Complete |
    | OP-02 | Phase 8 | Complete |
    | OP-03 | Phase 8 | Complete |
    | OP-04 | Phase 8 | Complete |
    | OP-05 | Phase 8 | Complete |
    | OP-06 | Phase 8 | Complete |
    | OP-07 | Phase 8 | Complete |
    | OP-08 | Phase 8 | Complete |
    | OP-09 | Phase 8 | Complete |
    | OP-10 | Phase 8 | Complete |
    | OP-11 | Phase 8 | Complete |
    | OP-12 | Phase 8 | Complete |
    | OP-13 | Phase 8 | Complete |
    | OP-14 | Phase 8 | Complete |
    | OP-15 | Phase 8 | Complete |
    | REBRAND-01 | Phase 14 | Complete |
    | REBRAND-02 | Phase 14 | Complete |
    | REBRAND-03 | Phase 14 | Complete |
    | REBRAND-04 | Phase 14 | Complete |
    | REBRAND-05 | Phase 14 | Complete |
    | REBRAND-06 | Phase 14 | Complete |
    ```

    Note on OP-10 status: Complete (not PARTIAL). The PARTIAL qualifier in `08-VERIFICATION.md:142` was resolved via commit 66b8a52 (recorded in `08-VERIFICATION.md:7-9` frontmatter `gaps[].status: resolved`). Per 16-PATTERNS.md §4 OP-10 status exception, this gets `Complete`.

    **Edit 3 — Update the coverage footer.**

    Locate the current coverage block (currently lines 277-280):

    ```markdown
    **Coverage:**
    - v1 requirements: 102 total
    - Mapped to phases: 102 (RSPDR-06 and RSPDR-10 reassigned to Phase 15 gap closure)
    - Unmapped: 0
    ```

    Replace with EXACTLY:

    ```markdown
    **Coverage:**
    - v1 requirements: 123 total (102 + 15 OP + 6 REBRAND backfilled in Phase 16)
    - Mapped to phases: 123 (RSPDR-06 and RSPDR-10 reassigned to Phase 15 gap closure)
    - Unmapped: 0
    ```

    Math check: 102 (prior total per 16-PATTERNS.md §4) + 15 OP + 6 REBRAND = 123. Both the "total" and "Mapped to phases" lines move to 123; "Unmapped" stays 0 (no IDs left unregistered).

    **Edit 4 — Update the "Last updated" line.**

    Locate currently line 284:

    ```markdown
    *Last updated: 2026-04-17 after v1.0 milestone audit — RSPDR-06 and RSPDR-10 reopened for Phase 15 gap closure*
    ```

    Replace with EXACTLY:

    ```markdown
    *Last updated: 2026-04-17 after Phase 16 traceability backfill — OP-01..15 and REBRAND-01..06 added to registry and coverage table (Phase 15 gap closure for RSPDR-06 and RSPDR-10 tracked separately in Phase 15 artifacts)*
    ```

    This preserves the Phase 15 context while recording the Phase 16 backfill.

    Do NOT modify:
    - Any other requirement line (FNDTN, INTK, DSPTCH, RSPDR, INTGR, ANLTCS, CITIZEN, DS, UNIT, COMM, MOBILE entries all stay as-is)
    - Any other row in the traceability table
    - The `## v1 Requirements`, `## v2 Requirements`, `## Out of Scope`, `## Traceability` H2 headers
    - The top matter (`# Requirements: IRMS`, `**Defined:** ...`, `**Core Value:** ...`)

    Why the 123 total (per D-03): Audit flagged OP/REBRAND as "orphaned from REQUIREMENTS.md" (v1.0-MILESTONE-AUDIT.md lines 208, 223). Backfilling brings the registry into one-to-one correspondence with the phase-scoped IDs referenced in ROADMAP.md Phase 8 and Phase 14 entries.
  </action>
  <behavior>
    - File reads: `### PWA & Push Notifications` section → (blank line) → `### Sentinel Rebrand` section with 6 `[x]` items → (blank line) → `## v2 Requirements` H2 (unchanged)
    - Traceability table retains all 102 pre-existing rows in original order; the 21 new rows are appended in OP→REBRAND prefix order after `MOBILE-02`
    - Coverage footer shows `v1 requirements: 123 total` and `Mapped to phases: 123`
    - `Last updated:` line records the Phase 16 backfill and retains the Phase 15 context
  </behavior>
  <verify>
    <automated>grep -qE '^### Sentinel Rebrand$' .planning/REQUIREMENTS.md && grep -cE '^\| (OP-[0-9]{2}|REBRAND-0[1-6]) \| Phase (8|14) \| Complete \|$' .planning/REQUIREMENTS.md | grep -q '^21$' && grep -q 'v1 requirements: 123 total' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains the exact line `### Sentinel Rebrand` (grep-checkable)
    - File contains 6 lines matching the regex `^- \[x\] \*\*REBRAND-0[1-6]\*\*:` (one per REBRAND-01..06)
    - File contains all 15 `| OP-NN | Phase 8 | Complete |` rows (OP-01 through OP-15)
    - File contains all 6 `| REBRAND-NN | Phase 14 | Complete |` rows (REBRAND-01 through REBRAND-06)
    - File contains the exact line `- v1 requirements: 123 total (102 + 15 OP + 6 REBRAND backfilled in Phase 16)`
    - File contains the exact line `- Mapped to phases: 123 (RSPDR-06 and RSPDR-10 reassigned to Phase 15 gap closure)`
    - File contains the exact substring `*Last updated: 2026-04-17 after Phase 16 traceability backfill`
    - File still contains `## v2 Requirements` (grep-checkable — unchanged)
    - File still contains `| MOBILE-02 | Phase 13 | Complete |` (grep-checkable — unchanged, must be immediately above the new OP-01 row)
    - File does NOT contain `v1 requirements: 102 total` (grep -q exits 1 — old count removed)
  </acceptance_criteria>
  <done>Sentinel Rebrand section inserted, 21 traceability rows appended in OP→REBRAND order, coverage count updated to 123, "Last updated" line reflects Phase 16 backfill. File is in sync with all 123 v1 requirements.</done>
</task>

</tasks>

<verification>
After both tasks complete, run these checks:

```bash
# 1. Structural sanity — section headers present
grep -cE '^### (Foundation|Intake|Operator Role|Dispatch|Responder|Integration|Analytics|Citizen Reporting|Design System Alignment|Units CRUD|Bi-directional Communication|PWA & Push Notifications|Sentinel Rebrand)$' .planning/REQUIREMENTS.md
# Expected: 13 (was 11 — gained Operator Role + Sentinel Rebrand)

# 2. OP count in statements section
grep -cE '^- \[x\] \*\*OP-(0[1-9]|1[0-5])\*\*:' .planning/REQUIREMENTS.md
# Expected: 15

# 3. REBRAND count in statements section
grep -cE '^- \[x\] \*\*REBRAND-0[1-6]\*\*:' .planning/REQUIREMENTS.md
# Expected: 6

# 4. Traceability table row count for new entries
grep -cE '^\| (OP-[0-9]{2}|REBRAND-0[1-6]) \| Phase (8|14) \| Complete \|$' .planning/REQUIREMENTS.md
# Expected: 21

# 5. Coverage footer
grep -q 'v1 requirements: 123 total' .planning/REQUIREMENTS.md && echo "OK: 123 total"
grep -q 'Mapped to phases: 123' .planning/REQUIREMENTS.md && echo "OK: Mapped 123"
grep -q 'v1 requirements: 102 total' .planning/REQUIREMENTS.md && echo "FAIL: old 102 still present"

# 6. "Last updated" line
grep -q 'after Phase 16 traceability backfill' .planning/REQUIREMENTS.md && echo "OK: Last updated line updated"

# 7. Total count of ID-rows in traceability table (all v1)
grep -cE '^\| [A-Z]+-[0-9]{2}[a-z]? \| Phase' .planning/REQUIREMENTS.md
# Expected: 123 (102 prior + 21 new) — sanity check the math

# 8. No accidentally-deleted rows (spot check top + bottom)
grep -q '| FNDTN-01 | Phase 1 | Complete |' .planning/REQUIREMENTS.md && echo "OK: FNDTN-01 preserved"
grep -q '| MOBILE-02 | Phase 13 | Complete |' .planning/REQUIREMENTS.md && echo "OK: MOBILE-02 preserved"
```

All 8 checks must pass. No new tooling, no Pest runs — this is a documentation edit.
</verification>

<success_criteria>
1. `.planning/REQUIREMENTS.md` contains a complete `### Operator Role` H3 section between `### Intake` and `### Dispatch`, with 15 `[x]` statements (OP-01 through OP-15) derived from `08-VERIFICATION.md`
2. `.planning/REQUIREMENTS.md` contains a complete `### Sentinel Rebrand` H3 section between `### PWA & Push Notifications` and `## v2 Requirements`, with 6 `[x]` statements (REBRAND-01 through REBRAND-06) derived from `14-VERIFICATION.md`
3. The Traceability table at the bottom of the file has 21 new rows appended after `| MOBILE-02 | Phase 13 | Complete |`, grouped OP-01..15 then REBRAND-01..06, each `Phase 8` or `Phase 14` with status `Complete`
4. The Coverage footer reads `v1 requirements: 123 total (102 + 15 OP + 6 REBRAND backfilled in Phase 16)` and `Mapped to phases: 123 ...`
5. The `Last updated:` line credits the Phase 16 backfill while preserving the Phase 15 gap-closure note
6. All pre-existing requirement lines and traceability rows remain unchanged (FNDTN/INTK/DSPTCH/RSPDR/INTGR/ANLTCS/CITIZEN/DS/UNIT/COMM/MOBILE all intact)
7. v1.0-MILESTONE-AUDIT.md Tech Debt items "OP-01..15 missing from REQUIREMENTS.md" and "REBRAND-01..06 missing from REQUIREMENTS.md" are closed
</success_criteria>

<output>
After completion, create `.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-02-SUMMARY.md` documenting:
- The 4 edit sub-operations (Operator Role section insert, Sentinel Rebrand section insert, 21 traceability rows appended, coverage footer + Last-updated line update)
- Final line counts: 15 OP statements + 6 REBRAND statements + 21 traceability rows + 1 coverage footer update + 1 Last-updated line update
- Confirmation that 102 → 123 math reconciles (102 prior + 15 OP + 6 REBRAND = 123)
- Audit item closure references (`v1.0-MILESTONE-AUDIT.md` Phase 8 line 208, Phase 14 line 223)
</output>
