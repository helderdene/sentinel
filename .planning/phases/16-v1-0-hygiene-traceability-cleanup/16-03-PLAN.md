---
phase: 16
plan: 03
plan_number: 03
title: Validation & verification updates (Phase 14 approval + Phase 10 human verification)
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md
  - .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md
autonomous: false
requirements: []
requirements_addressed: []

must_haves:
  truths:
    - "Phase 14 VALIDATION.md frontmatter has `status: approved`"
    - "Phase 14 VALIDATION.md frontmatter includes `audited: 2026-04-17` (matching Phase 13 precedent verbatim — see D-13 rationale below)"
    - "Phase 10 VERIFICATION.md has a new `### Human Verification Checklist (Phase 16 follow-up)` section with 3 checks (focus ring, color-mix, dark mode contrast)"
    - "Each of the 3 Phase 10 checks has explicit Chrome light + Chrome dark screenshot slots pointing to `./10-verification-screenshots/{check-slug}-{light|dark}.png`"
    - "Phase 10 screenshots directory `.planning/phases/10-.../10-verification-screenshots/` exists with 6 PNG files after user completes the verification"
    - "Phase 10 VERIFICATION.md frontmatter `status: human_needed` is flipped to `status: passed` AFTER the user ticks all 3 checks and commits screenshots"
  artifacts:
    - path: ".planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md"
      provides: "Phase 14 approval record with audited date"
      contains: "status: approved"
    - path: ".planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md"
      provides: "Human verification checklist with 3 checks and 6 screenshot references"
      contains: "### Human Verification Checklist (Phase 16 follow-up)"
    - path: ".planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/"
      provides: "6 PNG files (3 checks × 2 modes)"
      contains: "focus-ring-light.png"
  key_links:
    - from: ".planning/phases/14-.../14-VALIDATION.md frontmatter"
      to: ".planning/phases/13-pwa-setup/13-VALIDATION.md frontmatter (precedent)"
      via: "Mirror exact key names — status, nyquist_compliant, wave_0_complete, created, audited"
      pattern: "status: approved\\n(?s:.*)audited: 2026-04-17"
    - from: ".planning/phases/10-.../10-VERIFICATION.md §Human Verification Checklist"
      to: ".planning/phases/10-.../10-verification-screenshots/"
      via: "Relative path references in markdown"
      pattern: "\\./10-verification-screenshots/"
---

<objective>
Flip Phase 14's `14-VALIDATION.md` from `draft` to `approved` (docs-only single-file edit, matching the Phase 13 precedent pattern), AND prepare `10-VERIFICATION.md` for the human browser-verification of three visual-fidelity checks, then (after the user completes the checks and commits screenshots) flip `10-VERIFICATION.md` from `human_needed` to `passed`.

Purpose: Closes v1.0 Milestone Audit Tech Debt items for Phase 14 (audit line 224: `14-VALIDATION.md` `status: draft` — `nyquist_compliant: true` but not formally approved) and Phase 10 (audit line 211: `human_needed` status — visual fidelity has not been confirmed in a browser). Addresses frontmatter decisions D-13, D-14, D-15, D-16, D-17, D-18.

Output: Two edited planning documents, one new screenshots directory with 6 PNG files, and two frontmatter status transitions recorded.

**D-13 conflict resolution (surfaced by pattern-mapper):** CONTEXT.md's D-13 says append "approved: 2026-04-17" — but the Phase 13 precedent it explicitly references uses `audited: 2026-04-17`, NOT `approved:`. Per 16-PATTERNS.md §5 recommendation and CONTEXT.md "Claude's Discretion" bullet 2 ("match Phase 13 if Phase 13 has precedent"), this plan uses `audited: 2026-04-17` — matching Phase 13's literal key for precedent fidelity. Rationale: D-13's core intent is "match the Phase 13 precedent pattern"; the literal key in that pattern is `audited`. Using `approved:` would create a new key not present in Phase 13's frontmatter and would drift from the precedent D-13 asked us to follow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-pwa-setup/13-VALIDATION.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-VERIFICATION.md
@.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md
@.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md
</context>

<threat_model>
## Trust Boundaries

Not applicable. This plan edits two planning documents and writes 6 screenshot PNG files into a sub-directory of `.planning/phases/10-.../`. No code, no routes, no runtime surface. All edits are reviewed and committed by the developer.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | N/A | `14-VALIDATION.md` frontmatter flip | accept | Docs-only. Git history preserves prior `status: draft` state. |
| T-16-05 | N/A | `10-VERIFICATION.md` frontmatter flip + checklist append | accept | Docs-only. Git history preserves prior `status: human_needed` state. |
| T-16-06 | Information Disclosure | Screenshot PNG files under `.planning/phases/10-.../10-verification-screenshots/` | accept | Screenshots show UI of the authenticated app in a local dev environment. They may contain demo-seeded incident data (e.g., "AMB-01", placeholder phone numbers). No production PII; no secrets embedded; no VAPID keys visible. Repo `.gitignore` already excludes `.env`; screenshots committed to `.planning/` are acceptable per existing project convention. |

**Net assessment:** Docs-only edits. Zero runtime security surface. ASVS not applicable. Developer should spot-check screenshots for accidental inclusion of sensitive data (real user emails, real phone numbers) before committing — but local dev seed data is the norm and presents negligible risk.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Flip Phase 14 VALIDATION.md frontmatter to approved (match Phase 13 precedent verbatim)</name>
  <files>.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md</files>
  <read_first>
    - .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md (current state — the file being edited)
    - .planning/phases/13-pwa-setup/13-VALIDATION.md (Phase 13 precedent frontmatter — lines 1-9 provide the exact shape to mirror)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md §5 (full before/after YAML block + D-13 conflict resolution rationale — copy-paste-ready)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md D-13, D-14 (decisions; note D-13 literal wording vs Phase 13 precedent key — resolved in favour of Phase 13 literal key per plan objective)
    - v1.0-MILESTONE-AUDIT.md line 224 (audit finding: "14-VALIDATION.md `status: draft` — `nyquist_compliant: true` but not formally approved")
  </read_first>
  <action>
    Edit `.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md`. This is a frontmatter-only edit — do not touch the body content.

    **Before (current frontmatter, lines 1-8 of the file):**

    ```yaml
    ---
    phase: 14
    slug: update-design-system-to-sentinel-branding-and-rename-app
    status: draft
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-03-15
    ---
    ```

    **After (target frontmatter — two-line diff):**

    ```yaml
    ---
    phase: 14
    slug: update-design-system-to-sentinel-branding-and-rename-app
    status: approved
    nyquist_compliant: true
    wave_0_complete: true
    created: 2026-03-15
    audited: 2026-04-17
    ---
    ```

    Exact diff:
    - Line 4: `status: draft` → `status: approved` (single-word replace)
    - Line 7 (new line inserted BEFORE the closing `---`): `audited: 2026-04-17`

    Key choice rationale (`audited:` vs `approved:`): D-13 says "match the Phase 13 precedent pattern" and separately says "append a line `approved: 2026-04-17`". Phase 13's actual frontmatter key (`.planning/phases/13-pwa-setup/13-VALIDATION.md:8`) is literally `audited: 2026-04-17`, not `approved:`. Per 16-PATTERNS.md §5 and CONTEXT.md "Claude's Discretion" bullet 2, this plan elects to match Phase 13's literal key (`audited`) for precedent fidelity — D-13's core intent ("match Phase 13") trumps its literal example ("approved"). Using `audited:` keeps both validation files frontmatter-shape-identical, which makes future bulk queries (e.g., "which phases are audited?") work consistently.

    **Body content — preserve verbatim:**
    - All 14 lines after the closing `---` (Test Infrastructure table, Sampling Rate, Per-Task Verification Map, Wave 0 Requirements, Manual-Only Verifications, Validation Sign-Off) stay exactly as-is
    - Do NOT change `**Approval:** pending` at the bottom of the file (line 82) — that is historical record; approval is now recorded in the frontmatter per D-14's "no need to rerun `/gsd-validate-phase 14`" and per the Phase 13 precedent pattern where the frontmatter is authoritative

    Per D-14: Do NOT re-run `/gsd-validate-phase 14`. That command would regenerate the whole file, discarding the task verification map and sign-off records. The frontmatter flip is sufficient because `nyquist_compliant: true` and `wave_0_complete: true` are already set.

    Per v1.0-MILESTONE-AUDIT.md line 224: Audit flagged that `nyquist_compliant: true` was set but `status: draft` — this edit reconciles the two.
  </action>
  <behavior>
    - Opening `14-VALIDATION.md`, the first 9 lines are the YAML frontmatter with `status: approved` and an `audited: 2026-04-17` line directly above the closing `---`
    - Body content (Test Infrastructure table, sampling rate, per-task verification map, etc.) is identical to pre-edit
    - `grep 'status:' .planning/phases/14-*/14-VALIDATION.md` returns `status: approved`
    - Phase 14 is now formally approved in a machine-readable way (frontmatter key) that matches Phase 13's precedent exactly
  </behavior>
  <verify>
    <automated>head -10 .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md | grep -q '^status: approved$' && head -10 .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md | grep -q '^audited: 2026-04-17$'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md` line 4 is exactly `status: approved`
    - File contains a line `audited: 2026-04-17` (grep-checkable; Phase 13 precedent key, not `approved:`)
    - File does NOT contain `status: draft` (grep -q exits 1 — old status removed)
    - File still contains `nyquist_compliant: true` (grep-checkable — unchanged)
    - File still contains `wave_0_complete: true` (grep-checkable — unchanged)
    - File still contains `phase: 14` and `slug: update-design-system-to-sentinel-branding-and-rename-app` (grep-checkable — unchanged)
    - File still contains `## Test Infrastructure` H2 header (grep-checkable — body preserved)
    - File still contains `## Validation Sign-Off` H2 header (grep-checkable — body preserved)
    - Frontmatter shape matches Phase 13's pattern exactly (same key names in same order except for the different phase/slug and the addition of audited)
  </acceptance_criteria>
  <done>14-VALIDATION.md frontmatter flipped to approved + audited: 2026-04-17; body content preserved; Phase 13 precedent pattern matched verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Prepare Phase 10 VERIFICATION.md checklist + create screenshots directory</name>
  <files>.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md, .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/</files>
  <read_first>
    - .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md (current state — file being edited; frontmatter lines 1-26, body starts line 28, `### Human Verification Required` subsection at line 125)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-PATTERNS.md §6 (full checklist scaffold, directory layout, filename scheme — copy-paste-ready)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md D-15, D-16, D-17, D-18 (decisions on screenshot scheme, 3 checks, human-user verification, frontmatter flip timing)
    - resources/css/app.css line 304 (DS-03 focus ring source rule — will be referenced in the checklist)
    - resources/js/components/incidents/PrioritySelector.vue (color-mix source component)
    - resources/js/components/analytics/ReportRow.vue (dark-mode contrast target)
    - v1.0-MILESTONE-AUDIT.md line 211 (audit finding: "`human_needed` status: visual fidelity (focus ring, color-mix opacity, dark mode contrast) has not been confirmed in a browser")
  </read_first>
  <action>
    Make two edits to `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md`, then create the screenshots directory.

    **Edit 1 — Append a "Human Verification Checklist (Phase 16 follow-up)" section BEFORE the trailing metadata lines.**

    Locate the end of the `### Gaps Summary` body (currently ends around line 158 with `Frontend builds successfully. All automated checks pass.`) and the trailing `---` horizontal rule (currently line 160) followed by metadata lines 162-164 (`_Verified: 2026-03-14T00:00:00Z_`, `_Verifier: Claude (gsd-verifier)_`, `_Re-verification after plan 10-05 gap closure_`).

    Insert the new section AFTER the `---` on line 160 and BEFORE the `_Verified:` line on line 162. So the order becomes:
    1. Existing body → `### Gaps Summary` → (last body paragraph)
    2. Existing `---` horizontal rule
    3. NEW `### Human Verification Checklist (Phase 16 follow-up)` section (inserted here)
    4. Existing `_Verified: 2026-03-14T00:00:00Z_` metadata
    5. Existing `_Verifier: ..._` and `_Re-verification ..._` lines

    Insert EXACTLY:

    ```markdown
    ### Human Verification Checklist (Phase 16 follow-up)

    **Verified by:** [user — fill in name]
    **Verified date:** [YYYY-MM-DD — fill in when all 3 checks pass]
    **Context:** These 3 human-only visual checks closed the Phase 10 `status: human_needed` gap identified in the v1.0 Milestone Audit (line 211). Source: `.planning/v1.0-MILESTONE-AUDIT.md` and `.planning/phases/16-.../16-CONTEXT.md` D-16.

    Screenshots live under `./10-verification-screenshots/` (relative to this file). Filename scheme: `{check-slug}-{light|dark}.png`.

    ---

    #### Check 1 — Focus ring rendering (DS-03)

    - [ ] **Tested**
    - **Test:** Tab through all input fields on the Login page (`/login`) and the Profile settings page (`/settings/profile`) using the Tab key.
    - **Expected:** Each focused input shows `border-color: #2563eb` + `box-shadow: 0 0 0 3px rgba(37,99,235,0.1)` — NOT the default Tailwind `outline-ring/50` ring.
    - **Source rule:** `resources/css/app.css:304` — `[data-slot]:focus-visible { outline: none; border-color: #2563eb; box-shadow: 0 0 0 3px rgba(37,99,235,0.1); }`
    - **Screenshots (required):**
      - Chrome light: `./10-verification-screenshots/focus-ring-light.png`
      - Chrome dark: `./10-verification-screenshots/focus-ring-dark.png`
    - **Pass criteria:** Both screenshots show the blue border + soft box-shadow combo on a focused input.

    ![Focus ring (light)](./10-verification-screenshots/focus-ring-light.png)
    ![Focus ring (dark)](./10-verification-screenshots/focus-ring-dark.png)

    ---

    #### Check 2 — color-mix() opacity tinting (PrioritySelector inactive buttons)

    - [ ] **Tested**
    - **Test:** Navigate to `/incidents/create`. Observe the P1/P2/P3/P4 priority selector. The ACTIVE priority button uses `bg-t-p1..t-p4` (solid color). The INACTIVE buttons must show:
      - Border at 40% opacity tint of their token (via `color-mix(in srgb, var(--t-pN) 40%, transparent)`)
      - Hover state at 8% opacity background tint (via `color-mix(in srgb, var(--t-pN) 8%, transparent)`)
    - **Expected:** Inactive buttons render as visibly tinted variants of the `--t-p1`, `--t-p2`, `--t-p3`, `--t-p4` tokens — NOT hardcoded `red-500`/`orange-500`/`amber-500`/`green-500`.
    - **Source component:** `resources/js/components/incidents/PrioritySelector.vue`
    - **Screenshots (required):**
      - Chrome light: `./10-verification-screenshots/priority-selector-light.png`
      - Chrome dark: `./10-verification-screenshots/priority-selector-dark.png`
    - **Pass criteria:** Screenshots show 4 inactive buttons with tinted borders that match their priority color family at reduced opacity (not pure red/orange/amber/green).

    ![Priority selector (light)](./10-verification-screenshots/priority-selector-light.png)
    ![Priority selector (dark)](./10-verification-screenshots/priority-selector-dark.png)

    ---

    #### Check 3 — Dark-mode contrast (ReportRow badges)

    - [ ] **Tested**
    - **Test:** Toggle appearance to dark mode. Navigate to `/analytics/reports`. Observe the TYPE_BADGES and STATUS_BADGES on each report row.
    - **Expected:** All badges remain legible — the `color-mix()` applied to `--t-accent` (quarterly), `--t-role-supervisor` (annual), `--t-online` (dilg), `--t-p2` (ndrrmc), `--t-p3` (generating), `--t-online` (ready), `--t-p1` (failed) must produce text that contrasts sufficiently with the tinted background AND the surrounding dark surface.
    - **Source component:** `resources/js/components/analytics/ReportRow.vue`
    - **Screenshots (required):**
      - Chrome light: `./10-verification-screenshots/report-row-light.png`
      - Chrome dark: `./10-verification-screenshots/report-row-dark.png`
    - **Pass criteria:** Screenshots show all badge types readable in BOTH modes (no washed-out / low-contrast failures in dark mode).

    ![Report row badges (light)](./10-verification-screenshots/report-row-light.png)
    ![Report row badges (dark)](./10-verification-screenshots/report-row-dark.png)

    ---

    **Completion flow:**
    1. User captures the 6 screenshots (3 checks × light/dark)
    2. User places them at the relative paths above
    3. User ticks the 3 `[ ] Tested` checkboxes → `[x] Tested`
    4. User fills in the `Verified by:` and `Verified date:` fields
    5. **Task 3 of this plan** flips the frontmatter `status: human_needed` → `status: passed`
    6. User commits: `docs(phase-10): human verification complete` (or equivalent)

    ```

    **Edit 2 — Create the screenshots directory.**

    Run: `mkdir -p .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/`

    Add a `.gitkeep` file so the directory is committable even before screenshots land:

    Write `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/.gitkeep` with content:

    ```
    # Placeholder to allow the directory to be committed.
    # Delete this file once the 6 verification screenshots are added:
    #   - focus-ring-light.png
    #   - focus-ring-dark.png
    #   - priority-selector-light.png
    #   - priority-selector-dark.png
    #   - report-row-light.png
    #   - report-row-dark.png
    ```

    Per D-15: Screenshot filename scheme is `{check-slug}-{light|dark}.png` (6 total: 3 checks × 2 modes) placed under the new `10-verification-screenshots/` subdirectory, referenced via relative paths in the checklist. Per D-17, inline embedded images via `![alt](path)` markdown — renderers display them and the checklist reader sees the check + evidence side-by-side.

    Do NOT:
    - Modify the existing frontmatter yet (that's Task 3, after user confirms the 3 checks)
    - Modify any existing truth/artifact/key-link tables in the body
    - Modify the existing `### Human Verification Required` subsection at line 125 (historical record of what was originally flagged — keep for traceability)
  </action>
  <behavior>
    - `10-VERIFICATION.md` now has a new `### Human Verification Checklist (Phase 16 follow-up)` section near the end (after `### Gaps Summary` body + `---` rule, before `_Verified: ..._` metadata)
    - The new section contains 3 numbered checks, each with: `[ ] Tested` checkbox, test instructions, expected behavior, source file reference, two explicit screenshot paths + markdown image embeds, and pass criteria
    - Screenshots directory exists at `.planning/phases/10-.../10-verification-screenshots/` with a `.gitkeep` placeholder file
    - `10-VERIFICATION.md` frontmatter is STILL `status: human_needed` at this point (Task 3 flips it after user completes the checks)
  </behavior>
  <verify>
    <automated>grep -q '### Human Verification Checklist (Phase 16 follow-up)' .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md && test -d .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/10-.../10-VERIFICATION.md` contains the exact line `### Human Verification Checklist (Phase 16 follow-up)` (grep-checkable)
    - File contains 3 `[ ] **Tested**` checkboxes (grep returns 3 matches)
    - File references all 6 screenshot paths: `focus-ring-light.png`, `focus-ring-dark.png`, `priority-selector-light.png`, `priority-selector-dark.png`, `report-row-light.png`, `report-row-dark.png` (6 `grep` matches for `./10-verification-screenshots/`)
    - File references `resources/css/app.css:304` (the DS-03 source rule — grep-checkable)
    - File references `resources/js/components/incidents/PrioritySelector.vue` (grep-checkable)
    - File references `resources/js/components/analytics/ReportRow.vue` (grep-checkable)
    - Directory `.planning/phases/10-.../10-verification-screenshots/` exists (`test -d` exits 0)
    - File `.planning/phases/10-.../10-verification-screenshots/.gitkeep` exists
    - Frontmatter `status:` is STILL `human_needed` (NOT flipped yet — Task 3 handles that)
    - Existing body unchanged: `### Observable Truths` table retains 12 VERIFIED rows; `### Required Artifacts` table unchanged; `### Human Verification Required` subsection at line 125 unchanged
  </acceptance_criteria>
  <done>Phase 10 VERIFICATION.md has a new Phase 16 follow-up checklist with 3 checks and 6 screenshot slots; screenshots directory created with .gitkeep; frontmatter still says human_needed (flipped in Task 3).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser verification + frontmatter flip (human-verify checkpoint)</name>
  <files>.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md, .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/*.png</files>
  <read_first>
    - .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md §Human Verification Checklist (Phase 16 follow-up) (the 3 checks + completion-flow guide written in Task 2)
    - .planning/phases/16-v1-0-hygiene-traceability-cleanup/16-CONTEXT.md D-16, D-17, D-18 (decisions locking human-user verification and the `status: passed` flip timing)
  </read_first>
  <what-built>
    Task 2 prepared the Phase 10 VERIFICATION.md document with a 3-check human verification checklist and an empty screenshots directory. This checkpoint hands off to the user to execute the checks in a real browser (Chrome light + Chrome dark for each check), capture screenshots, and confirm the results.
  </what-built>
  <how-to-verify>
    The user performs the following workflow (offline):

    **Step 1 — Start the dev server.**
    Run `composer run dev` in the project root. The site is served at `https://irms.test` via Laravel Herd.

    **Step 2 — Chrome light mode setup.**
    In Chrome, ensure system appearance is set to light (macOS: System Settings > Appearance > Light). Open `https://irms.test`. If not already authenticated, log in.

    **Step 3 — Check 1 (focus ring) — light mode.**
    Navigate to `/login` (logout first if needed, or use an Incognito window). Click into the email field then Tab through the inputs. Observe the focus ring: it must be a BLUE BORDER + SOFT BOX-SHADOW (not the default Tailwind outline-ring/50 gray).
    - If it passes: screenshot the focused input with visible ring.
    - Save as: `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/focus-ring-light.png`
    - Repeat for the `/settings/profile` page (one of its inputs, focused).

    **Step 4 — Check 2 (PrioritySelector) — light mode.**
    Log in, navigate to `/incidents/create`. Observe the PrioritySelector. The active priority should be solid color; inactive should show TINTED borders (40% opacity of the token) — not hardcoded red/orange/amber/green.
    - Screenshot the priority selector widget.
    - Save as: `priority-selector-light.png`

    **Step 5 — Check 3 (ReportRow badges) — light mode.**
    Navigate to `/analytics/reports`. Observe the TYPE + STATUS badges on each report row.
    - Screenshot a row or the list showing multiple badge colors.
    - Save as: `report-row-light.png`

    **Step 6 — Chrome DARK mode setup.**
    In Chrome, flip system appearance to dark (macOS: System Settings > Appearance > Dark). The app's theme should auto-swap (IRMS uses `.dark` cascade). If the app has its own toggle, use it.

    **Step 7 — Re-run Checks 1, 2, 3 in dark mode.**
    Capture 3 more screenshots:
    - `focus-ring-dark.png`
    - `priority-selector-dark.png`
    - `report-row-dark.png`

    **Step 8 — Confirm all 6 files exist at the expected paths.**
    Run: `ls -la .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/`
    Expected output: 6 `.png` files + `.gitkeep` (or .gitkeep deleted per its own comment once screenshots are added).

    **Step 9 — Edit `10-VERIFICATION.md` to record the pass.**

    Make these three edits to `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md`:

    **9a.** Frontmatter: flip `status: human_needed` → `status: passed` (line 4).

    Before:
    ```yaml
    ---
    phase: 10-update-all-pages-design-to-match-irms-intake-design-system
    verified: 2026-03-14T00:00:00Z
    status: human_needed
    score: 12/12 must-haves verified
    ```

    After:
    ```yaml
    ---
    phase: 10-update-all-pages-design-to-match-irms-intake-design-system
    verified: 2026-03-14T00:00:00Z
    status: passed
    score: 12/12 must-haves verified
    ```

    **9b.** In the new `### Human Verification Checklist (Phase 16 follow-up)` section, update:
    - `**Verified by:** [user — fill in name]` → `**Verified by:** Helder Dene` (or the user's actual name)
    - `**Verified date:** [YYYY-MM-DD — fill in when all 3 checks pass]` → `**Verified date:** 2026-04-17` (or the actual verification date)
    - Change all three `- [ ] **Tested**` checkboxes to `- [x] **Tested**`

    **9c.** Optionally delete `.planning/phases/10-.../10-verification-screenshots/.gitkeep` now that real PNGs are present.

    **Step 10 — Commit the verification.**

    ```bash
    git add .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md
    git add .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/
    git commit -m "docs(phase-10): human verification complete (status: passed)"
    ```

    Expected commit contents: `10-VERIFICATION.md` diff (status + checkboxes + verified-by/date) and 6 new PNG files.

    **If a check fails:** Do NOT flip the frontmatter. Instead:
    - Keep `status: human_needed`
    - Note the failure in the Phase 16 follow-up section (e.g., add a `**Failure note:**` block under the failing check)
    - Reopen the issue as a new gap-closure phase (e.g., Phase 16.1) to fix the visual issue, then re-run the check

    Per D-18: The frontmatter flip to `status: passed` is the final step, performed ONLY after all 3 checks pass.
  </how-to-verify>
  <resume-signal>Type `approved` after committing the screenshots and frontmatter flip, OR describe the failing check(s) so we can open a gap-closure.</resume-signal>
  <acceptance_criteria>
    - Directory `.planning/phases/10-.../10-verification-screenshots/` contains exactly 6 `.png` files: `focus-ring-light.png`, `focus-ring-dark.png`, `priority-selector-light.png`, `priority-selector-dark.png`, `report-row-light.png`, `report-row-dark.png`
    - `.planning/phases/10-.../10-VERIFICATION.md` frontmatter line 4 is `status: passed` (no longer `human_needed`)
    - File's Phase 16 follow-up section has `**Verified by:**` populated with a name (not `[user — fill in name]`)
    - File's Phase 16 follow-up section has `**Verified date:**` populated with a date (not `[YYYY-MM-DD — fill in when all 3 checks pass]`)
    - All three `- [ ] **Tested**` checkboxes in the Phase 16 follow-up section are flipped to `- [x] **Tested**`
    - Frontmatter body content (score, re_verification, human_verification arrays) remains unchanged — only `status:` flipped
    - Screenshots directory is committable (no giant files — each PNG should be under a few hundred KB; if larger, consider optimizing with `pngcrush` or similar, but this is nice-to-have)
  </acceptance_criteria>
  <done>User has browser-verified all 3 Phase 10 visual checks in light + dark mode, committed 6 screenshots to `.planning/phases/10-.../10-verification-screenshots/`, flipped `10-VERIFICATION.md` frontmatter to `status: passed`, and ticked all 3 checklist checkboxes. Phase 10 is no longer `human_needed`.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete, run:

```bash
# Phase 14 approval
grep -q '^status: approved$' .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md
grep -q '^audited: 2026-04-17$' .planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md

# Phase 10 verification
grep -q '^status: passed$' .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md
grep -q '### Human Verification Checklist (Phase 16 follow-up)' .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md

# Screenshots present
ls .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/*.png | wc -l
# Expected: 6

# All 3 tested boxes ticked
grep -cE '^- \[x\] \*\*Tested\*\*$' .planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md
# Expected: 3
```

All 5 checks must match the expected values. No test suite runs — this is documentation + human verification only.
</verification>

<success_criteria>
1. `.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-VALIDATION.md` has frontmatter `status: approved` and `audited: 2026-04-17` (matching Phase 13's literal key per D-13 conflict resolution)
2. `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md` has a new `### Human Verification Checklist (Phase 16 follow-up)` section with 3 checks, each linking to 2 screenshot paths
3. `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-verification-screenshots/` contains 6 PNG files (3 checks × 2 modes)
4. `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-VERIFICATION.md` frontmatter `status: human_needed` → `status: passed` AFTER user completes the checks
5. All three checklist checkboxes flipped to `[x] Tested`; `Verified by:` and `Verified date:` populated
6. v1.0-MILESTONE-AUDIT.md Tech Debt items "Phase 14 `14-VALIDATION.md` `status: draft`" (line 224) and "Phase 10 `human_needed` status" (line 211) are closed
</success_criteria>

<output>
After completion, create `.planning/phases/16-v1-0-hygiene-traceability-cleanup/16-03-SUMMARY.md` documenting:
- The Phase 14 frontmatter flip (status + audited line) with D-13 conflict resolution note (chose `audited:` over `approved:` for Phase 13 precedent fidelity — single-line rationale)
- The Phase 10 checklist insertion (3 checks, 6 screenshot slots, completion-flow guide)
- The screenshots directory creation + final contents (6 PNG filenames + sizes)
- The Phase 10 frontmatter flip (status: human_needed → status: passed) + human verifier identity + date
- Audit item closure references (v1.0-MILESTONE-AUDIT.md lines 211, 224)
</output>
