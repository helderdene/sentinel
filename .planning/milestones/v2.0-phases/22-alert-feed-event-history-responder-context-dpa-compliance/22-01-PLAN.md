---
phase: 22
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_22_010001_add_dismissed_by_and_reason_to_recognition_events_table.php
  - database/migrations/2026_04_22_010002_create_fras_access_log_table.php
  - database/migrations/2026_04_22_010003_create_fras_purge_runs_table.php
  - database/migrations/2026_04_22_010004_create_fras_legal_signoffs_table.php
  - database/migrations/2026_04_22_010005_add_fras_audio_muted_to_users_table.php
  - app/Enums/FrasDismissReason.php
  - app/Enums/FrasAccessSubject.php
  - app/Enums/FrasAccessAction.php
  - app/Models/FrasAccessLog.php
  - app/Models/FrasPurgeRun.php
  - app/Models/FrasLegalSignoff.php
  - app/Models/RecognitionEvent.php
  - app/Models/User.php
  - database/factories/FrasAccessLogFactory.php
autonomous: true
requirements: [ALERTS-02, DPA-04, DPA-05, DPA-07]
must_haves:
  truths:
    - "recognition_events has the 3 missing dismiss columns + 2 date indexes; acknowledged_by is NOT re-declared"
    - "fras_access_log, fras_purge_runs, fras_legal_signoffs tables exist with UUID PKs + timestamptz"
    - "users.fras_audio_muted column exists with default false"
    - "FrasDismissReason / FrasAccessSubject / FrasAccessAction enums expose the locked string-backed values"
  artifacts:
    - path: "database/migrations/2026_04_22_010001_add_dismissed_by_and_reason_to_recognition_events_table.php"
      provides: "ALTER adding dismissed_by (foreignId), dismiss_reason (varchar 32 + CHECK), dismiss_reason_note (text), indexes on acknowledged_at + dismissed_at"
      contains: "$table->foreignId('dismissed_by')"
    - path: "database/migrations/2026_04_22_010002_create_fras_access_log_table.php"
      provides: "fras_access_log schema: uuid id, foreignId actor_user_id, ipAddress, uuid subject_id, subject_type + action CHECK constraints"
      contains: "fras_access_log_subject_type_check"
    - path: "database/migrations/2026_04_22_010003_create_fras_purge_runs_table.php"
      provides: "fras_purge_runs schema"
      contains: "face_crops_purged"
    - path: "database/migrations/2026_04_22_010004_create_fras_legal_signoffs_table.php"
      provides: "fras_legal_signoffs schema"
      contains: "signed_by_name"
    - path: "database/migrations/2026_04_22_010005_add_fras_audio_muted_to_users_table.php"
      provides: "users.fras_audio_muted boolean default false"
      contains: "fras_audio_muted"
    - path: "app/Enums/FrasDismissReason.php"
      provides: "FalseMatch / TestEvent / Duplicate / Other cases + label()"
      exports: ["FalseMatch", "TestEvent", "Duplicate", "Other", "label"]
    - path: "app/Enums/FrasAccessSubject.php"
      provides: "RecognitionEventFace / RecognitionEventScene / PersonnelPhoto cases"
      exports: ["RecognitionEventFace", "RecognitionEventScene", "PersonnelPhoto"]
    - path: "app/Enums/FrasAccessAction.php"
      provides: "View / Download cases"
      exports: ["View", "Download"]
    - path: "app/Models/FrasAccessLog.php"
      provides: "Eloquent model with HasUuids, $table='fras_access_log', fillable + enum casts + actor() relation"
      contains: "use HasUuids"
    - path: "app/Models/FrasPurgeRun.php"
      provides: "Eloquent model with HasUuids + boolean/integer casts"
      contains: "use HasUuids"
    - path: "app/Models/FrasLegalSignoff.php"
      provides: "Eloquent model with HasUuids"
      contains: "use HasUuids"
  key_links:
    - from: "app/Models/RecognitionEvent.php"
      to: "app/Enums/FrasDismissReason.php"
      via: "$casts['dismiss_reason']"
      pattern: "FrasDismissReason::class"
    - from: "app/Models/FrasAccessLog.php"
      to: "app/Enums/FrasAccessSubject.php"
      via: "$casts['subject_type']"
      pattern: "FrasAccessSubject::class"
---

<objective>
Wave 1 (Plan 1 of 2): Ship the schema + enums + models that every later wave binds to. Five migrations (additive to recognition_events, three new tables, one user column add), three enums, three new models, two model extensions. NO business logic — just storage + shape.

Purpose: Establish the DPA-grade append-only audit surface (`fras_access_log`) + retention summary (`fras_purge_runs`) + legal sign-off (`fras_legal_signoffs`) + the missing dismiss columns on `recognition_events` before any controller touches them. Honors RESEARCH reconciliation #2 (acknowledged_by already exists — don't re-declare) and reconciliation #1 (foreignId everywhere, never foreignUuid).

Output: 5 migrations + 3 enums + 3 new models + RecognitionEvent/User model extensions + FrasAccessLog factory — all committed, green under `php artisan migrate:fresh --env=testing`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-CONTEXT.md
@.planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-RESEARCH.md
@.planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-PATTERNS.md
@CLAUDE.md

<interfaces>
Key existing references executor should lean on:

From `database/migrations/2026_04_21_000004_create_recognition_events_table.php:46-51`:
```php
$table->string('severity', 10)->default('info');
$table->foreignId('acknowledged_by')->nullable()->constrained('users')->nullOnDelete();
$table->timestampTz('acknowledged_at', precision: 0)->nullable();
$table->timestampTz('dismissed_at', precision: 0)->nullable();
```
These already exist — migration 010001 MUST NOT re-declare them.

From `app/Enums/PersonnelCategory.php` (analog for string-backed enum + label()):
```php
enum PersonnelCategory: string { case Block = 'block'; ... public function label(): string {...} }
```

From `app/Enums/RecognitionSeverity.php` (3-case string-backed analog).

From `app/Models/RecognitionEvent.php` (existing model being extended; already has `acknowledgedBy()` relation per Phase 18).

From `database/migrations/2026_04_22_000002_add_barangay_and_notes_to_cameras_table.php` (ALTER idiom with ->after() chaining).

From `database/migrations/2026_04_22_000001_add_photo_access_token_to_personnel_table.php` (single-column additive migration analog).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five migrations (additive to recognition_events + 3 new tables + users column)</name>
  <files>
    - database/migrations/2026_04_22_010001_add_dismissed_by_and_reason_to_recognition_events_table.php
    - database/migrations/2026_04_22_010002_create_fras_access_log_table.php
    - database/migrations/2026_04_22_010003_create_fras_purge_runs_table.php
    - database/migrations/2026_04_22_010004_create_fras_legal_signoffs_table.php
    - database/migrations/2026_04_22_010005_add_fras_audio_muted_to_users_table.php
  </files>
  <read_first>
    - database/migrations/2026_04_21_000004_create_recognition_events_table.php (full file — confirm acknowledged_by/acknowledged_at/dismissed_at already exist; see lines 46-51; also see the existing severity CHECK constraint pattern at lines 62-65 for the DB::statement idiom)
    - database/migrations/2026_04_22_000002_add_barangay_and_notes_to_cameras_table.php (ALTER pattern with ->after() + DB::statement CHECK; verbatim analog for migration 010001)
    - database/migrations/2026_04_22_000001_add_photo_access_token_to_personnel_table.php (single-column additive analog for migration 010005)
    - .planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-PATTERNS.md §Wave 1 §§ "add_dismissed_by_and_reason" / "create_fras_access_log" / "create_fras_purge_runs" / "create_fras_legal_signoffs" / "add_fras_audio_muted" (the 5 migration excerpts; copy verbatim)
    - .planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-CONTEXT.md §D-03, D-15, D-24, D-38 (schema shapes — but override foreignUuid→foreignId per RESEARCH reconciliation #2)
    - .planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-RESEARCH.md §"Pitfalls & Landmines" #1, #2, #12 (acknowledged_by pre-existence; user FK type; fras_audio_muted default)
  </read_first>
  <behavior>
    - Migration 010001: ONLY adds `dismissed_by` (foreignId nullable constrained('users') nullOnDelete), `dismiss_reason` (varchar 32 nullable), `dismiss_reason_note` (text nullable), index on acknowledged_at, index on dismissed_at, raw DB::statement adding CHECK constraint `recognition_events_dismiss_reason_check` covering `NULL OR IN ('false_match','test_event','duplicate','other')`. DOES NOT touch acknowledged_by/acknowledged_at/dismissed_at (they exist from Phase 18).
    - Migration 010002 creates `fras_access_log`: uuid id PK, foreignId actor_user_id constrained('users') cascadeOnDelete, ipAddress, string user_agent 255 nullable, string subject_type 48, uuid subject_id, string action 16, timestampTz accessed_at precision 0 + index, timestamps(), composite index on (subject_type, subject_id), composite index on (actor_user_id, accessed_at). Two raw DB::statement CHECK constraints: subject_type IN ('recognition_event_face','recognition_event_scene','personnel_photo'); action IN ('view','download').
    - Migration 010003 creates `fras_purge_runs`: uuid id PK, timestampTz started_at precision 0, timestampTz finished_at precision 0 nullable, boolean dry_run default false, unsignedInteger face_crops_purged default 0, unsignedInteger scene_images_purged default 0, unsignedInteger skipped_for_active_incident default 0, unsignedInteger access_log_rows_purged default 0, text error_summary nullable, timestamps().
    - Migration 010004 creates `fras_legal_signoffs`: uuid id PK, string signed_by_name 150, string contact 150, timestampTz signed_at precision 0, text notes nullable, timestamps().
    - Migration 010005 adds `users.fras_audio_muted` boolean default false after 'role' (NOT nullable per RESEARCH pitfall #12).
    - Every migration ships a matching `down()` that reverses changes (drop CHECK first, then drop columns/table).
    - Run `php artisan migrate:fresh --env=testing` and the migration set applies cleanly.
  </behavior>
  <action>
Create the five migration files. File naming convention: `2026_04_22_0100{N}_*.php` where N is 1–5. Use `foreignId` NOT `foreignUuid` on every user FK per RESEARCH reconciliation #2 (users table has bigint PK via `$table->id()`). Use `timestampTz(..., precision: 0)` for every datetime column (Phase 18 convention). DO NOT use `->cascadeOnDelete()` on actor_user_id — use the exact shape in PATTERNS.md §Wave 1 fras_access_log (it says cascadeOnDelete; match it). 

For migration 010001, the CHECK constraint body:
```php
DB::statement(
    'ALTER TABLE recognition_events ADD CONSTRAINT recognition_events_dismiss_reason_check '
    ."CHECK (dismiss_reason IS NULL OR dismiss_reason IN ('false_match','test_event','duplicate','other'))"
);
```
Drop the CHECK in `down()` BEFORE dropping the column: `DB::statement('ALTER TABLE recognition_events DROP CONSTRAINT IF EXISTS recognition_events_dismiss_reason_check');`

For migration 010002, DO NOT put a `->foreign()` on `subject_id` — polymorphic across recognition_events.id + personnel.id; integrity is CHECK + enum-guarded at app layer.

After writing, run `vendor/bin/pint --dirty --format agent` to format. Then run `php artisan migrate:fresh --env=testing --seed=false` to confirm full migration stack green (existing phases + these 5 new ones).
  </action>
  <verify>
    <automated>php artisan migrate:fresh --env=testing 2>&1 | tail -20 &amp;&amp; php -r "echo file_exists('database/migrations/2026_04_22_010001_add_dismissed_by_and_reason_to_recognition_events_table.php') &amp;&amp; file_exists('database/migrations/2026_04_22_010002_create_fras_access_log_table.php') &amp;&amp; file_exists('database/migrations/2026_04_22_010003_create_fras_purge_runs_table.php') &amp;&amp; file_exists('database/migrations/2026_04_22_010004_create_fras_legal_signoffs_table.php') &amp;&amp; file_exists('database/migrations/2026_04_22_010005_add_fras_audio_muted_to_users_table.php') ? 'ALL 5 EXIST' : 'MISSING';"</automated>
  </verify>
  <acceptance_criteria>
    - grep `foreignId('dismissed_by')` database/migrations/2026_04_22_010001_*.php finds the line
    - grep `acknowledged_by` database/migrations/2026_04_22_010001_*.php finds ZERO matches (Phase 18 already has it)
    - grep `fras_access_log_subject_type_check` database/migrations/2026_04_22_010002_*.php finds the raw CHECK statement
    - grep `foreignUuid` database/migrations/2026_04_22_010*.php returns ZERO matches (all user FKs are foreignId)
    - grep `fras_audio_muted` database/migrations/2026_04_22_010005_*.php finds `$table->boolean('fras_audio_muted')->default(false)`
    - `php artisan migrate:fresh --env=testing` exits 0 and reports 5 new migrations run
    - `psql` (or sqlite equivalent via artisan) shows `recognition_events.dismissed_by` column (inspect via `php artisan db:show --env=testing`)
  </acceptance_criteria>
  <done>All 5 migrations committed; `migrate:fresh --env=testing` runs clean; no pre-existing migration re-touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three enums + three new models + RecognitionEvent/User extensions + FrasAccessLogFactory</name>
  <files>
    - app/Enums/FrasDismissReason.php
    - app/Enums/FrasAccessSubject.php
    - app/Enums/FrasAccessAction.php
    - app/Models/FrasAccessLog.php
    - app/Models/FrasPurgeRun.php
    - app/Models/FrasLegalSignoff.php
    - app/Models/RecognitionEvent.php
    - app/Models/User.php
    - database/factories/FrasAccessLogFactory.php
  </files>
  <read_first>
    - app/Enums/PersonnelCategory.php (string-backed + label() analog for FrasDismissReason)
    - app/Enums/RecognitionSeverity.php (3-case string-backed analog for FrasAccessSubject)
    - app/Enums/CameraStatus.php (2-case string-backed analog for FrasAccessAction)
    - app/Models/RecognitionEvent.php (existing — already has acknowledgedBy() relation + acknowledged_at cast from Phase 18; Phase 22 only adds dismiss_* wiring)
    - app/Models/User.php (existing — extend $fillable + $casts)
    - app/Models/CameraEnrollment.php (UUID PK + HasUuids analog for FrasPurgeRun/FrasLegalSignoff)
    - .planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-PATTERNS.md §Wave 1 §§ FrasDismissReason / FrasAccessSubject / FrasAccessAction / FrasAccessLog / FrasPurgeRun / RecognitionEvent MOD / User MOD (copy-ready excerpts)
    - .planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-UI-SPEC.md §Copywriting Contract §DismissReasonModal (enum labels: False match / Test event / Duplicate alert / Other)
  </read_first>
  <behavior>
    - `FrasDismissReason` string-backed enum with 4 cases (FalseMatch='false_match', TestEvent='test_event', Duplicate='duplicate', Other='other') + `label()` method returning UI-SPEC copy.
    - `FrasAccessSubject` string-backed enum with 3 cases (RecognitionEventFace='recognition_event_face', RecognitionEventScene='recognition_event_scene', PersonnelPhoto='personnel_photo').
    - `FrasAccessAction` string-backed enum with 2 cases (View='view', Download='download').
    - `FrasAccessLog` model: HasUuids, `$table = 'fras_access_log'` (explicit to dodge pluralizer), fillable = [actor_user_id, ip_address, user_agent, subject_type, subject_id, action, accessed_at], casts = [subject_type => FrasAccessSubject::class, action => FrasAccessAction::class, accessed_at => 'immutable_datetime'], actor() BelongsTo User via actor_user_id.
    - `FrasPurgeRun` model: HasUuids, fillable covering all summary columns (started_at, finished_at, dry_run, face_crops_purged, scene_images_purged, skipped_for_active_incident, access_log_rows_purged, error_summary), casts (dry_run=>bool, started_at/finished_at=>'immutable_datetime').
    - `FrasLegalSignoff` model: HasUuids, fillable [signed_by_name, contact, signed_at, notes], cast signed_at => 'immutable_datetime'.
    - `RecognitionEvent` MOD: append 'dismissed_by', 'dismiss_reason', 'dismiss_reason_note' to $fillable; append `'dismiss_reason' => \App\Enums\FrasDismissReason::class` to $casts; add `dismissedBy(): BelongsTo => $this->belongsTo(User::class, 'dismissed_by')`. DO NOT touch existing acknowledgedBy() or acknowledged_at cast.
    - `User` MOD: append 'fras_audio_muted' to $fillable; append `'fras_audio_muted' => 'bool'` to $casts.
    - `FrasAccessLogFactory` with definition producing valid row (User::factory() for actor_user_id, fake ip, subject_type default RecognitionEventFace, action default View, accessed_at=now()).
  </behavior>
  <action>
Create enums under `app/Enums/`. Enum pattern shape (from PersonnelCategory):
```php
<?php
namespace App\Enums;
enum FrasDismissReason: string {
    case FalseMatch = 'false_match';
    case TestEvent = 'test_event';
    case Duplicate = 'duplicate';
    case Other = 'other';

    public function label(): string {
        return match ($this) {
            self::FalseMatch => 'False match',
            self::TestEvent => 'Test event',
            self::Duplicate => 'Duplicate alert',
            self::Other => 'Other',
        };
    }
}
```

Create models under `app/Models/`. FrasAccessLog shape (per PATTERNS.md):
```php
<?php
namespace App\Models;
use App\Enums\FrasAccessAction;
use App\Enums\FrasAccessSubject;
use Illuminate\Database\Eloquent\Concerns\HasUuids;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

class FrasAccessLog extends Model {
    use HasUuids;
    protected $table = 'fras_access_log';
    protected $fillable = ['actor_user_id','ip_address','user_agent','subject_type','subject_id','action','accessed_at'];
    protected $casts = [
        'subject_type' => FrasAccessSubject::class,
        'action' => FrasAccessAction::class,
        'accessed_at' => 'immutable_datetime',
    ];
    public function actor(): BelongsTo { return $this->belongsTo(User::class, 'actor_user_id'); }
}
```

For RecognitionEvent — open the file, add the 3 fillable strings to the existing array, add the cast, add the dismissedBy() method next to existing acknowledgedBy(). DO NOT rewrite or duplicate existing content.

For User — same minimal additive edit; locate $fillable, append 'fras_audio_muted'; locate $casts, add the bool cast.

Create database/factories/FrasAccessLogFactory.php with `definition()` returning an array covering all fillable fields. Use `Str::uuid()->toString()` for subject_id.

After: `vendor/bin/pint --dirty --format agent`. Then run `php artisan test --compact --filter=FrasPhotoAccessControllerTest` (existing Phase 20 test) to prove RecognitionEvent + User MODs don't break existing green tests.
  </action>
  <verify>
    <automated>vendor/bin/pint --dirty --format agent &amp;&amp; php artisan test --compact --filter=FrasPhotoAccessControllerTest</automated>
  </verify>
  <acceptance_criteria>
    - `php -r "enum_exists('App\\Enums\\FrasDismissReason') ? print 'OK' : print 'MISSING';"` prints OK
    - `php -r "enum_exists('App\\Enums\\FrasAccessSubject') ? print 'OK' : print 'MISSING';"` prints OK
    - `php -r "enum_exists('App\\Enums\\FrasAccessAction') ? print 'OK' : print 'MISSING';"` prints OK
    - grep `case FalseMatch = 'false_match'` app/Enums/FrasDismissReason.php matches
    - grep `case RecognitionEventFace = 'recognition_event_face'` app/Enums/FrasAccessSubject.php matches
    - grep `protected \$table = 'fras_access_log'` app/Models/FrasAccessLog.php matches
    - grep `use HasUuids` app/Models/FrasAccessLog.php matches
    - grep `dismissedBy` app/Models/RecognitionEvent.php matches (new relation)
    - grep `fras_audio_muted` app/Models/User.php finds it in $fillable AND $casts
    - grep `FrasDismissReason::class` app/Models/RecognitionEvent.php matches
    - `php artisan test --compact --filter=FrasPhotoAccessControllerTest` exits 0 (Phase 20 regression check green)
  </acceptance_criteria>
  <done>3 enums, 3 new models, 2 model extensions, 1 factory committed; Pint clean; existing FRAS tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema → DPA audit | fras_access_log is the legal record of biometric image access; schema integrity underpins DPA-02 |
| Column CHECK constraints → enum whitelist | Any enum drift (renaming a case) without CHECK update fails downstream writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | T (Tampering) | Migration 010001 re-declaring `acknowledged_by` | mitigate | RESEARCH pitfall #1 flagged; acceptance criterion greps for absence. Re-declaring would fail migration on pgsql (duplicate column) — detected at migrate:fresh time. |
| T-22-01-02 | T (Tampering) | Wrong FK type on user columns (`foreignUuid` vs `foreignId`) | mitigate | All 4 user FKs in Wave 1 use `foreignId` per RESEARCH reconciliation #2; acceptance criterion greps ZERO matches for `foreignUuid` in new migrations |
| T-22-01-03 | R (Repudiation) | fras_access_log FK violation silently dropping audit row | mitigate | foreignId on actor_user_id → constrained('users')->cascadeOnDelete; any stale FK fails Wave 2 controller writes loudly (500) instead of silently skipping the audit row |
| T-22-01-04 | T (Tampering) | Enum value drift (e.g. renaming FrasDismissReason::Other) without updating CHECK | accept | Phase 22 enums are new, never renamed; any future rename ships a new migration updating both enum + CHECK simultaneously (standard Laravel enum evolution) |
| T-22-01-05 | I (Info disclosure) | fras_audio_muted column leaking via share($request) to other users | mitigate | Wave 3 Plan 4 will extend HandleInertiaRequests to expose this ONLY on `auth.user.fras_audio_muted` (own user); no cross-user leak because the share only surfaces the authenticated user's own row |
</threat_model>

<verification>
```bash
php artisan migrate:fresh --env=testing
php artisan test --compact --filter=FrasPhotoAccessControllerTest
vendor/bin/pint --dirty --format agent --test
```
</verification>

<success_criteria>
- All 5 migrations committed and `migrate:fresh --env=testing` exits 0
- 3 new enums + 3 new models + 2 model extensions + 1 factory committed
- Phase 18 recognition_events.acknowledged_by NOT re-declared (grep confirms)
- Every user FK in Wave 1 uses foreignId (grep confirms zero foreignUuid)
- Phase 20 FrasPhotoAccessControllerTest still green (regression check)
</success_criteria>

<output>
After completion, create `.planning/phases/22-alert-feed-event-history-responder-context-dpa-compliance/22-01-SUMMARY.md`
</output>
