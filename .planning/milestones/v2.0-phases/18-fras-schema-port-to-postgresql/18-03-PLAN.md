---
phase: 18
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - database/migrations/2026_04_21_000003_create_camera_enrollments_table.php
  - app/Enums/CameraEnrollmentStatus.php
  - app/Models/CameraEnrollment.php
  - database/factories/CameraEnrollmentFactory.php
autonomous: true
requirements:
  - FRAMEWORK-04
user_setup: []

must_haves:
  truths:
    - "camera_enrollments pivot table exists with UUID PK, FK to cameras + personnel (both ON DELETE CASCADE), status VARCHAR(20) DEFAULT 'pending' with CHECK constraint, and UNIQUE (camera_id, personnel_id) (D-21..D-30)"
    - "Composite indexes (camera_id, status) and (personnel_id, status) exist for Phase 20 enrollment progress queries (D-29)"
    - "CameraEnrollmentStatus enum cases Pending/Syncing/Done/Failed map to lowercase backed strings matching DB CHECK (D-24, D-65)"
    - "CameraEnrollment model uses HasUuids + HasFactory + CameraEnrollmentStatus cast; extends plain Model (not Pivot) because row has its own UUID PK (18-PATTERNS.md §CameraEnrollment)"
    - "CameraEnrollmentFactory builds via Camera::factory() + Personnel::factory() sub-factories (D-61)"
  artifacts:
    - path: "database/migrations/2026_04_21_000003_create_camera_enrollments_table.php"
      provides: "Pivot DDL with composite UNIQUE + status CHECK + per-camera and per-personnel indexes"
      contains: "Schema::create('camera_enrollments'"
    - path: "app/Enums/CameraEnrollmentStatus.php"
      provides: "string-backed enum for camera_enrollments.status"
      contains: "enum CameraEnrollmentStatus: string"
    - path: "app/Models/CameraEnrollment.php"
      provides: "Model (not Pivot) with HasUuids + status cast"
      contains: "class CameraEnrollment extends Model"
    - path: "database/factories/CameraEnrollmentFactory.php"
      provides: "Factory with sub-factory FK wiring"
      contains: "Camera::factory()"
  key_links:
    - from: "database/migrations/2026_04_21_000003_create_camera_enrollments_table.php"
      to: "cameras, personnel tables"
      via: "foreignUuid('camera_id')->constrained()->cascadeOnDelete() and foreignUuid('personnel_id')->constrained()->cascadeOnDelete()"
      pattern: "foreignUuid\\('(camera_id|personnel_id)'\\)->constrained\\(\\)->cascadeOnDelete"
    - from: "database/migrations/2026_04_21_000003_create_camera_enrollments_table.php"
      to: "camera_enrollments_status_check constraint"
      via: "raw DB::statement after Schema::create"
      pattern: "camera_enrollments_status_check"
---

<objective>
Create the `camera_enrollments` pivot table with FK cascades, composite UNIQUE, and status CHECK, plus its model, CameraEnrollmentStatus enum, and factory.

Purpose: Satisfy FRAMEWORK-04 for the enrollment pivot. Phase 20 EnrollPersonnelBatch writes status transitions (pending → syncing → done/failed); admin CRUD reads per-camera progress via the composite index.

Output: 4 new files. Depends on Wave 1 plans (cameras + personnel tables must exist before FK constraints resolve). Atomic commit.

Dependency rationale: This plan declares `depends_on: [18-01, 18-02]` so execute-phase orders it after cameras + personnel tables land. Although migrations would run in filename order regardless, this dependency avoids a partial-wave scenario where CameraEnrollment model references Camera/Personnel models that don't yet exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-fras-schema-port-to-postgresql/18-CONTEXT.md
@.planning/phases/18-fras-schema-port-to-postgresql/18-RESEARCH.md
@.planning/phases/18-fras-schema-port-to-postgresql/18-PATTERNS.md
@.planning/phases/18-fras-schema-port-to-postgresql/18-01-SUMMARY.md
@.planning/phases/18-fras-schema-port-to-postgresql/18-02-SUMMARY.md
@CLAUDE.md

# Canonical analogs
@database/migrations/2026_03_13_200001_create_incident_unit_table.php
@database/migrations/2026_03_12_000006_create_incidents_table.php
@app/Models/Incident.php
@app/Models/IncidentUnit.php
@app/Enums/IncidentOutcome.php
@database/factories/IncidentFactory.php

<interfaces>
<!-- Key contracts -->

From 18-PATTERNS.md §camera_enrollments migration (adaptation for UUID FK sugar in Laravel 13):
```php
$table->foreignUuid('camera_id')->constrained()->cascadeOnDelete();
$table->foreignUuid('personnel_id')->constrained()->cascadeOnDelete();
// composite UNIQUE + per-camera/per-personnel status index:
$table->unique(['camera_id', 'personnel_id']);
$table->index(['camera_id', 'status']);
$table->index(['personnel_id', 'status']);
```

IMPORTANT: `foreignUuid('personnel_id')->constrained()` infers table `personnel` — since Laravel's pluralizer maps `personnel_id` → `personnel` table in current versions, this works. If infrastructure fails, fall back to `->constrained('personnel')` explicitly.

From 18-PATTERNS.md §CameraEnrollment model: extends `Model` NOT `Pivot` (row has UUID PK + timestamps). Copy Camera/Personnel model structure, not IncidentUnit's Pivot shape.

From database/factories/IncidentFactory.php line 28 (sub-factory FK wiring):
```php
'incident_type_id' => IncidentType::factory(),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CameraEnrollmentStatus enum and camera_enrollments migration</name>
  <files>app/Enums/CameraEnrollmentStatus.php, database/migrations/2026_04_21_000003_create_camera_enrollments_table.php</files>
  <read_first>
    - @.planning/phases/18-fras-schema-port-to-postgresql/18-CONTEXT.md §camera_enrollments table (D-21..D-30)
    - @.planning/phases/18-fras-schema-port-to-postgresql/18-PATTERNS.md §camera_enrollments migration + §CameraEnrollmentStatus
    - @app/Enums/IncidentOutcome.php (enum skeleton)
    - @database/migrations/2026_03_13_200001_create_incident_unit_table.php (pivot skeleton — adapt to foreignUuid sugar and add own UUID PK)
  </read_first>
  <action>
    Step A — Create `app/Enums/CameraEnrollmentStatus.php`:
    - Namespace `App\Enums`, string-backed.
    - Cases (lowercase per D-24): `Pending = 'pending'`, `Syncing = 'syncing'`, `Done = 'done'`, `Failed = 'failed'`.
    - `label()` method: "Pending", "Syncing", "Done", "Failed".

    Step B — Create `database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`:
    - Skeleton from create_incidents_table.php lines 1-14, 50-58.
    - Add `use Illuminate\Support\Facades\DB;`.
    - Inside `Schema::create('camera_enrollments', function (Blueprint $table) { ... })`:
      - `$table->uuid('id')->primary();` (D-21)
      - `$table->foreignUuid('camera_id')->constrained()->cascadeOnDelete();` (D-22)
      - `$table->foreignUuid('personnel_id')->constrained('personnel')->cascadeOnDelete();` (D-23 — explicit table arg because Laravel pluralizer may not map `personnel_id` → `personnel`)
      - `$table->string('status', 20)->default('pending');` (D-24)
      - `$table->timestampTz('enrolled_at', precision: 0)->nullable();` (D-25 — uniform non-microsecond precision 0)
      - `$table->string('photo_hash', 32)->nullable();` (D-26)
      - `$table->text('last_error')->nullable();` (D-27)
      - `$table->timestamps();` (D-30)
      - `$table->unique(['camera_id', 'personnel_id']);` (D-28)
      - `$table->index(['camera_id', 'status']);` (D-29)
      - `$table->index(['personnel_id', 'status']);` (D-29)
    - AFTER `Schema::create` closure:
      ```php
      DB::statement(
          "ALTER TABLE camera_enrollments ADD CONSTRAINT camera_enrollments_status_check "
          . "CHECK (status IN ('pending','syncing','done','failed'))"
      );
      ```
    - `down()`: `Schema::dropIfExists('camera_enrollments');`

    Step C — `vendor/bin/pint --dirty --format agent`. (Pint is idempotent, formats in-place; no `--test` re-run per CLAUDE.md.)
  </action>
  <verify>
    <automated>php artisan migrate:fresh 2>&1 | grep -q "create_camera_enrollments_table"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Enums/CameraEnrollmentStatus.php`
    - `grep -q "case Pending = 'pending'" app/Enums/CameraEnrollmentStatus.php`
    - `test -f database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "foreignUuid('camera_id')->constrained()->cascadeOnDelete" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "foreignUuid('personnel_id')->constrained('personnel')->cascadeOnDelete" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "\$table->timestampTz('enrolled_at', precision: 0)" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "unique(\\['camera_id', 'personnel_id'\\])" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "index(\\['camera_id', 'status'\\])" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `grep -q "camera_enrollments_status_check" database/migrations/2026_04_21_000003_create_camera_enrollments_table.php`
    - `php artisan migrate:fresh` exits 0
    - `php artisan db:table camera_enrollments` shows all 9 columns
  </acceptance_criteria>
  <done>camera_enrollments pivot migrates green with FK cascades + composite UNIQUE + per-status indexes + CHECK constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Create CameraEnrollment model and factory</name>
  <files>app/Models/CameraEnrollment.php, database/factories/CameraEnrollmentFactory.php</files>
  <read_first>
    - @app/Models/Incident.php lines 1-97 (HasUuids + casts pattern)
    - @.planning/phases/18-fras-schema-port-to-postgresql/18-PATTERNS.md §CameraEnrollment model (critical: extends Model NOT Pivot)
    - @.planning/phases/18-fras-schema-port-to-postgresql/18-CONTEXT.md §D-63, D-64, D-61
    - @database/factories/IncidentFactory.php (factory skeleton)
  </read_first>
  <action>
    Step A — Create `app/Models/CameraEnrollment.php`:
    - Namespace `App\Models`.
    - Imports: `App\Enums\CameraEnrollmentStatus`, `Database\Factories\CameraEnrollmentFactory`, `Illuminate\Database\Eloquent\Concerns\HasUuids`, `Illuminate\Database\Eloquent\Factories\HasFactory`, `Illuminate\Database\Eloquent\Model`, `Illuminate\Database\Eloquent\Relations\BelongsTo`.
    - Class `CameraEnrollment extends Model` (per 18-PATTERNS.md: do NOT extend Pivot — this row has its own UUID PK + timestamps):
      - `/** @use HasFactory<CameraEnrollmentFactory> */` + `use HasFactory, HasUuids;`
      - `protected $fillable = ['camera_id', 'personnel_id', 'status', 'enrolled_at', 'photo_hash', 'last_error'];`
      - `protected function casts(): array` per D-64:
        ```php
        [
            'status' => CameraEnrollmentStatus::class,
            'enrolled_at' => 'datetime',
        ]
        ```
      - Minimal relations (D-66): `camera(): BelongsTo { return $this->belongsTo(Camera::class); }` and `personnel(): BelongsTo { return $this->belongsTo(Personnel::class); }`.

    Step B — Create `database/factories/CameraEnrollmentFactory.php`:
    - Namespace `Database\Factories`.
    - Imports: `App\Enums\CameraEnrollmentStatus`, `App\Models\Camera`, `App\Models\CameraEnrollment`, `App\Models\Personnel`, `Illuminate\Database\Eloquent\Factories\Factory`.
    - `@extends Factory<CameraEnrollment>` docblock.
    - `protected $model = CameraEnrollment::class;`
    - `public function definition(): array` per 18-PATTERNS.md §CameraEnrollmentFactory:
      ```php
      return [
          'camera_id' => Camera::factory(),
          'personnel_id' => Personnel::factory(),
          'status' => CameraEnrollmentStatus::Pending,
          'enrolled_at' => null,
          'photo_hash' => null,
          'last_error' => null,
      ];
      ```

    Step C — `vendor/bin/pint --dirty --format agent`. (Pint is idempotent, formats in-place; no `--test` re-run per CLAUDE.md.)
  </action>
  <verify>
    <automated>php artisan tinker --execute 'echo \App\Models\CameraEnrollment::factory()->create()->status->value;' 2>&1 | grep -q "^pending"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/Models/CameraEnrollment.php`
    - `grep -q "class CameraEnrollment extends Model" app/Models/CameraEnrollment.php` (NOT Pivot)
    - `grep -q "use HasFactory, HasUuids;" app/Models/CameraEnrollment.php`
    - `grep -q "'status' => CameraEnrollmentStatus::class" app/Models/CameraEnrollment.php`
    - `grep -q "function camera():" app/Models/CameraEnrollment.php`
    - `grep -q "function personnel():" app/Models/CameraEnrollment.php`
    - `test -f database/factories/CameraEnrollmentFactory.php`
    - `grep -q "Camera::factory()" database/factories/CameraEnrollmentFactory.php`
    - `grep -q "Personnel::factory()" database/factories/CameraEnrollmentFactory.php`
    - `php artisan tinker --execute 'CameraEnrollment::factory()->create();'` exits 0 (creates camera + personnel + enrollment row together via nested factories)
    - Duplicate `(camera_id, personnel_id)` insert throws UniqueConstraintViolationException (verified later in Plan 05 but confirmed structurally here)
  </acceptance_criteria>
  <done>CameraEnrollment::factory()->create() builds full camera + personnel + enrollment row chain; status enum casts correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FK cascade | Hard-delete of camera or personnel cascades camera_enrollments rows; no recognition_events cascade (Plan 04 uses RESTRICT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | camera_enrollments.status | mitigate | DB CHECK `status IN ('pending','syncing','done','failed')` (D-24) rejects garbage writes |
| T-18-01b | Data Integrity | duplicate enrollment | mitigate | UNIQUE (camera_id, personnel_id) (D-28) prevents double enrollment at DB layer — Phase 20 EnrollPersonnelBatch cannot accidentally insert duplicates |
| T-18-03 | Denial of Service | cascade on camera delete | accept | Decommission flow is admin-only (Phase 20). Hard-deleting a camera legitimately cleans enrollment rows; recognition_events separately protected via RESTRICT in Plan 04 |
</threat_model>

<verification>
- `php artisan migrate:fresh` exits 0 with cameras, personnel, camera_enrollments in order
- `CameraEnrollment::factory()->create()` builds and persists
- Duplicate (camera_id, personnel_id) insert fails at DB layer
- Step C's `vendor/bin/pint --dirty --format agent` formats in-place; Pint is idempotent
</verification>

<success_criteria>
- camera_enrollments table exists with 9 columns
- CameraEnrollmentStatus enum importable
- DB CHECK + UNIQUE + 2 composite indexes present in pg_constraint / pg_indexes
- Full suite `php artisan test --compact` green
</success_criteria>

<output>
After completion, create `.planning/phases/18-fras-schema-port-to-postgresql/18-03-SUMMARY.md` summarizing files created, decisions, and suite status.
</output>
</content>
</invoke>