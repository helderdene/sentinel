---
phase: 08-implement-operator-role-and-intake-layer-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Enums/UserRole.php
  - app/Enums/IncidentStatus.php
  - app/Providers/AppServiceProvider.php
  - app/Providers/FortifyServiceProvider.php
  - app/Http/Controllers/IntakeStationController.php
  - app/Http/Requests/TriageIncidentRequest.php
  - app/Http/Requests/ManualEntryRequest.php
  - app/Http/Middleware/HandleInertiaRequests.php
  - database/factories/UserFactory.php
  - database/seeders/OperatorUserSeeder.php
  - database/seeders/DatabaseSeeder.php
  - routes/web.php
  - routes/channels.php
  - resources/js/types/auth.ts
  - resources/js/types/incident.ts
  - resources/js/pages/intake/IntakeStation.vue
  - tests/Unit/Enums/UserRoleTest.php
  - tests/Unit/Enums/IncidentStatusTest.php
  - tests/Feature/Intake/IntakeGatesTest.php
  - tests/Feature/Intake/IntakeStationTest.php
  - tests/Feature/Intake/TriageIncidentTest.php
  - tests/Feature/Auth/OperatorRedirectTest.php
autonomous: true
requirements: [OP-01, OP-02, OP-03, OP-04, OP-06, OP-08, OP-15]

must_haves:
  truths:
    - "Operator role exists and can be assigned to users"
    - "TRIAGED status exists between PENDING and DISPATCHED"
    - "6 new intake gates enforce correct role permissions"
    - "Operator is redirected to intake station after login"
    - "Intake station page renders for operator/supervisor/admin"
    - "Intake station is forbidden for dispatcher/responder"
    - "Triage action transitions incident from PENDING to TRIAGED"
    - "Manual entry creates a new incident directly as TRIAGED in one request"
    - "Operator can subscribe to dispatch.incidents WebSocket channel"
  artifacts:
    - path: "app/Enums/UserRole.php"
      provides: "Operator case in UserRole enum"
      contains: "case Operator"
    - path: "app/Enums/IncidentStatus.php"
      provides: "Triaged case in IncidentStatus enum"
      contains: "case Triaged"
    - path: "app/Http/Controllers/IntakeStationController.php"
      provides: "Intake station page, triage action, and manual entry"
      exports: ["show", "triage", "storeAndTriage"]
    - path: "app/Http/Requests/TriageIncidentRequest.php"
      provides: "Triage form validation"
      contains: "rules"
    - path: "app/Http/Requests/ManualEntryRequest.php"
      provides: "Manual entry form validation (creates + triages in one step)"
      contains: "rules"
    - path: "tests/Unit/Enums/UserRoleTest.php"
      provides: "Operator enum existence unit test"
    - path: "tests/Unit/Enums/IncidentStatusTest.php"
      provides: "TRIAGED status existence unit test"
    - path: "tests/Feature/Intake/IntakeGatesTest.php"
      provides: "Gate permission tests"
    - path: "tests/Feature/Intake/IntakeStationTest.php"
      provides: "Route access tests"
    - path: "tests/Feature/Intake/TriageIncidentTest.php"
      provides: "Triage action and manual entry tests"
  key_links:
    - from: "routes/web.php"
      to: "IntakeStationController"
      via: "intake.station route"
      pattern: "intake\\.station"
    - from: "routes/web.php"
      to: "IntakeStationController::storeAndTriage"
      via: "intake.store-and-triage route"
      pattern: "intake\\.store-and-triage"
    - from: "routes/channels.php"
      to: "UserRole::Operator"
      via: "dispatch.incidents channel auth"
      pattern: "UserRole::Operator"
    - from: "FortifyServiceProvider"
      to: "intake.station route"
      via: "authenticatedRedirectUsing"
      pattern: "authenticatedRedirectUsing"
---

<objective>
Add the Operator role as 5th user role, TRIAGED incident status, 6 new intake gates, IntakeStationController with triage and manual entry actions, Fortify role-based redirect, WebSocket channel authorization, and comprehensive Pest tests.

Purpose: Backend foundation for the intake station -- every subsequent plan depends on the role, gates, route, and controller being in place. The storeAndTriage endpoint enables the manual entry workflow where operators create and classify an incident in a single request.
Output: Working backend with operator role, intake station route, triage endpoint, manual entry endpoint, and passing test suite.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-CONTEXT.md
@.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/Enums/UserRole.php:
```php
enum UserRole: string
{
    case Admin = 'admin';
    case Dispatcher = 'dispatcher';
    case Responder = 'responder';
    case Supervisor = 'supervisor';
}
```

From app/Enums/IncidentStatus.php:
```php
enum IncidentStatus: string
{
    case Pending = 'PENDING';
    case Dispatched = 'DISPATCHED';
    // ... through Resolved
}
```

From app/Providers/AppServiceProvider.php configureGates():
```php
Gate::define('create-incidents', fn (User $user): bool => in_array($user->role, [
    UserRole::Dispatcher, UserRole::Supervisor, UserRole::Admin,
], true));
```

From database/factories/UserFactory.php:
```php
public function admin(): static { return $this->state(fn () => ['role' => UserRole::Admin]); }
public function dispatcher(): static { ... }
public function responder(): static { ... }
public function supervisor(): static { ... }
```

From routes/channels.php:
```php
$dispatchRoles = [UserRole::Dispatcher, UserRole::Supervisor, UserRole::Admin];
```

From app/Http/Middleware/HandleInertiaRequests.php share():
```php
'can' => [
    'manage_users' => $user->can('manage-users'),
    // ... 9 gates shared
]
```

From resources/js/types/auth.ts:
```typescript
export type UserRole = 'admin' | 'dispatcher' | 'responder' | 'supervisor';
export type UserPermissions = { manage_users: boolean; ... };
```

From resources/js/types/incident.ts:
```typescript
export type IncidentStatus = 'PENDING' | 'DISPATCHED' | ... | 'RESOLVED';
```

Database columns are VARCHAR (string), NOT DB-level enums:
- users.role: string(20)
- incidents.status: string(30)
No migration needed for enum value changes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Operator role, TRIAGED status, intake gates, factory, seeder, and tests</name>
  <files>
    app/Enums/UserRole.php
    app/Enums/IncidentStatus.php
    app/Providers/AppServiceProvider.php
    app/Http/Middleware/HandleInertiaRequests.php
    database/factories/UserFactory.php
    database/seeders/OperatorUserSeeder.php
    database/seeders/DatabaseSeeder.php
    routes/channels.php
    resources/js/types/auth.ts
    resources/js/types/incident.ts
    tests/Unit/Enums/UserRoleTest.php
    tests/Unit/Enums/IncidentStatusTest.php
    tests/Feature/Intake/IntakeGatesTest.php
  </files>
  <behavior>
    - Test (unit): Operator case exists in UserRole enum with value 'operator'
    - Test (unit): UserRole enum has exactly 5 cases (Admin, Dispatcher, Operator, Responder, Supervisor)
    - Test (unit): Triaged case exists in IncidentStatus enum with value 'TRIAGED'
    - Test (unit): IncidentStatus enum cases are ordered: Pending, Triaged, Dispatched, ...
    - Test: Gate 'triage-incidents' grants operator, supervisor, admin; denies dispatcher, responder
    - Test: Gate 'manual-entry' grants operator, supervisor, admin; denies dispatcher, responder
    - Test: Gate 'submit-dispatch' grants operator, supervisor, admin; denies dispatcher, responder
    - Test: Gate 'override-priority' grants supervisor, admin; denies operator, dispatcher, responder
    - Test: Gate 'recall-incident' grants supervisor, admin; denies operator, dispatcher, responder
    - Test: Gate 'view-session-log' grants supervisor, admin; denies operator, dispatcher, responder
    - Test: Operator is also granted 'create-incidents' gate (they create/update incidents during triage)
    - Test: User::factory()->operator() creates user with operator role
    - Test: Operator can subscribe to dispatch.incidents channel
  </behavior>
  <action>
    1. Add `case Operator = 'operator'` to UserRole enum (alphabetical: Admin, Dispatcher, Operator, Responder, Supervisor)
    2. Add `case Triaged = 'TRIAGED'` to IncidentStatus enum between Pending and Dispatched
    3. In AppServiceProvider::configureGates(), add Operator to the 'create-incidents' gate role array. Add 6 new gates:
       - 'triage-incidents': Operator, Supervisor, Admin
       - 'manual-entry': Operator, Supervisor, Admin
       - 'submit-dispatch': Operator, Supervisor, Admin
       - 'override-priority': Supervisor, Admin only
       - 'recall-incident': Supervisor, Admin only
       - 'view-session-log': Supervisor, Admin only
    4. In HandleInertiaRequests::share(), add 6 new gate permissions to the 'can' array: triage_incidents, manual_entry, submit_dispatch, override_priority, recall_incident, view_session_log. Also add 'operator' to the channelCounts role check.
    5. Add `operator()` factory state to UserFactory returning `['role' => UserRole::Operator]`
    6. Create OperatorUserSeeder with 3 demo users from design system: Santos M.L. (operator), Reyes J.A. (supervisor override), Admin (admin). Use php artisan make:seeder --no-interaction.
    7. Register OperatorUserSeeder in DatabaseSeeder
    8. In routes/channels.php, add UserRole::Operator to the $dispatchRoles array
    9. Update resources/js/types/auth.ts: add 'operator' to UserRole union, add 6 new permissions to UserPermissions type
    10. Update resources/js/types/incident.ts: add 'TRIAGED' to IncidentStatus union type
    11. Write tests FIRST (RED), then implement (GREEN). Tests use php artisan make:test --pest.
    12. Create tests/Unit/Enums/UserRoleTest.php: assert Operator case exists with value 'operator', assert exactly 5 cases
    13. Create tests/Unit/Enums/IncidentStatusTest.php: assert Triaged case exists with value 'TRIAGED', assert ordering (Triaged comes after Pending, before Dispatched)

    NOTE on scope: This task touches 13 files. The seeder + factory changes (steps 5-7) are mechanical, and the TypeScript type updates (steps 9-10) are one-line additions. If context pressure is felt, the seeder can be deferred to after the gate tests pass.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Unit/Enums/UserRoleTest.php tests/Unit/Enums/IncidentStatusTest.php tests/Feature/Intake/IntakeGatesTest.php -x</automated>
  </verify>
  <done>
    Operator enum case exists, TRIAGED status exists, 6 new gates defined with correct role matrix, factory state works, channel auth updated, TypeScript types updated, all unit and gate tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IntakeStationController (show, triage, storeAndTriage), requests, routes, Fortify redirect, and tests</name>
  <files>
    app/Http/Controllers/IntakeStationController.php
    app/Http/Requests/TriageIncidentRequest.php
    app/Http/Requests/ManualEntryRequest.php
    app/Providers/FortifyServiceProvider.php
    routes/web.php
    resources/js/pages/intake/IntakeStation.vue
    tests/Feature/Intake/IntakeStationTest.php
    tests/Feature/Intake/TriageIncidentTest.php
    tests/Feature/Auth/OperatorRedirectTest.php
  </files>
  <behavior>
    - Test: GET /intake renders IntakeStation for operator (200)
    - Test: GET /intake renders IntakeStation for supervisor (200)
    - Test: GET /intake renders IntakeStation for admin (200)
    - Test: GET /intake returns 403 for dispatcher
    - Test: GET /intake returns 403 for responder
    - Test: GET /intake redirects unauthenticated to login
    - Test: IntakeStation page receives incidentTypes, channels, priorities, pendingIncidents, triagedIncidents props
    - Test: POST /intake/{incident}/triage with valid data transitions PENDING to TRIAGED
    - Test: POST /intake/{incident}/triage creates timeline entry with event_type 'incident_triaged'
    - Test: POST /intake/{incident}/triage fires IncidentStatusChanged event
    - Test: POST /intake/{incident}/triage validates required fields (incident_type_id, priority, location_text)
    - Test: POST /intake/{incident}/triage returns 403 for responder
    - Test: POST /intake/manual with valid data creates a new incident directly as TRIAGED
    - Test: POST /intake/manual creates timeline entries for both 'incident_created' and 'incident_triaged'
    - Test: POST /intake/manual fires IncidentCreated and IncidentStatusChanged events
    - Test: POST /intake/manual validates required fields (incident_type_id, priority, location_text, channel)
    - Test: POST /intake/manual returns 403 for responder
    - Test: Operator login redirects to /intake (not /dashboard)
    - Test: Dispatcher login redirects to /dashboard (unchanged)
  </behavior>
  <action>
    1. Create IntakeStationController with php artisan make:controller --no-interaction:
       - show(): Returns Inertia::render('intake/IntakeStation') with props: incidentTypes (grouped by category), channels (IncidentChannel::cases()), priorities (IncidentPriority::cases()), pendingIncidents (PENDING, ordered by priority then FIFO), triagedIncidents (TRIAGED, ordered by priority then FIFO), priorityConfig (config('priority'))
       - triage(TriageIncidentRequest, Incident): Transitions incident status from PENDING to TRIAGED, updates incident fields (incident_type_id, priority, location_text, caller_name, caller_contact, notes, coordinates, barangay_id), creates 'incident_triaged' timeline entry, dispatches IncidentStatusChanged event, returns back()
       - storeAndTriage(ManualEntryRequest): Creates a brand-new Incident with status TRIAGED directly (no intermediate PENDING step). Generates incident_no via the existing INC-YYYY-NNNNN pattern. Sets all triage fields (incident_type_id, priority, location_text, caller_name, caller_contact, notes, channel, coordinates, barangay_id). Creates both 'incident_created' and 'incident_triaged' timeline entries. Dispatches both IncidentCreated and IncidentStatusChanged events. Returns back(). Gate check: 'manual-entry'.
    2. Create TriageIncidentRequest with php artisan make:request --no-interaction:
       - authorize(): Gate::allows('triage-incidents')
       - rules(): incident_type_id (required, exists), priority (required, in P1-P4), location_text (required, string, max:500), caller_name (nullable, string, max:255), caller_contact (nullable, string, max:100), notes (nullable, string), latitude (nullable, numeric), longitude (nullable, numeric), barangay_id (nullable, exists)
       - Use array-style rules per project convention
    3. Create ManualEntryRequest with php artisan make:request --no-interaction:
       - authorize(): Gate::allows('manual-entry')
       - rules(): Same as TriageIncidentRequest PLUS channel (required, in IncidentChannel enum values) since manual entries have no pre-existing incident to inherit channel from
       - Use array-style rules per project convention
    4. Add routes in routes/web.php within auth+verified group:
       - New middleware group for role:operator,supervisor,admin
       - GET /intake -> IntakeStationController::show, name: intake.station
       - POST /intake/{incident}/triage -> IntakeStationController::triage, name: intake.triage
       - POST /intake/manual -> IntakeStationController::storeAndTriage, name: intake.store-and-triage
       - Also expose priority suggestion and geocoding search APIs for operator role
    5. In FortifyServiceProvider::boot(), add authenticatedRedirectUsing callback:
       - If user role is Operator, return route('intake.station')
       - Otherwise return config('fortify.home')
    6. Create a minimal stub IntakeStation.vue page (single root div with "Intake Station" text) so Inertia page existence checks pass. Full UI comes in Plans 02-04.
    7. Write tests FIRST (RED), then implement (GREEN). Include manual entry tests in TriageIncidentTest.php.
  </action>
  <verify>
    <automated>php artisan test --compact tests/Feature/Intake/IntakeStationTest.php tests/Feature/Intake/TriageIncidentTest.php tests/Feature/Auth/OperatorRedirectTest.php -x</automated>
  </verify>
  <done>
    Intake station route accessible for operator/supervisor/admin, forbidden for dispatcher/responder. Triage action transitions PENDING to TRIAGED with timeline and event. Manual entry (storeAndTriage) creates a new TRIAGED incident in a single request with both timeline entries and events. Operator login redirects to intake station. All tests pass, full existing suite unbroken.
  </done>
</task>

</tasks>

<verification>
```bash
# All new tests pass (unit + feature)
php artisan test --compact tests/Unit/Enums/ tests/Feature/Intake/ tests/Feature/Auth/OperatorRedirectTest.php -x

# Full suite still green (no regressions)
php artisan test --compact

# PHP formatting
vendor/bin/pint --dirty --format agent

# TypeScript types valid
npm run types:check
```
</verification>

<success_criteria>
- Operator role and TRIAGED status exist in PHP enums and TypeScript types
- 6 new intake gates with correct role matrix
- IntakeStationController serves intake page, processes triage action, and handles manual entry
- Manual entry endpoint (POST /intake/manual) creates + triages in one request
- Operator redirected to /intake after login; other roles to /dashboard
- WebSocket channel auth allows operator on dispatch.incidents
- All new Pest tests pass (unit enum tests + feature tests), existing suite unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/08-implement-operator-role-and-intake-layer-ui/08-01-SUMMARY.md`
</output>
