---
phase: 10-update-all-pages-design-to-match-irms-intake-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/app.css
  - resources/js/layouts/AuthLayout.vue
  - resources/js/layouts/auth/AuthSimpleLayout.vue
  - resources/js/layouts/auth/AuthCardLayout.vue
  - resources/js/layouts/auth/AuthSplitLayout.vue
  - resources/js/pages/auth/Login.vue
  - resources/js/pages/auth/Register.vue
  - resources/js/pages/auth/ForgotPassword.vue
  - resources/js/pages/auth/ResetPassword.vue
  - resources/js/pages/auth/TwoFactorChallenge.vue
  - resources/js/pages/auth/VerifyEmail.vue
  - resources/js/pages/auth/ConfirmPassword.vue
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-04

must_haves:
  truths:
    - "Shadcn CSS variables (--background, --foreground, --border, --card, --primary, etc.) reference design system tokens (--t-bg, --t-text, --t-border, etc.) instead of hardcoded HSL values"
    - "Dark mode variables in .dark block also reference design system dark token values"
    - "All auth pages render with CDRRMO branding (icon, 'CDRRMO Butuan City', subtitle), centered card layout, Level 4 shadow, 14px border-radius"
    - "The three old auth layout variants (AuthSimpleLayout, AuthCardLayout, AuthSplitLayout) are deleted"
    - "All Shadcn components throughout the app inherit design system colors via CSS variable cascade"
    - "Focus ring on all [data-slot] components uses combined border-color + box-shadow pattern (DS-03) instead of Shadcn outline-ring/50"
  artifacts:
    - path: "resources/css/app.css"
      provides: "CSS variable remapping from Shadcn to design system tokens"
      contains: "--background: var(--t-bg)"
    - path: "resources/js/layouts/AuthLayout.vue"
      provides: "Rebuilt auth layout with CDRRMO branding and design system styling"
      contains: "CDRRMO Butuan City"
  key_links:
    - from: "resources/css/app.css"
      to: "all Shadcn components"
      via: "CSS variable inheritance"
      pattern: "--background: var\\(--t-bg\\)"
    - from: "resources/js/layouts/AuthLayout.vue"
      to: "resources/js/pages/auth/*.vue"
      via: "component import"
      pattern: "AuthLayout"
---

<objective>
Remap Shadcn CSS variables to IRMS design system tokens and consolidate auth layouts into a single CDRRMO-branded layout.

Purpose: This is the highest-leverage change in the entire phase. Remapping ~30 CSS variables in one file cascades design system styling to ALL Shadcn components app-wide. Auth layout consolidation removes 3 redundant layout files and establishes the branded auth experience.

Output: Updated app.css with design system token references, rebuilt AuthLayout.vue, 3 deleted layout variants, 7 updated auth pages.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-CONTEXT.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-RESEARCH.md
@docs/IRMS-Intake-Design-System.md
@resources/css/app.css
@resources/js/layouts/AuthLayout.vue
@resources/js/layouts/auth/AuthSimpleLayout.vue

<interfaces>
<!-- Current Shadcn CSS variable system in app.css :root block -->
<!-- These HSL values will be remapped to design system token references -->
:root {
    --background: hsl(0 0% 100%);     /* -> var(--t-bg) */
    --foreground: hsl(0 0% 3.9%);     /* -> var(--t-text) */
    --border: hsl(0 0% 92.8%);        /* -> var(--t-border) */
    --primary: hsl(0 0% 9%);          /* -> var(--t-brand) */
    --card: hsl(0 0% 100%);           /* -> var(--t-surface) */
    /* ...etc */
}

<!-- Design system tokens already defined in app.css -->
:root {
    --t-bg: #f4f6f9;
    --t-surface: #ffffff;
    --t-text: #0f172a;
    --t-text-mid: #334155;
    --t-text-dim: #64748b;
    --t-text-faint: #94a3b8;
    --t-border: #e2e8f0;
    --t-brand: #1e3a6e;
    --t-accent: #2563eb;
    --t-p1: #dc2626;
    /* ...etc */
}

<!-- Auth pages all import from @/layouts/AuthLayout.vue -->
<!-- AuthLayout.vue currently delegates to AuthSimpleLayout.vue -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap Shadcn CSS variables to design system tokens</name>
  <files>resources/css/app.css</files>
  <action>
In `resources/css/app.css`, update the `:root` block to remap ALL Shadcn CSS variables to design system token references. The design system tokens (--t-*) remain unchanged as authoritative hex values. Only the Shadcn variables change to point TO the design system tokens.

**:root block remapping (replace HSL values with var() references):**
- `--background: var(--t-bg);` (was hsl(0 0% 100%))
- `--foreground: var(--t-text);` (was hsl(0 0% 3.9%))
- `--card: var(--t-surface);`
- `--card-foreground: var(--t-text);`
- `--popover: var(--t-surface);`
- `--popover-foreground: var(--t-text);`
- `--primary: var(--t-brand);` (was hsl(0 0% 9%) -- design system brand color #1e3a6e)
- `--primary-foreground: #ffffff;`
- `--secondary: var(--t-surface-alt);`
- `--secondary-foreground: var(--t-text-mid);`
- `--muted: var(--t-surface-alt);`
- `--muted-foreground: var(--t-text-dim);`
- `--accent: var(--t-surface-alt);`
- `--accent-foreground: var(--t-text);`
- `--destructive: var(--t-p1);` (red)
- `--destructive-foreground: #ffffff;`
- `--border: var(--t-border);`
- `--input: var(--t-border);`
- `--ring: var(--t-accent);` (blue focus ring)
- `--radius: 0.4375rem;` (7px -- design system default)
- Sidebar variables: `--sidebar-background: var(--t-surface);`, `--sidebar-foreground: var(--t-text-mid);`, `--sidebar-primary: var(--t-brand);`, `--sidebar-primary-foreground: #ffffff;`, `--sidebar-accent: color-mix(in srgb, var(--t-accent) 8%, transparent);`, `--sidebar-accent-foreground: var(--t-accent);`, `--sidebar-border: var(--t-border);`, `--sidebar-ring: var(--t-accent);`, `--sidebar: var(--t-surface);`
- Chart variables: `--chart-1: var(--t-accent);`, `--chart-2: var(--t-online);`, `--chart-3: var(--t-p2);`, `--chart-4: var(--t-p3);`, `--chart-5: var(--t-ch-iot);`

**CRITICAL: ONE-DIRECTION ONLY.** Shadcn variables point TO design system tokens. Design system tokens remain as hex values. Never reverse (that creates circular references).

**.dark block remapping (same pattern, using dark token values):**
- `--background: var(--t-bg);` (dark: #0f172a)
- `--foreground: var(--t-text);` (dark: #f8fafc)
- `--card: var(--t-surface);` (dark: #1e293b)
- `--card-foreground: var(--t-text);`
- `--popover: var(--t-surface);`
- `--popover-foreground: var(--t-text);`
- `--primary: #e2e8f0;` (light text on dark for primary buttons)
- `--primary-foreground: var(--t-bg);`
- `--secondary: var(--t-surface-alt);` (dark: #334155)
- `--secondary-foreground: var(--t-text-mid);` (dark: #cbd5e1)
- `--muted: var(--t-surface-alt);`
- `--muted-foreground: var(--t-text-dim);` (dark: #94a3b8)
- `--accent: var(--t-surface-alt);`
- `--accent-foreground: var(--t-text);`
- `--destructive: var(--t-p1);`
- `--destructive-foreground: #ffffff;`
- `--border: var(--t-border);` (dark: #334155)
- `--input: var(--t-border);`
- `--ring: var(--t-accent);` (dark: uses --t-border-foc #3b82f6)
- Sidebar dark: `--sidebar-background: var(--t-surface);`, `--sidebar-foreground: var(--t-text-mid);`, `--sidebar-primary: #e2e8f0;`, `--sidebar-primary-foreground: var(--t-bg);`, `--sidebar-accent: color-mix(in srgb, var(--t-accent) 12%, transparent);`, `--sidebar-accent-foreground: var(--t-accent);`, `--sidebar-border: var(--t-border);`, `--sidebar-ring: var(--t-accent);`, `--sidebar: var(--t-surface);`
- Chart dark: `--chart-1: #3b82f6;`, `--chart-2: #22c55e;`, `--chart-3: #f97316;`, `--chart-4: #a855f7;`, `--chart-5: #ef4444;`

**Update @layer base block** to use design system border color:
```css
@layer base {
    *,
    ::after,
    ::before,
    ::backdrop,
    ::file-selector-button {
        border-color: var(--color-border, currentColor);
    }
}
```

**Replace the second `@layer base` block** with design system focus ring override (DS-03). Remove Shadcn's default `outline-ring/50` and add the required combined border-color + box-shadow focus pattern per user decision:
```css
@layer base {
    * {
        @apply border-border;
    }
    body {
        @apply bg-background text-foreground;
    }

    /* DS-03: Design system focus ring override
       Replaces Shadcn's outline-ring/50 with the locked
       border-color + box-shadow pattern from CONTEXT.md */
    [data-slot]:focus-visible {
        outline: none;
        border-color: #2563eb;
        box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.10);
    }
}
```

**Important:** The old `outline-ring/50` on `*` is removed. The `[data-slot]` selector targets all Reka UI / Shadcn components (which use `data-slot` attributes) and applies the design system's combined border-color change + box-shadow focus ring. This matches the locked decision in CONTEXT.md: "border-color: #2563eb + box-shadow: 0 0 0 3px rgba(37,99,235,.10)".

**Add fadeUp keyframe animation** for auth pages (after the p1-flash animation):
```css
@keyframes fadeUp {
    from {
        opacity: 0;
        transform: translateY(12px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}
```

**Add shadow scale CSS custom properties** in :root (for use across the app):
```css
--shadow-1: 0 1px 3px rgba(0,0,0,.04);
--shadow-2: 0 1px 4px rgba(0,0,0,.06);
--shadow-3: 0 2px 8px rgba(0,0,0,.06);
--shadow-4: 0 4px 24px rgba(0,0,0,.08);
--shadow-5: 0 8px 24px rgba(0,0,0,.12);
```

Do NOT modify the `@theme inline` block or the design system token definitions (--t-* values). Only modify the Shadcn variable mappings and add the new utilities.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All Shadcn CSS variables in both :root and .dark blocks reference design system tokens. DS-03 focus ring override implemented via [data-slot]:focus-visible with combined border-color (#2563eb) + box-shadow (0 0 0 3px rgba(37,99,235,.10)) pattern -- Shadcn's outline-ring/50 removed. Shadow scale and fadeUp animation defined. Frontend builds successfully. No circular CSS variable references.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate auth layouts and restyle auth pages</name>
  <files>resources/js/layouts/AuthLayout.vue, resources/js/layouts/auth/AuthSimpleLayout.vue, resources/js/layouts/auth/AuthCardLayout.vue, resources/js/layouts/auth/AuthSplitLayout.vue, resources/js/pages/auth/Login.vue, resources/js/pages/auth/Register.vue, resources/js/pages/auth/ForgotPassword.vue, resources/js/pages/auth/ResetPassword.vue, resources/js/pages/auth/TwoFactorChallenge.vue, resources/js/pages/auth/VerifyEmail.vue, resources/js/pages/auth/ConfirmPassword.vue</files>
  <action>
**1. Rebuild AuthLayout.vue** as a single self-contained layout with CDRRMO branding. No longer delegates to AuthSimpleLayout. Structure:

```vue
<script setup lang="ts">
defineProps<{
    title?: string;
    description?: string;
}>();
</script>

<template>
    <div class="flex min-h-svh items-center justify-center bg-background p-6">
        <div
            class="w-full max-w-md rounded-[14px] border border-border bg-card p-10 shadow-[var(--shadow-4)]"
            style="animation: fadeUp 400ms ease"
        >
            <!-- CDRRMO branding -->
            <div class="mb-8 flex flex-col items-center gap-2">
                <div
                    class="flex size-[52px] items-center justify-center rounded-[10px]"
                    style="background: color-mix(in srgb, var(--t-brand) 10%, transparent)"
                >
                    <!-- Shield/crest SVG icon for CDRRMO -->
                    <svg class="size-7 text-t-brand" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" />
                        <path d="M12 8v4" />
                        <path d="M12 16h.01" />
                    </svg>
                </div>
                <span class="text-base font-semibold text-t-brand">
                    CDRRMO Butuan City
                </span>
                <span class="text-xs text-muted-foreground">
                    IRMS &mdash; Incident Response Management System
                </span>
            </div>

            <!-- Page title -->
            <div class="mb-6 text-center">
                <h1 class="text-lg font-semibold text-foreground">{{ title }}</h1>
                <p v-if="description" class="mt-1 text-sm text-muted-foreground">
                    {{ description }}
                </p>
            </div>

            <slot />
        </div>
    </div>
</template>
```

Note: Uses `bg-background`, `bg-card`, `text-foreground`, `text-muted-foreground`, `border-border` which NOW resolve to design system tokens thanks to Task 1's CSS variable remapping. The shadow uses `var(--shadow-4)` from the shadow scale.

**2. Delete the three old auth layout variants:**
- `resources/js/layouts/auth/AuthSimpleLayout.vue` -- DELETE
- `resources/js/layouts/auth/AuthCardLayout.vue` -- DELETE
- `resources/js/layouts/auth/AuthSplitLayout.vue` -- DELETE

Before deleting, verify no files outside of AuthLayout.vue import them. Based on codebase grep, only AuthLayout.vue imports AuthSimpleLayout.vue, and no file imports the other two.

**3. Update auth pages for design system consistency.** Most auth pages already use `<AuthLayout>` (or `AuthBase` alias) as their wrapper and Shadcn components (Input, Button, Label, etc.) that will automatically inherit design system styling from the CSS variable remapping. The main changes per page are:

For ALL 7 auth pages (Login, Register, ForgotPassword, ResetPassword, TwoFactorChallenge, VerifyEmail, ConfirmPassword):
- Ensure import is `import AuthLayout from '@/layouts/AuthLayout.vue';` (some use `AuthBase` alias -- standardize the alias name but both work)
- Replace any hardcoded `text-green-600` status messages with `text-t-online` (design system green)
- Replace any `text-muted-foreground` that should be `text-t-text-dim` -- actually since --muted-foreground now maps to --t-text-dim, these are equivalent. No change needed.
- Verify `flex-col gap-6` form spacing is consistent across all pages

The Login.vue page specifically: The form structure is already clean. The `text-green-600` status message should use `text-t-online` for design system consistency.

Run `npm run lint` and `npm run format` after all changes to ensure code style compliance.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5 && php artisan test --compact tests/Feature/Auth/ 2>&1 | tail -10</automated>
  </verify>
  <done>AuthLayout.vue rebuilt with CDRRMO branding (52x52 icon, "CDRRMO Butuan City", subtitle), centered card, Level 4 shadow, 14px border-radius, fadeUp animation. Three old auth layout variants deleted. All 7 auth pages render correctly with design system styling. All auth tests pass. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (no CSS compilation errors, no missing imports)
2. `php artisan test --compact tests/Feature/Auth/` -- all auth tests pass
3. `npm run types:check` -- TypeScript compiles cleanly
4. No circular CSS variable references (verify in browser dev tools: elements have correct background colors, not transparent)
5. All Shadcn components throughout the app visually inherit design system colors
6. Focus ring on [data-slot] elements uses border-color #2563eb + box-shadow (not outline-ring/50)
</verification>

<success_criteria>
- app.css :root and .dark blocks use design system token references for ALL Shadcn variables
- DS-03 focus ring override: [data-slot]:focus-visible uses border-color #2563eb + box-shadow 0 0 0 3px rgba(37,99,235,.10)
- Shadow scale (5 levels) defined as CSS custom properties
- fadeUp keyframe animation defined
- AuthLayout.vue is self-contained with CDRRMO branding, no delegation
- AuthSimpleLayout.vue, AuthCardLayout.vue, AuthSplitLayout.vue deleted
- All 7 auth pages render with new branded layout
- `npm run build` passes
- `php artisan test --compact tests/Feature/Auth/` passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-01-SUMMARY.md`
</output>
