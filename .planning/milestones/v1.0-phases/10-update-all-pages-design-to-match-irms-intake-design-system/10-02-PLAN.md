---
phase: 10-update-all-pages-design-to-match-irms-intake-design-system
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - resources/js/components/AppLogo.vue
  - resources/js/components/AppSidebar.vue
  - resources/js/components/AppSidebarHeader.vue
  - resources/js/components/NavMain.vue
  - resources/js/components/NavUser.vue
  - resources/js/components/Heading.vue
  - resources/js/layouts/app/AppSidebarLayout.vue
  - resources/js/layouts/settings/Layout.vue
  - resources/js/pages/settings/Profile.vue
  - resources/js/pages/settings/Password.vue
  - resources/js/pages/settings/TwoFactor.vue
  - resources/js/pages/settings/Appearance.vue
  - resources/js/pages/Dashboard.vue
autonomous: true
requirements:
  - DS-05
  - DS-06
  - DS-07

must_haves:
  truths:
    - "Sidebar shows CDRRMO icon + 'IRMS' text in header instead of 'Laravel Starter Kit'"
    - "Sidebar navigation section labels use Space Mono uppercase 9px styling"
    - "Content area behind sidebar pages has t-bg (#f4f6f9 light / #0f172a dark) background"
    - "Dashboard cards use design system tokens (no more neutral-* classes)"
    - "Settings pages use design system tokens for navigation and form areas"
    - "Heading component uses design system typography tokens"
  artifacts:
    - path: "resources/js/components/AppLogo.vue"
      provides: "CDRRMO icon + IRMS branding"
      contains: "IRMS"
    - path: "resources/js/components/NavMain.vue"
      provides: "Space Mono section labels"
      contains: "font-mono"
    - path: "resources/js/layouts/app/AppSidebarLayout.vue"
      provides: "bg-t-bg content area wrapper"
      contains: "bg-t-bg"
    - path: "resources/js/pages/Dashboard.vue"
      provides: "Dashboard with design system tokens"
      min_lines: 50
  key_links:
    - from: "resources/js/components/AppLogo.vue"
      to: "resources/js/components/AppSidebar.vue"
      via: "component import"
      pattern: "AppLogo"
    - from: "resources/js/layouts/app/AppSidebarLayout.vue"
      to: "all sidebar pages"
      via: "layout wrapper"
      pattern: "bg-t-bg"
---

<objective>
Restyle sidebar navigation, shared components, settings pages, and dashboard with design system tokens.

Purpose: The sidebar layout wraps ALL authenticated pages (except intake/dispatch/responder). Restyling it and its sub-components (AppLogo, NavMain, NavUser, AppSidebarHeader, Heading) propagates the design system look to every sidebar-based page. The Dashboard and Settings pages are then individually updated to complete the authenticated experience.

Output: Updated sidebar components with CDRRMO branding and Space Mono labels, bg-t-bg content area, restyled Dashboard and Settings pages.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-CONTEXT.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-RESEARCH.md
@.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-01-SUMMARY.md
@docs/IRMS-Intake-Design-System.md
@resources/css/app.css
@resources/js/components/AppLogo.vue
@resources/js/components/AppSidebar.vue
@resources/js/components/AppSidebarHeader.vue
@resources/js/components/NavMain.vue
@resources/js/components/NavUser.vue
@resources/js/components/Heading.vue
@resources/js/layouts/app/AppSidebarLayout.vue
@resources/js/layouts/settings/Layout.vue
@resources/js/pages/Dashboard.vue

<interfaces>
<!-- AppSidebar.vue uses these components -->
import AppLogo from '@/components/AppLogo.vue';
import NavMain from '@/components/NavMain.vue';
import NavUser from '@/components/NavUser.vue';

<!-- AppSidebarLayout.vue wraps slot content -->
<AppShell variant="sidebar">
    <AppSidebar />
    <AppContent variant="sidebar">
        <AppSidebarHeader :breadcrumbs="breadcrumbs" />
        <ConnectionBanner />
        <slot />
    </AppContent>
</AppShell>

<!-- NavMain receives items and renders SidebarGroupLabel -->
<SidebarGroupLabel>Platform</SidebarGroupLabel>

<!-- Heading component props -->
type Props = { title: string; description?: string; variant?: 'default' | 'small'; };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle sidebar components and layout shell</name>
  <files>resources/js/components/AppLogo.vue, resources/js/components/AppSidebar.vue, resources/js/components/AppSidebarHeader.vue, resources/js/components/NavMain.vue, resources/js/components/NavUser.vue, resources/js/components/Heading.vue, resources/js/layouts/app/AppSidebarLayout.vue</files>
  <action>
**1. AppLogo.vue** -- Replace "Laravel Starter Kit" with CDRRMO branding:
- Replace the icon container: use a rounded-[8px] container with `bg-t-brand` background
- Inside, use a shield SVG icon (same as auth layout) in white (`text-white size-5`)
- Replace "Laravel Starter Kit" text with "IRMS" in DM Sans 600 weight
- Keep the existing flex layout structure for sidebar menu button compatibility

**2. NavMain.vue** -- Add Space Mono section labels:
- The `<SidebarGroupLabel>Platform</SidebarGroupLabel>` should have Space Mono uppercase 9px styling
- Add class to SidebarGroupLabel: `class="font-mono text-[9px] font-bold tracking-[2px] text-t-text-faint uppercase"`
- The sidebar menu items themselves use the existing SidebarMenuButton which inherits CSS variables from Plan 01's remapping -- no changes needed for active/hover states

**3. NavUser.vue** -- Minimal changes needed since it uses Shadcn sidebar components that inherit CSS variables. Verify the user name/email text uses appropriate design system text colors. If it uses hardcoded colors, replace with `text-foreground` and `text-muted-foreground` (which now map to design system tokens).

**4. AppSidebarHeader.vue** -- Update the header bar:
- Replace `border-sidebar-border/70` with `border-border` (which now maps to design system --t-border)
- Ensure the breadcrumbs and sidebar trigger use design system colors (inherited from CSS variables)

**5. Heading.vue** -- Update typography to use design system tokens:
- The `text-xl font-semibold tracking-tight` for default variant should use `text-foreground` explicitly
- The description `text-muted-foreground` already maps to design system tokens via Plan 01 -- verify it works
- No structural changes needed

**6. AppSidebarLayout.vue** -- Add bg-t-bg background to content area:
- The `<AppContent>` wrapper or the slot container needs `bg-t-bg` class to create the visual depth between sidebar (t-surface) and content area (t-bg)
- Wrap the `<slot />` in a container div with `class="flex-1 bg-t-bg"` if not already present, or add `bg-t-bg` to the AppContent component
- Verify that the sidebar renders with `bg-sidebar` (which now maps to t-surface via CSS variables)

**7. AppSidebar.vue** -- The sidebar already uses `variant="inset"` and CSS variables. After Plan 01's remapping, `--sidebar-background` maps to `--t-surface`. Verify this renders correctly. No structural changes needed. The only potential change is if the `variant="inset"` creates visual conflicts with the bg-t-bg content area -- if so, consider switching to `variant="sidebar"` instead.

Run `npm run lint` and `npm run format` after all changes.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>AppLogo shows "IRMS" with CDRRMO icon. NavMain section labels use Space Mono uppercase. AppSidebarLayout content area has bg-t-bg background. Heading uses design system typography. All sidebar-based pages inherit the updated look. Frontend builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Dashboard and Settings pages</name>
  <files>resources/js/pages/Dashboard.vue, resources/js/layouts/settings/Layout.vue, resources/js/pages/settings/Profile.vue, resources/js/pages/settings/Password.vue, resources/js/pages/settings/TwoFactor.vue, resources/js/pages/settings/Appearance.vue</files>
  <action>
**1. Dashboard.vue** -- Replace ALL hardcoded neutral-* and sidebar-border classes with design system tokens:

Replace class patterns:
- `text-neutral-900 dark:text-neutral-100` -> `text-foreground` (maps to t-text)
- `text-neutral-500 dark:text-neutral-400` -> `text-muted-foreground` (maps to t-text-dim)
- `text-neutral-400 dark:text-neutral-500` -> `text-t-text-faint`
- `text-neutral-300 dark:text-neutral-600` -> `text-t-text-faint`
- `text-neutral-400 group-hover:text-neutral-600 dark:group-hover:text-neutral-300` -> `text-t-text-faint group-hover:text-t-text-mid`
- `border-sidebar-border/70 dark:border-sidebar-border` -> `border-border`
- `hover:bg-neutral-50 dark:hover:bg-neutral-800/50` -> `hover:bg-accent`
- `rounded-xl` -> `rounded-[var(--radius)]` or keep rounded-xl (within design system scale)

For dashboard cards (admin grid, dispatcher stats, supervisor stats, responder):
- Add `bg-card` background explicitly
- Add `shadow-[var(--shadow-1)]` for Level 1 elevation
- Use `transition-colors` on hover

**2. Settings Layout (Layout.vue)** -- Update with design system tokens:
- The outer container `px-4 py-6` is fine
- Heading component will inherit design system styling from Task 1
- Settings nav buttons: `bg-muted` (active state) already maps to t-surface-alt via CSS variables
- Add `bg-card rounded-[var(--radius)] border border-border p-6 shadow-[var(--shadow-1)]` wrapper around the settings form content area for card elevation
- Consider adding `bg-t-bg` to the settings page background for consistency with other sidebar pages (this comes from AppSidebarLayout in Task 1)

**3. Settings pages (Profile.vue, Password.vue, TwoFactor.vue, Appearance.vue):**
These pages are rendered inside the settings Layout.vue slot. They primarily use Shadcn components (Input, Button, Label, Select) which already inherit design system styling from Plan 01's CSS variable remapping.

Scan each file for hardcoded color classes:
- Replace any `text-neutral-*`, `bg-neutral-*`, `border-neutral-*` with design system equivalents
- Replace any `text-zinc-*`, `bg-zinc-*` with design system equivalents
- Replace `bg-muted/50` or similar with `bg-accent` or `bg-card`
- Ensure form sections have consistent spacing using design system 4px base unit (p-4, gap-4, etc.)
- Verify labels and descriptions use `text-foreground` and `text-muted-foreground`

For TwoFactor.vue specifically: The 2FA setup modal and recovery code display should use design system tokens. Verify `font-mono` is applied to recovery codes (Space Mono via CSS variable).

For Appearance.vue: The appearance/theme selector cards should use design system tokens for active/inactive states.

Run `npm run lint` and `npm run format` after all changes.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build 2>&1 | tail -5 && php artisan test --compact tests/Feature/Settings/ 2>&1 | tail -10 && php artisan test --compact tests/Feature/DashboardTest.php 2>&1 | tail -5</automated>
  </verify>
  <done>Dashboard uses design system tokens (no neutral-* or sidebar-border classes). Settings layout has card elevation with shadow. All 4 settings pages use design system tokens. All tests pass. Frontend builds cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `php artisan test --compact tests/Feature/Settings/` -- all settings tests pass
3. `php artisan test --compact tests/Feature/DashboardTest.php` -- dashboard test passes
4. Sidebar shows "IRMS" with CDRRMO icon (not "Laravel Starter Kit")
5. Content area has lighter bg-t-bg background vs sidebar bg-t-surface
6. Dashboard cards have design system elevation and typography
</verification>

<success_criteria>
- AppLogo shows CDRRMO icon + "IRMS" text
- NavMain section labels use Space Mono uppercase 9px with tracking-[2px]
- AppSidebarLayout content area has bg-t-bg background
- Dashboard has zero neutral-*/sidebar-border hardcoded classes
- Settings pages use design system tokens throughout
- Heading component renders with design system typography
- All tests pass, frontend builds
</success_criteria>

<output>
After completion, create `.planning/phases/10-update-all-pages-design-to-match-irms-intake-design-system/10-02-SUMMARY.md`
</output>
