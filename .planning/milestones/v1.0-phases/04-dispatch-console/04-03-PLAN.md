---
phase: 04-dispatch-console
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - resources/js/components/dispatch/DispatchQueuePanel.vue
  - resources/js/components/dispatch/QueueCard.vue
  - resources/js/components/dispatch/IncidentDetailPanel.vue
  - resources/js/components/dispatch/UnitStatusPanel.vue
  - resources/js/components/dispatch/UnitDetailPanel.vue
  - resources/js/components/dispatch/AssignmentChip.vue
  - resources/js/components/dispatch/AckTimerRing.vue
  - resources/js/components/dispatch/SlaProgressBar.vue
  - resources/js/components/dispatch/StatusPipeline.vue
  - resources/js/composables/useAckTimer.ts
  - resources/js/composables/useDispatchSession.ts
  - resources/js/pages/dispatch/Console.vue
autonomous: true
requirements: [DSPTCH-05, DSPTCH-06, DSPTCH-08, DSPTCH-10]

must_haves:
  truths:
    - "Left panel shows incident queue ordered by priority then FIFO with filter tabs"
    - "Clicking a queue card selects the incident and shows detail in right panel"
    - "Right panel defaults to unit status roster grouped by agency"
    - "Right panel switches to incident detail when incident selected"
    - "Incident detail shows SLA window progress bar with priority-based targets"
    - "Incident detail shows status progression pipeline with ADVANCE button"
    - "Available units ranked by proximity appear as dispatch chips with distance and ETA"
    - "One-click assign on chip assigns unit and moves it to assignees section"
    - "90-second ack timer counts down next to each assigned unit"
    - "Session metrics (including averageHandleTime) update reactively as incidents/units change"
  artifacts:
    - path: "resources/js/components/dispatch/IncidentDetailPanel.vue"
      provides: "Full incident detail with SLA, status pipeline, assignees, dispatch chips, timeline"
      min_lines: 100
    - path: "resources/js/components/dispatch/DispatchQueuePanel.vue"
      provides: "Left panel with filter tabs and priority-ordered queue cards"
      min_lines: 60
    - path: "resources/js/composables/useAckTimer.ts"
      provides: "90-second countdown timer with color state"
      min_lines: 30
    - path: "resources/js/composables/useDispatchSession.ts"
      provides: "Session metrics computed from incidents and units, including averageHandleTime"
      min_lines: 30
  key_links:
    - from: "resources/js/components/dispatch/IncidentDetailPanel.vue"
      to: "DispatchConsoleController@nearbyUnits"
      via: "fetch nearby units on incident select"
      pattern: "nearbyUnits"
    - from: "resources/js/components/dispatch/AssignmentChip.vue"
      to: "DispatchConsoleController@assignUnit"
      via: "Wayfinder action on click"
      pattern: "dispatch.assign"
    - from: "resources/js/composables/useAckTimer.ts"
      to: "resources/js/composables/useAlertSystem.ts"
      via: "playAckExpiredTone on timer expiry"
      pattern: "playAckExpiredTone"
---

<objective>
All dispatch console panel components: left incident queue, right contextual panels (incident detail, unit status, unit detail), assignment chips with proximity ranking, acknowledgement timer, SLA progress bar, status pipeline, and session metrics composable.

Purpose: The panels are the dispatcher's primary interaction surface. Without them, the map is just a visualization. Assignment workflow, incident detail, and metrics enable actual dispatch operations.
Output: 10 Vue components, 2 composables, Console.vue fully wired with panel interactions.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dispatch-console/04-CONTEXT.md
@.planning/phases/04-dispatch-console/04-RESEARCH.md
@.planning/phases/04-dispatch-console/04-01-SUMMARY.md
@.planning/phases/04-dispatch-console/04-02-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01 and 02 that this plan uses -->

From resources/js/types/dispatch.ts (created in Plan 02):
```typescript
export interface DispatchIncident {
    // Full incident with assigned_units array, incident_type, barangay
    assigned_units: Array<{ unit_id: string; callsign: string; assigned_at: string; acknowledged_at: string | null }>;
    // All lifecycle timestamps, coordinates, etc.
}
export interface DispatchUnit { id, callsign, type, agency, crew_capacity, status, coordinates }
export interface DispatchMetrics { totalIncidents, activeIncidents, criticalIncidents, unitsAvailable, unitsTotal, averageHandleTime: number | null }
export interface NearbyUnit { id, callsign, type, agency, crew_capacity, distance_km, eta_minutes }
export interface DispatchAgency { id, name, code, contact_phone, contact_email, radio_channel, incident_types }
```

From resources/js/composables/useDispatchMap.ts (created in Plan 02):
```typescript
export function useDispatchMap(containerId: string): {
    map, isLoaded,
    setIncidentData, setUnitData,
    updateUnitPosition, animateUnitTo,
    updateConnectionLines,
    flyToIncident, flyToUnit,
    switchStyle,
    onIncidentClick, onUnitClick, onDeselect
}
```

From resources/js/composables/useAlertSystem.ts (created in Plan 02):
```typescript
export function useAlertSystem(): {
    playPriorityTone, playAckExpiredTone, triggerP1Flash
}
```

From app/Http/Controllers/DispatchConsoleController.php (Plan 01):
```
// Wayfinder routes:
// GET /dispatch → show (props: incidents, units, agencies, metrics)
//   metrics includes: totalIncidents, activeIncidents, criticalIncidents, unitsAvailable, unitsTotal, averageHandleTime
// POST /dispatch/{incident}/assign → assignUnit (body: {unit_id})
// POST /dispatch/{incident}/unassign → unassignUnit (body: {unit_id})
// POST /dispatch/{incident}/advance-status → advanceStatus (body: {status})
// POST /dispatch/{incident}/mutual-aid → requestMutualAid (body: {agency_id, notes})
// GET /dispatch/{incident}/nearby-units → nearbyUnits (returns NearbyUnit[])
```

From resources/js/components/intake/PriBadge.vue:
```typescript
// Reusable priority badge component -- use directly for dispatch cards
defineProps<{ priority: IncidentPriority }>()
```

From resources/js/components/intake/IntakeTopbar.vue (reference for stat pills):
```
<!-- Pattern: stat pill with value + label, font-mono for data -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue panel, queue cards, useDispatchSession composable</name>
  <files>
    resources/js/components/dispatch/DispatchQueuePanel.vue,
    resources/js/components/dispatch/QueueCard.vue,
    resources/js/composables/useDispatchSession.ts,
    resources/js/pages/dispatch/Console.vue
  </files>
  <action>
    1. **useDispatchSession.ts** composable:
       - Accept initial incidents (DispatchIncident[]) and units (DispatchUnit[]) as reactive refs, plus initialAverageHandleTime (number | null) from the server-side metrics prop
       - Compute metrics reactively: activeIncidents (DISPATCHED + EN_ROUTE + ON_SCENE count), criticalIncidents (P1 among active), totalIncidents (all non-resolved), unitsAvailable (AVAILABLE count), unitsTotal (non-OFFLINE count)
       - **averageHandleTime**: Initialize from the server-provided value (initialAverageHandleTime). When an incident is resolved client-side (status changes to RESOLVED via WebSocket), recompute by tracking resolved incidents in a local array: push { created_at, resolved_at: now() }, then average all entries. If no resolved incidents yet and no server value, return null. This ensures the metric stays current between page loads.
       - Expose computed `metrics: DispatchMetrics` (including averageHandleTime) and individual computed values
       - This replaces the server-side metrics from Plan 01 props with reactive client-side computation (keeps in sync with WebSocket mutations)

    2. **QueueCard.vue**: Individual incident card in the queue:
       - Left border colored by priority (4px, use priority color tokens)
       - Content: incident_no (font-mono, small), incident_type name, location_text (truncated), elapsed time since created_at (relative, font-mono), status badge, assigned unit callsign if any
       - PriBadge reused from intake for priority indicator
       - Selected state: bg-t-brand/10 highlight, border-l-4 brighter
       - Compact layout -- these stack in a 320px panel, keep height ~72px
       - Emit 'select' with incident id on click

    3. **DispatchQueuePanel.vue**: Left panel (320px width):
       - Header: "QUEUE" label with incident count badge
       - Filter tabs: ALL, P1, P1-2 (P1+P2), ACTIVE (DISPATCHED + EN_ROUTE + ON_SCENE). Use tabs similar to intake ChannelFeed filter pattern.
       - Incident list: QueueCard for each incident, filtered by active tab, sorted by priority rank then created_at
       - Scrollable overflow-y-auto for long lists
       - Show incidents with status: TRIAGED, DISPATCHED, EN_ROUTE, ON_SCENE (not PENDING -- those are in intake; not RESOLVED -- closed)
       - Accept incidents array and selectedIncidentId as props
       - Emit 'select-incident' with incident id

    4. **Wire into Console.vue**:
       - Initialize useDispatchSession with reactive copies of incidents and units props, plus props.metrics.averageHandleTime as the initial server value (following the established local copy pattern from Phase 3)
       - Place DispatchQueuePanel in the left panel slot
       - Wire queue card selection to update selectedIncidentId ref
       - On select: call flyToIncident from useDispatchMap
       - Pass computed metrics from useDispatchSession to DispatchLayout

    Run `npm run lint` and `npm run format`.
  </action>
  <verify>
    <automated>npm run types:check && npm run build</automated>
  </verify>
  <done>Left queue panel renders with filter tabs, queue cards show priority-colored borders and incident info, selecting a card updates selectedIncidentId and triggers flyTo, session metrics compute reactively from incident/unit data including averageHandleTime</done>
</task>

<task type="auto">
  <name>Task 2: Right panels (incident detail, unit status, unit detail), assignment chips, ack timer, SLA bar, status pipeline</name>
  <files>
    resources/js/components/dispatch/IncidentDetailPanel.vue,
    resources/js/components/dispatch/UnitStatusPanel.vue,
    resources/js/components/dispatch/UnitDetailPanel.vue,
    resources/js/components/dispatch/AssignmentChip.vue,
    resources/js/components/dispatch/AckTimerRing.vue,
    resources/js/components/dispatch/SlaProgressBar.vue,
    resources/js/components/dispatch/StatusPipeline.vue,
    resources/js/composables/useAckTimer.ts,
    resources/js/pages/dispatch/Console.vue
  </files>
  <action>
    1. **useAckTimer.ts** composable:
       - Accept `assignedAt: string` (ISO timestamp) as parameter
       - 90-second countdown from assigned_at
       - Return: `{ remainingSeconds, isExpired, colorClass }` where colorClass is 'text-green-500' when > 30s, 'text-red-500' when <= 30s
       - Use `useIntervalFn` from @vueuse/core (already installed) for 1-second ticks
       - On expiry: emit callback (parent will call playAckExpiredTone)
       - If acknowledged (acknowledged_at provided), stop timer and show checkmark
       - Display format: "M:SS" (e.g., "1:12", "0:30")

    2. **AckTimerRing.vue**: Visual timer display:
       - Text countdown "1:12" in font-mono
       - Color transitions: green > 30s, red <= 30s
       - If expired: show warning icon + "EXPIRED" text in red
       - If acknowledged: show check icon + "ACK" text in green
       - Compact size for inline display next to unit in assignees section

    3. **SlaProgressBar.vue**: SLA window progress bar:
       - Accept: priority (for target), created_at (start time)
       - SLA targets per CONTEXT.md: P1 = 5min, P2 = 10min, P3 = 20min, P4 = 30min
       - Calculate elapsed since created_at as percentage of target
       - Display: horizontal bar, colored fill (green < 50%, amber 50-80%, red > 80%), percentage text, "SLA WINDOW" label, remaining time
       - Auto-updates every second

    4. **StatusPipeline.vue**: Status progression visualization:
       - Show 5 stages: REPORTED, DISPATCHED, EN ROUTE, ON SCENE, RESOLVED
       - Active stage highlighted, completed stages filled, future stages dimmed
       - Connected by lines/arrows between stages
       - Map TRIAGED to REPORTED display (TRIAGED is an intake concept, dispatch shows as "reported")
       - "ADVANCE" button below showing next valid status. On click: call Wayfinder action for dispatch.advance-status with the next status value. Use `router.post()` pattern.
       - Only show ADVANCE button for statuses the dispatcher can advance (DISPATCHED onward). Disable if at RESOLVED.

    5. **AssignmentChip.vue**: Unit chip for proximity-ranked dispatch section:
       - Shows: callsign (bold), type icon/badge, distance_km (e.g., "2.3 km", font-mono), eta_minutes (e.g., "~5 min", font-mono)
       - One-click assign: on click, POST to dispatch.assign via Wayfinder action with {unit_id}. No confirmation modal (speed is critical per locked decision).
       - Loading state while POST in flight
       - After assignment, chip should visually move to assignees section (parent handles this by updating incident data)

    6. **IncidentDetailPanel.vue**: Right panel when incident selected (360px):
       - Header: incident_no (font-mono), incident_type name, PriBadge, status badge, close (X) button to deselect
       - SlaProgressBar component
       - Info section: location_text, barangay name, caller_name, caller_contact, coordinates (font-mono), elapsed time
       - Description/notes text (if present)
       - StatusPipeline component with ADVANCE button
       - ASSIGNEES section: List of assigned units with callsign, status badge, AckTimerRing. Click assigned unit shows confirmation dialog then calls dispatch.unassign.
       - DISPATCH section: "Available Units" header. On mount / on incident select, fetch GET dispatch.nearby-units via Wayfinder action. Display AssignmentChip for each nearby unit. Show "No available units nearby" if empty.
       - TIMELINE section: Chronological list of incident timeline entries (if loaded). Show event_type icon, description, actor name, timestamp (font-mono). Scrollable, max-height.
       - "REQUEST MUTUAL AID" button at bottom (opens modal in Plan 04)
       - Scrollable overflow-y-auto for the entire panel

    7. **UnitStatusPanel.vue**: Right panel default (no incident selected):
       - Header: "UNIT STATUS" with total count
       - Group units by agency (AMB, BFP, PNP, RESCUE, etc.)
       - Each group: agency header, then unit rows with: callsign, status dot (colored), status text, crew_capacity
       - Click unit row: emit 'select-unit' with unit id

    8. **UnitDetailPanel.vue**: Right panel when unit marker clicked on map:
       - Header: callsign (large, font-mono), type, agency
       - Status badge with dot
       - Crew count
       - Current assignment info (if dispatched/en_route/on_scene): incident_no, incident_type, priority
       - Coordinates (font-mono) if available
       - Back button to return to unit status panel

    9. **Wire all into Console.vue**:
       - Track right panel mode: 'unit-status' (default) | 'incident-detail' | 'unit-detail'
       - On incident select (from queue or map click): mode = 'incident-detail', pass selected incident
       - On unit click (from map): mode = 'unit-detail', pass selected unit
       - On deselect (map empty click or X button): mode = 'unit-status'
       - On assignment success: refresh incident data (the assigned_units array) -- use router.reload({ only: ['incidents'] }) or local mutation
       - Wire ack timer expiry to useAlertSystem.playAckExpiredTone()

    Run `npm run lint` and `npm run format`.
  </action>
  <verify>
    <automated>npm run types:check && npm run build</automated>
  </verify>
  <done>All three right panel modes render correctly: unit status (grouped by agency), incident detail (with SLA bar, status pipeline, assignees with ack timer, dispatch chips with proximity ranking, timeline), unit detail. Assignment chips trigger one-click assign. Ack timer counts down 90 seconds with color transitions. Status pipeline shows ADVANCE button. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run types:check` -- no TypeScript errors
- `npm run build` -- Vite build succeeds
- `php artisan test --compact` -- full suite green
</verification>

<success_criteria>
- Left panel: queue with filter tabs (ALL, P1, P1-2, ACTIVE), priority-ordered cards, selection triggers flyTo
- Right panel defaults to unit roster grouped by agency
- Right panel shows incident detail on selection: SLA bar, status pipeline with ADVANCE, assignees with ack timer, dispatch chips with distance/ETA
- One-click assign on dispatch chip triggers POST and updates UI
- 90-second ack timer with green > 30s, red <= 30s, expired state
- Session metrics compute reactively from local incident/unit data, including averageHandleTime
- All components compile and render without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-dispatch-console/04-03-SUMMARY.md`
</output>
