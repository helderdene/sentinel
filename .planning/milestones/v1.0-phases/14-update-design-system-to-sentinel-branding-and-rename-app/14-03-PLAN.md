---
phase: 14-update-design-system-to-sentinel-branding-and-rename-app
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - resources/js/composables/useDispatchMap.ts
  - resources/js/composables/useAnalyticsMap.ts
  - resources/js/composables/useAlertSystem.ts
  - resources/js/pages/analytics/Dashboard.vue
  - resources/js/components/analytics/KpiCard.vue
  - resources/js/components/analytics/KpiLineChart.vue
  - resources/js/components/responder/NavTab.vue
  - resources/js/components/responder/OutcomeSheet.vue
  - resources/js/components/responder/StandbyScreen.vue
  - resources/js/components/responder/StatusButton.vue
  - resources/js/components/intake/QueueRow.vue
  - resources/js/components/intake/PriBadge.vue
  - resources/js/components/intake/ChBadge.vue
  - resources/js/components/NavMain.vue
autonomous: true
requirements:
  - REBRAND-06

must_haves:
  truths:
    - "Dispatch map markers use Sentinel priority colors (P1 #E24B4A, P2 #EF9F27, P3 #1D9E75, P4 #378ADD)"
    - "Dispatch map unit status markers use Sentinel palette (available teal, on-scene amber)"
    - "Analytics charts use Sentinel palette colors"
    - "Responder UI components use Sentinel priority/status colors"
    - "Nav section labels use DM Mono at 10px with 2-3px letter-spacing"
    - "PriBadge has pill shape with dot indicator, colored border (40% opacity), and tinted background (15% opacity) per brand guide"
    - "ChBadge retains its existing border style (already has border) — no regression"
  artifacts:
    - path: "resources/js/composables/useDispatchMap.ts"
      provides: "Sentinel priority and status colors for MapLibre WebGL layers"
      contains: "#E24B4A"
    - path: "resources/js/composables/useAnalyticsMap.ts"
      provides: "Sentinel priority colors for analytics popup rendering"
      contains: "#E24B4A"
    - path: "resources/js/pages/analytics/Dashboard.vue"
      provides: "Sentinel palette for Chart.js charts"
      contains: "#378ADD"
    - path: "resources/js/components/NavMain.vue"
      provides: "DM Mono section labels at 10px with 2.5px tracking"
      contains: "text-[10px]"
    - path: "resources/js/components/intake/PriBadge.vue"
      provides: "Sentinel badge style: pill shape, dot indicator, colored border, 15% tinted background"
      contains: "color-mix"
    - path: "resources/js/components/intake/ChBadge.vue"
      provides: "Channel badge — verify border style not regressed"
      contains: "borderColor"
  key_links:
    - from: "resources/js/composables/useDispatchMap.ts PRIORITY_COLORS"
      to: "MapLibre circle-color expressions for incident markers"
      via: "MapLibre style spec 'match' expression"
      pattern: "match.*get.*priority"
    - from: "resources/js/composables/useDispatchMap.ts STATUS_COLORS"
      to: "MapLibre circle-color expressions for unit markers"
      via: "MapLibre style spec 'match' expression"
      pattern: "match.*get.*status"
    - from: "resources/js/components/NavMain.vue section label"
      to: "All sidebar navigation groups"
      via: "SidebarGroupLabel component"
      pattern: "SidebarGroupLabel"
    - from: "resources/js/components/intake/PriBadge.vue"
      to: "FeedCard, TriageForm, QueueRow, IncidentDetailPanel, etc."
      via: "Component import across intake/dispatch surfaces"
      pattern: "PriBadge"
---

<objective>
Sweep all hardcoded hex colors in MapLibre map composables, Chart.js analytics components, responder UI components, and intake QueueRow to use Sentinel palette. Update PriBadge to Sentinel badge style (pill shape, colored border, 15% tinted background). Update NavMain section label typography to DM Mono 10px per brand guide.

Purpose: The token cascade (Plan 01) handles ~80% of the UI automatically. This plan catches the remaining ~20% — hardcoded hex strings in JavaScript/TypeScript that MapLibre and Chart.js require, inline style colors in responder components, badge style updates per brand guide, and nav label typography.

Output: All visible colors in the app match Sentinel palette. Badge components match brand guide style. Zero remaining old palette hex values in the codebase.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-CONTEXT.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-RESEARCH.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-01-SUMMARY.md

<interfaces>
<!-- Color mapping reference for all hardcoded replacements -->

Priority colors (bright/dark variants for map markers and dark UI):
| Priority | Old Light | Old Dark/Map | Sentinel Light | Sentinel Dark/Map |
|----------|-----------|-------------|----------------|-------------------|
| P1       | #dc2626   | #ef4444     | #A32D2D        | #E24B4A           |
| P2       | #ea580c   | #f97316     | #854F0B        | #EF9F27           |
| P3       | #ca8a04   | #eab308     | #0F6E56        | #1D9E75           |
| P4       | #16a34a   | #22c55e     | #185FA5        | #378ADD           |

Unit status colors (dispatch map always dark base):
| Status     | Old Hex   | Sentinel Hex | Note |
|------------|-----------|--------------|------|
| AVAILABLE  | #16a34a   | #1D9E75      | Teal |
| DISPATCHED | #2563eb   | #378ADD      | Blue |
| EN_ROUTE   | #3b82f6   | #378ADD      | Blue |
| ON_SCENE   | #ca8a04   | #EF9F27      | Amber |
| OFFLINE    | #6b7280   | #6b7280      | Gray (keep) |

Accent/brand replacements:
| Old Hex   | Sentinel Hex | Context |
|-----------|-------------|---------|
| #2563eb   | #378ADD     | Accent blue in dark contexts |
| #3b82f6   | #378ADD     | Lighter accent blue |
| #1e3a6e   | #042C53     | Brand color |
| #0f172a   | #05101E     | Dark background |
| #1e293b   | #041E38     | Dark surface |

From resources/js/composables/useDispatchMap.ts:
- PRIORITY_COLORS: MapLibre expression ['match', ['get', 'priority'], 'P1', '...', ...]
- STATUS_COLORS: MapLibre expression ['match', ['get', 'status'], 'AVAILABLE', '...', ...]
- ICON_COLORS: Similar match expression for icon layer colors

From resources/js/components/NavMain.vue:
- SidebarGroupLabel: `text-[9px] tracking-[2px]` -> `text-[10px] tracking-[2.5px]`

From resources/js/components/intake/PriBadge.vue (current state):
- Uses `color-mix(in srgb, ${color} 8%, transparent)` for background — change to 15%
- Has dot indicator (span with backgroundColor: color) — keep
- Has `rounded` class — change to `rounded-full` for pill shape
- NO border currently — add colored border at 40% opacity via color-mix()
- Uses var(--t-p1) through var(--t-p4) for colors — these already cascade from Plan 01 token updates

From resources/js/components/intake/ChBadge.vue (current state):
- Already has border: `borderColor: color-mix(in srgb, ${config.color} 21%, transparent)`
- Already has background: `color-mix(in srgb, ${config.color} 7%, transparent)`
- Has `rounded` class — change to `rounded-full` for pill shape consistency
- Adjust background from 7% to 15% and border from 21% to 40% to match Sentinel badge style
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update hardcoded MapLibre and analytics colors to Sentinel palette</name>
  <files>resources/js/composables/useDispatchMap.ts, resources/js/composables/useAnalyticsMap.ts, resources/js/composables/useAlertSystem.ts, resources/js/pages/analytics/Dashboard.vue, resources/js/components/analytics/KpiCard.vue, resources/js/components/analytics/KpiLineChart.vue</files>
  <action>
**First: Read each file to identify exact hex values and their locations.**

**In `resources/js/composables/useDispatchMap.ts`:**

1. Find PRIORITY_COLORS expression and replace values:
   - `'P1', '#dc2626'` or `'P1', '#ef4444'` -> `'P1', '#E24B4A'`
   - `'P2', '#ea580c'` or `'P2', '#f97316'` -> `'P2', '#EF9F27'`
   - `'P3', '#ca8a04'` or `'P3', '#eab308'` -> `'P3', '#1D9E75'`
   - `'P4', '#16a34a'` or `'P4', '#22c55e'` -> `'P4', '#378ADD'`
   - Default/fallback: `'#888888'` (keep gray)

2. Find STATUS_COLORS expression and replace values:
   - `'AVAILABLE', '#16a34a'` -> `'AVAILABLE', '#1D9E75'`
   - `'DISPATCHED', '#2563eb'` or similar -> `'DISPATCHED', '#378ADD'`
   - `'EN_ROUTE', '#3b82f6'` or similar -> `'EN_ROUTE', '#378ADD'`
   - `'ON_SCENE', '#ca8a04'` or similar -> `'ON_SCENE', '#EF9F27'`
   - `'OFFLINE', '#6b7280'` -> keep (gray is fine)
   - Default/fallback: `'#888888'` (keep)

3. Find ICON_COLORS or any other hardcoded color arrays and update with the same mapping.

4. Search for any other old palette hex values: `#dc2626`, `#ea580c`, `#ca8a04`, `#16a34a`, `#2563eb`, `#3b82f6`, `#0f172a`, `#1e293b`, `#334155`, `#ef4444`, `#f97316`, `#eab308`, `#22c55e`. Replace with Sentinel equivalents.

**In `resources/js/composables/useAnalyticsMap.ts`:**

Find priority color mappings in popup rendering and replace with Sentinel palette. Same hex mapping as above.

**In `resources/js/composables/useAlertSystem.ts`:**

Search for P1 flash color or any hardcoded `#dc2626`/`rgba(220, 38, 38, ...)`. If present, replace with Sentinel P1: `#E24B4A` / `rgba(226, 75, 74, ...)`.

**In `resources/js/pages/analytics/Dashboard.vue`:**

Find Chart.js config color values. Replace with Sentinel palette:
- Any `#dc2626`/`#ef4444` -> `#E24B4A` (P1)
- Any `#ea580c`/`#f97316` -> `#EF9F27` (P2)
- Any `#ca8a04`/`#eab308` -> `#1D9E75` (P3)
- Any `#16a34a`/`#22c55e` -> `#378ADD` (P4)
- Any `#2563eb`/`#3b82f6` -> `#378ADD` (accent)

**In `resources/js/components/analytics/KpiCard.vue`:**

Find hardcoded KPI indicator colors. Replace with Sentinel palette equivalents.

**In `resources/js/components/analytics/KpiLineChart.vue`:**

Find Chart.js line colors. Replace with Sentinel palette.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build && npm run types:check</automated>
  </verify>
  <done>
    - useDispatchMap.ts PRIORITY_COLORS uses #E24B4A, #EF9F27, #1D9E75, #378ADD
    - useDispatchMap.ts STATUS_COLORS uses #1D9E75 (available), #378ADD (dispatched/en_route), #EF9F27 (on_scene)
    - useAnalyticsMap.ts priority colors updated to Sentinel palette
    - Analytics Dashboard/KpiCard/KpiLineChart use Sentinel chart colors
    - No old palette hex values (#dc2626, #ea580c, #ca8a04, #16a34a, #2563eb) remain in these files
    - TypeScript compiles
    - Frontend builds
  </done>
</task>

<task type="auto">
  <name>Task 2: Update responder, intake, badge components, and nav section label</name>
  <files>resources/js/components/responder/NavTab.vue, resources/js/components/responder/OutcomeSheet.vue, resources/js/components/responder/StandbyScreen.vue, resources/js/components/responder/StatusButton.vue, resources/js/components/intake/QueueRow.vue, resources/js/components/intake/PriBadge.vue, resources/js/components/intake/ChBadge.vue, resources/js/components/NavMain.vue</files>
  <action>
**First: Read each file to find hardcoded hex values and their exact locations.**

**In `resources/js/components/intake/PriBadge.vue`:**

Update to Sentinel badge style per brand guide. The current component already uses `var(--t-p*)` colors (which get Sentinel values from Plan 01's token update). The style changes are:

1. **Shape** — Change `rounded` class to `rounded-full` for pill shape
2. **Background opacity** — Change `color-mix(in srgb, ${color} 8%, transparent)` to `color-mix(in srgb, ${color} 15%, transparent)`
3. **Add border** — Add border properties to the `:style` binding:
   ```
   borderWidth: '1px',
   borderStyle: 'solid',
   borderColor: `color-mix(in srgb, ${color} 40%, transparent)`,
   ```
4. **Dot indicator** — Already exists (the inner `<span>` with backgroundColor: color). Keep as-is.

The result should match the brand guide pattern:
```vue
<span
    class="inline-flex items-center gap-1 rounded-full font-mono font-bold whitespace-nowrap"
    :class="isLarge ? 'px-2.5 py-1 text-[11px]' : 'px-[7px] py-[2px] text-[10px]'"
    :style="{
        backgroundColor: `color-mix(in srgb, ${color} 15%, transparent)`,
        color: color,
        borderWidth: '1px',
        borderStyle: 'solid',
        borderColor: `color-mix(in srgb, ${color} 40%, transparent)`,
    }"
>
```

**In `resources/js/components/intake/ChBadge.vue`:**

Update to match Sentinel badge style consistency:

1. **Shape** — Change `rounded` class to `rounded-full` for pill shape consistency with PriBadge
2. **Background opacity** — Change `color-mix(in srgb, ${config.color} 7%, transparent)` to `color-mix(in srgb, ${config.color} 15%, transparent)`
3. **Border opacity** — Change `color-mix(in srgb, ${config.color} 21%, transparent)` to `color-mix(in srgb, ${config.color} 40%, transparent)`

ChBadge already has the border — just adjust the opacity percentages to match the Sentinel brand guide pattern.

**In `resources/js/components/responder/NavTab.vue`:**

Find priority color map used for route/incident display. Replace:
- P1 red: any `#dc2626`/`#ef4444` -> `#E24B4A` (for dark responder UI) or `#A32D2D` (for light contexts)
- P2 orange: any `#ea580c`/`#f97316` -> `#EF9F27` or `#854F0B`
- P3 yellow: any `#ca8a04`/`#eab308` -> `#1D9E75` or `#0F6E56`
- P4 green: any `#16a34a`/`#22c55e` -> `#378ADD` or `#185FA5`

**In `resources/js/components/responder/OutcomeSheet.vue`:**

Find outcome icon colors. Replace old palette references with Sentinel equivalents. Keep semantic meaning (success = teal, danger = red, etc.).

**In `resources/js/components/responder/StandbyScreen.vue`:**

Find any hardcoded colors. Replace with Sentinel palette. This is likely accent/brand colors for the standby pulse animation.

**In `resources/js/components/responder/StatusButton.vue`:**

Find status color mapping (same as dispatch status colors):
- Available/online -> `#1D9E75` (teal)
- En Route -> `#378ADD` (blue)
- On Scene -> `#EF9F27` (amber)
- Any accent blue -> `#378ADD`

**In `resources/js/components/intake/QueueRow.vue`:**

Find override/recall icon colors. Replace:
- Red/danger references: old reds -> Sentinel P1 (`#E24B4A` dark / `#A32D2D` light)
- Blue/accent references: old blues -> Sentinel accent (`#378ADD` dark / `#185FA5` light)

**In `resources/js/components/NavMain.vue`:**

Update the SidebarGroupLabel typography per brand guide:
- Change `text-[9px]` to `text-[10px]`
- Change `tracking-[2px]` to `tracking-[2.5px]`

These are the only changes needed in NavMain — the font-mono class already references DM Mono through the token cascade from Plan 01.

**Final sweep** — After all changes, run a comprehensive grep for remaining old palette hex values across the entire frontend codebase:
```bash
grep -rn "#dc2626\|#ea580c\|#ca8a04\|#16a34a\|#ef4444\|#f97316\|#eab308\|#22c55e" --include="*.vue" --include="*.ts" resources/js/
```
Fix any remaining matches (excluding comments and documentation).

Also check for old dark bg/surface values:
```bash
grep -rn "#0f172a\|#1e293b\|#0B1120" --include="*.vue" --include="*.ts" resources/js/
```
These should now reference CSS variables (via token cascade) or use Sentinel equivalents (#05101E, #041E38).
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build && npm run types:check && npm run lint && php artisan test --compact</automated>
  </verify>
  <done>
    - PriBadge.vue uses rounded-full (pill), 15% background opacity, 40% border opacity with color-mix()
    - ChBadge.vue uses rounded-full (pill), 15% background opacity, 40% border opacity with color-mix()
    - Responder NavTab, OutcomeSheet, StandbyScreen, StatusButton use Sentinel colors
    - Intake QueueRow uses Sentinel colors for override/recall
    - NavMain SidebarGroupLabel is 10px with 2.5px tracking
    - grep for old palette hex values (#dc2626, #ea580c, #ca8a04, #16a34a) returns zero in resources/js/
    - ESLint passes
    - TypeScript compiles
    - Frontend builds
    - All tests pass
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. `npm run types:check` passes
3. `npm run lint` passes
4. `php artisan test --compact` passes
5. Zero old palette hex values found:
   `grep -rn "#dc2626\|#ea580c\|#ca8a04\|#16a34a\|#2563eb\|#ef4444\|#f97316\|#eab308\|#22c55e\|#0B1120" --include="*.vue" --include="*.ts" resources/js/ report-app/src/` returns 0 matches
6. NavMain label uses text-[10px] tracking-[2.5px]
7. PriBadge.vue uses `rounded-full` class and has `borderWidth: '1px'` in style binding
8. ChBadge.vue uses `rounded-full` class and border opacity is 40%
</verification>

<success_criteria>
- Every visible color in the entire app matches the Sentinel palette
- Dispatch map markers (priority + status) use Sentinel colors
- Analytics charts and KPI cards use Sentinel palette
- Responder components use Sentinel priority/status colors
- PriBadge and ChBadge match Sentinel brand guide: pill shape, dot indicator, colored border (40%), tinted background (15%)
- NavMain section labels match brand guide typography (DM Mono 10px, 2.5px tracking)
- Zero old palette hex values remain in application JavaScript/TypeScript/Vue code
- All tests pass, all builds succeed, TypeScript compiles, ESLint passes
</success_criteria>

<output>
After completion, create `.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-03-SUMMARY.md`
</output>
