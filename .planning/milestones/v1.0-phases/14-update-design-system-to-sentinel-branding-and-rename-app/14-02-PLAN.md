---
phase: 14-update-design-system-to-sentinel-branding-and-rename-app
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - resources/js/components/AppLogo.vue
  - resources/js/layouts/AuthLayout.vue
  - public/favicon.svg
  - public/pwa-192x192.png
  - public/pwa-512x512.png
  - public/maskable-icon-512x512.png
  - public/apple-touch-icon.png
  - .env
  - vite.config.ts
  - resources/js/sw.ts
  - resources/js/components/PushPermissionPrompt.vue
  - resources/js/components/dispatch/DispatchTopbar.vue
  - resources/js/components/dispatch/DispatchStatusbar.vue
  - resources/js/components/intake/IntakeTopbar.vue
  - resources/js/components/intake/IntakeStatusbar.vue
  - report-app/src/views/HomeView.vue
  - report-app/src/views/AboutView.vue
  - report-app/src/views/ReportConfirmView.vue
autonomous: true
requirements:
  - REBRAND-03
  - REBRAND-04
  - REBRAND-05

must_haves:
  truths:
    - "Auth page shows animated Sentinel shield with 'SENTINEL' title in Bebas Neue and 'Incident Response Management System' subtitle"
    - "Sidebar shows simplified Sentinel shield icon at 26x30px with 'SENTINEL' text"
    - "All user-facing text says 'Sentinel' — no 'IRMS' or 'CDRRMO' references remain"
    - "PWA manifest says 'Sentinel' and uses #042C53 Command Blue theme"
    - "Service worker push notifications use 'Sentinel' as default title"
    - "Favicon is the simplified Sentinel shield SVG"
    - "PWA icon PNGs show simplified Sentinel shield on #042C53 background"
  artifacts:
    - path: "resources/js/layouts/AuthLayout.vue"
      provides: "Sentinel branded auth page with animated shield and Bebas Neue title"
      contains: "SENTINEL"
    - path: "resources/js/components/AppLogo.vue"
      provides: "Simplified 26x30 Sentinel shield icon and SENTINEL text for sidebar"
      contains: "SENTINEL"
    - path: "public/favicon.svg"
      provides: "Simplified Sentinel shield SVG favicon"
      min_lines: 5
    - path: "public/pwa-192x192.png"
      provides: "192x192 PWA icon with Sentinel shield on #042C53"
    - path: "public/pwa-512x512.png"
      provides: "512x512 PWA icon with Sentinel shield on #042C53"
    - path: "public/maskable-icon-512x512.png"
      provides: "512x512 maskable PWA icon with Sentinel shield on #042C53"
    - path: "public/apple-touch-icon.png"
      provides: "180x180 Apple touch icon with Sentinel shield on #042C53"
    - path: "vite.config.ts"
      provides: "PWA manifest with Sentinel branding"
      contains: "Sentinel"
    - path: "resources/js/sw.ts"
      provides: "Push notifications with Sentinel title"
      contains: "Sentinel"
  key_links:
    - from: "resources/js/components/AppLogo.vue"
      to: "Sidebar component tree"
      via: "Imported by AppSidebarLayout"
      pattern: "SENTINEL"
    - from: "resources/js/layouts/AuthLayout.vue"
      to: "All auth pages (Login, Register, etc.)"
      via: "Layout wrapper for auth/* pages"
      pattern: "SENTINEL"
    - from: ".env APP_NAME"
      to: "config('app.name') in Blade templates"
      via: "Laravel config cascade"
      pattern: "APP_NAME=Sentinel"
---

<objective>
Replace all IRMS/CDRRMO visual branding with Sentinel identity: animated shield on auth page, simplified shield in sidebar and favicon, rename all user-facing strings, update PWA manifest, and regenerate PWA icon PNGs.

Purpose: This plan handles the identity layer of the rebrand — everything a user sees that says "IRMS" or "CDRRMO" must now say "Sentinel". The SVG shields are extracted directly from the brand guide at `docs/hd-sentinel-brand_1.html`. PWA icons are regenerated with the Sentinel shield on Command Blue background.

Output: Updated auth page with animated Sentinel shield, sidebar with simplified shield, favicon, PWA icon PNGs, PWA manifest, and all string references renamed.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-CONTEXT.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-RESEARCH.md
@.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-01-SUMMARY.md
@docs/hd-sentinel-brand_1.html

<interfaces>
<!-- SVG assets to extract from brand guide -->

Simplified Shield (sidebar, favicon) — 26x30px, from brand guide nav section (~line 1212):
```svg
<svg width="26" height="30" viewBox="0 0 26 30" fill="none">
  <path d="M13 1L25 6.5V16C25 22.5 13 29 13 29C13 29 1 22.5 1 16V6.5L13 1Z"
    fill="#0C447C" stroke="#378ADD" stroke-width="1.2"/>
  <path d="M13 3L23 7.5V15.5C23 21 13 27 13 27C13 27 3 21 3 15.5V7.5L13 3Z"
    fill="none" stroke="rgba(55,138,221,0.25)" stroke-width="0.5"/>
  <circle cx="13" cy="17" r="5.5" fill="none" stroke="#378ADD" stroke-width="0.8" opacity="0.4"/>
  <circle cx="13" cy="17" r="3.2" fill="#378ADD" opacity="0.9"/>
  <circle cx="13" cy="17" r="1.3" fill="#E6F1FB"/>
  <line x1="9" y1="9" x2="17" y2="9" stroke="#378ADD" stroke-width="0.7" opacity="0.5"/>
  <line x1="7.5" y1="11.5" x2="18.5" y2="11.5" stroke="#378ADD" stroke-width="0.5" opacity="0.3"/>
</svg>
```

Full Animated Shield (auth page) — Large hero shield (~85 lines) from brand guide hero section (~line 1245-1330):
Contains: shield outline with radial gradient glow, 6 concentric radar rings, crosshair lines, signal arcs, eye with iris/pupil/highlight, animated pulse ring, animated sweep line.
CSS animations (pulseRing, sweep) are added in Plan 01's app.css.

The executor MUST read the brand guide HTML to extract the full hero shield SVG — the research provides line numbers but the actual SVG content is in the file.

NOTE: This plan depends on Plan 01 (wave 2). Plan 01 adds pulseRing and sweep CSS keyframes to app.css, registers --font-display (Bebas Neue) in @theme inline, and updates the token palette. AuthLayout.vue's animated shield and font-display class depend on these being present.

<!-- Current components being replaced -->

From resources/js/components/AppLogo.vue:
- Currently shows a simple shield outline SVG + "IRMS" text
- Must become simplified Sentinel shield + "SENTINEL" (DM Sans 500)

From resources/js/layouts/AuthLayout.vue:
- Currently shows CDRRMO shield in color-mix() bg box + "CDRRMO Butuan City" + "IRMS -- Incident Response Management System"
- Must become full animated Sentinel shield + "SENTINEL" (Bebas Neue) + "Incident Response Management System"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract SVG shields, update auth page, sidebar logo, favicon, and generate PWA icon PNGs</name>
  <files>resources/js/layouts/AuthLayout.vue, resources/js/components/AppLogo.vue, public/favicon.svg, public/pwa-192x192.png, public/pwa-512x512.png, public/maskable-icon-512x512.png, public/apple-touch-icon.png</files>
  <action>
**Read the brand guide** at `docs/hd-sentinel-brand_1.html` to extract the exact SVG markup for both shields.

**In `resources/js/layouts/AuthLayout.vue`:**

Replace the entire branding section (the `<div class="mb-8 flex flex-col items-center gap-2">` block) with:

1. **Full animated Sentinel shield** — Extract the hero shield SVG from brand guide (lines ~1245-1330). Place it in a container sized ~120x136px. The SVG includes:
   - Shield outline with radial gradient glow
   - Concentric radar rings
   - Crosshair lines with tick marks
   - Signal arcs at top
   - Eye with iris, pupil, highlight
   - Animated pulse ring circle (uses `pulseRing 2.4s ease-out infinite` — keyframe added in Plan 01)
   - Animated sweep line (uses `sweep 4s linear infinite` — keyframe added in Plan 01)

   Note: The SVG references inline CSS animations. Since the keyframes are in app.css (Plan 01), use class names or inline style attributes on the animated elements that reference the keyframe names.

2. **"SENTINEL" title** — Replace "CDRRMO Butuan City" with:
   ```html
   <span class="text-2xl font-display tracking-[3px] text-t-brand">SENTINEL</span>
   ```
   Where `font-display` maps to `--font-display: 'Bebas Neue'` (from Plan 01's @theme inline).
   Note: If font-display is not registered as a Tailwind utility, use inline style `font-family: 'Bebas Neue', sans-serif` instead.

3. **Subtitle** — Replace "IRMS -- Incident Response Management System" with:
   ```html
   <span class="text-xs text-muted-foreground">Incident Response Management System</span>
   ```
   (Remove the "IRMS &mdash;" prefix — just the subtitle per user decision)

4. **Remove the 52x52 color-mix box** around the old shield — the animated shield is standalone, not in a colored box.

**In `resources/js/components/AppLogo.vue`:**

Replace the entire template with:

1. **Shield icon container** — Keep the `bg-t-brand` rounded box but update SVG:
   ```html
   <div class="flex aspect-square size-8 items-center justify-center rounded-[8px] bg-t-brand">
       <!-- Simplified Sentinel shield from brand guide nav -->
       <svg class="size-5" viewBox="0 0 26 30" fill="none">
           <!-- Use the simplified shield SVG from research/brand guide -->
           <!-- Shield outline, inner border, radar circles, dot, horizontal lines -->
       </svg>
   </div>
   ```

2. **Text** — Change "IRMS" to "SENTINEL" with DM Sans weight 500:
   ```html
   <div class="ml-1 grid flex-1 text-left text-sm">
       <span class="mb-0.5 truncate leading-tight font-medium">SENTINEL</span>
   </div>
   ```

**In `public/favicon.svg`:**

Replace with the simplified Sentinel shield SVG. Use the 26x30 simplified shield but adjust viewBox for a square favicon (32x32) by centering the shield in a 32x32 viewBox:
```svg
<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="-3 -1 32 32" fill="none">
    <!-- Same simplified shield paths, centered -->
</svg>
```

**Generate PWA icon PNGs:**

After creating `public/favicon.svg` with the simplified Sentinel shield, generate the four required PWA PNG icon files. Use one of these approaches (in order of preference):

1. **Use `npx @nicolo-ribaudo/pwa-asset-generator` or `npx pwa-asset-generator`** if available, passing the favicon.svg as source with `#042C53` background color.

2. **Use `npx sharp-cli`** — If sharp is available (it's a Vite dependency), create a small Node script that:
   - Reads the SVG shield
   - Composites it onto a `#042C53` background at each required size
   - Outputs the 4 PNG files

3. **Use `sips` (macOS built-in)** — As a fallback on macOS:
   - Create a temporary SVG at each target size with `#042C53` rect background + shield SVG centered
   - Convert using: `sips -s format png --resampleWidth {size} temp.svg --out public/{filename}.png`

4. **Manual generation script** — Write a small Node.js script (delete after use) that uses `sharp` (already in node_modules as transitive dep of Vite):
   ```js
   const sharp = require('sharp');
   // Create SVG string with #042C53 background rect + shield SVG
   // Generate: pwa-192x192.png (192x192), pwa-512x512.png (512x512),
   //           maskable-icon-512x512.png (512x512 with safe zone padding),
   //           apple-touch-icon.png (180x180)
   ```

The four files to generate:
- `public/pwa-192x192.png` — 192x192, Sentinel shield centered on #042C53 background
- `public/pwa-512x512.png` — 512x512, Sentinel shield centered on #042C53 background
- `public/maskable-icon-512x512.png` — 512x512, shield within safe zone (inner 80%), #042C53 background
- `public/apple-touch-icon.png` — 180x180, Sentinel shield centered on #042C53 background

All PNGs should show the simplified Sentinel shield (white/light variant for contrast on dark #042C53 background — use `#FFFFFF` or `#E6F1FB` fills instead of the dark fills used in the SVG favicon).
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build && php artisan test --compact tests/Feature/Auth/ && test -f public/pwa-192x192.png && test -f public/pwa-512x512.png && test -f public/maskable-icon-512x512.png && test -f public/apple-touch-icon.png && echo "All PWA icons exist"</automated>
  </verify>
  <done>
    - AuthLayout.vue shows animated Sentinel shield SVG (radar, eye, pulse ring, sweep line)
    - AuthLayout.vue title reads "SENTINEL" in Bebas Neue (font-display class)
    - AuthLayout.vue subtitle reads "Incident Response Management System" (no "IRMS" prefix)
    - AppLogo.vue shows simplified 26x30 Sentinel shield in bg-t-brand box
    - AppLogo.vue text reads "SENTINEL" not "IRMS"
    - public/favicon.svg contains simplified Sentinel shield
    - public/pwa-192x192.png exists and shows Sentinel shield on #042C53
    - public/pwa-512x512.png exists and shows Sentinel shield on #042C53
    - public/maskable-icon-512x512.png exists with shield in safe zone on #042C53
    - public/apple-touch-icon.png exists and shows Sentinel shield on #042C53
    - Auth tests pass
    - Frontend builds
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename all IRMS/CDRRMO references and update PWA manifest</name>
  <files>.env, vite.config.ts, resources/js/sw.ts, resources/js/components/PushPermissionPrompt.vue, resources/js/components/dispatch/DispatchTopbar.vue, resources/js/components/dispatch/DispatchStatusbar.vue, resources/js/components/intake/IntakeTopbar.vue, resources/js/components/intake/IntakeStatusbar.vue, report-app/src/views/HomeView.vue, report-app/src/views/AboutView.vue, report-app/src/views/ReportConfirmView.vue</files>
  <action>
**String replacement sweep** — Search and replace across all files. The exact changes per file:

**`.env`:**
- Change `APP_NAME=IRMS` (or whatever current value) to `APP_NAME=Sentinel`

**`vite.config.ts`:**
- `name: 'IRMS - Incident Response Management System'` -> `name: 'Sentinel - Incident Response Management System'`
- `short_name: 'IRMS'` -> `short_name: 'Sentinel'`
- `description: 'CDRRMO Butuan City Incident Response Management System'` -> `description: 'Sentinel Incident Response Management System'`
- `theme_color: '#0B1120'` -> `theme_color: '#042C53'`
- `background_color: '#0B1120'` -> `background_color: '#042C53'`

**`resources/js/sw.ts`:**
- `data.title ?? 'IRMS'` -> `data.title ?? 'Sentinel'`
- `data.tag ?? 'irms-notification'` -> `data.tag ?? 'sentinel-notification'`

**`resources/js/components/PushPermissionPrompt.vue`:**
- Any "IRMS" text -> "Sentinel" in the prompt copy

**`resources/js/components/dispatch/DispatchTopbar.vue`:**
- Any "IRMS" text reference -> "SENTINEL"

**`resources/js/components/dispatch/DispatchStatusbar.vue`:**
- "CDRRMO BUTUAN CITY" -> "SENTINEL" (or remove — check what makes sense for a generic product)

**`resources/js/components/intake/IntakeTopbar.vue`:**
- Any "IRMS" text reference -> "SENTINEL"

**`resources/js/components/intake/IntakeStatusbar.vue`:**
- "CDRRMO - BUTUAN CITY" or similar -> "SENTINEL"

**`report-app/src/views/HomeView.vue`:**
- All "CDRRMO" references -> "Sentinel"
- All "IRMS" references -> "Sentinel"
- Maintain the emergency reporting context but rebrand

**`report-app/src/views/AboutView.vue`:**
- All "CDRRMO Butuan City" references -> "Sentinel"
- All "IRMS" references -> "Sentinel"
- Update any organizational description to be generic product-focused

**`report-app/src/views/ReportConfirmView.vue`:**
- All "CDRRMO" references -> "Sentinel"

**Verification sweep** — After making changes, grep the entire codebase for remaining "IRMS" and "CDRRMO" references (excluding docs/, .planning/, node_modules/, vendor/, .git/):
```bash
grep -rn "IRMS\|CDRRMO\|cdrrmo" --include="*.vue" --include="*.ts" --include="*.php" --include="*.blade.php" --include="*.html" --include="*.env" --exclude-dir=node_modules --exclude-dir=vendor --exclude-dir=.git --exclude-dir=.planning --exclude-dir=docs
```
Fix any remaining references found (excluding documentation).

Note: `CLAUDE.md` and `docs/` files are documentation and should NOT be changed — only application code.
  </action>
  <verify>
    <automated>cd /Users/helderdene/IRMS && npm run build && php artisan test --compact && cd /Users/helderdene/IRMS/report-app && npm run build</automated>
  </verify>
  <done>
    - .env has APP_NAME=Sentinel
    - vite.config.ts PWA manifest says "Sentinel" with #042C53 theme
    - sw.ts uses "Sentinel" as default notification title
    - PushPermissionPrompt says "Sentinel" not "IRMS"
    - Dispatch topbar/statusbar say "SENTINEL" not "IRMS"/"CDRRMO"
    - Intake topbar/statusbar say "SENTINEL" not "IRMS"/"CDRRMO"
    - Report app views reference "Sentinel" not "CDRRMO"/"IRMS"
    - grep for IRMS/CDRRMO in application code returns zero results (docs excluded)
    - All tests pass
    - Both main and report-app builds succeed
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (main app)
2. `cd report-app && npm run build` succeeds (report app)
3. `php artisan test --compact` passes -- all existing tests
4. `php artisan test --compact tests/Feature/Auth/` passes -- auth page renders
5. `grep -rn "IRMS\|CDRRMO" --include="*.vue" --include="*.ts" --include="*.php" --include="*.blade.php" --include="*.html" resources/ app/ routes/ config/ report-app/src/ public/ .env` returns zero results
6. AuthLayout.vue contains "SENTINEL" and animated shield SVG
7. AppLogo.vue contains "SENTINEL" and simplified shield SVG
8. vite.config.ts manifest contains `name: 'Sentinel'` and `theme_color: '#042C53'`
9. All four PWA PNG files exist in public/ with non-zero file size:
   `ls -la public/pwa-192x192.png public/pwa-512x512.png public/maskable-icon-512x512.png public/apple-touch-icon.png`
</verification>

<success_criteria>
- Every user-facing surface says "Sentinel" instead of "IRMS" or "CDRRMO"
- Auth page displays the full animated Sentinel shield with radar/eye motif
- Sidebar displays the simplified Sentinel shield icon
- Favicon is the simplified Sentinel shield
- PWA icon PNGs show Sentinel shield on #042C53 Command Blue background
- PWA manifest is fully rebranded (name, short_name, description, theme_color)
- Push notifications use "Sentinel" as default title
- Zero remaining IRMS/CDRRMO references in application code (documentation excluded)
</success_criteria>

<output>
After completion, create `.planning/phases/14-update-design-system-to-sentinel-branding-and-rename-app/14-02-SUMMARY.md`
</output>
