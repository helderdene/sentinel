---
phase: 05-responder-workflow
plan: 04
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - resources/js/components/responder/AssignmentNotification.vue
  - resources/js/components/responder/AssignmentTab.vue
  - resources/js/components/responder/NavTab.vue
  - resources/js/components/responder/OutcomeSheet.vue
  - resources/js/components/responder/ResourceRequestModal.vue
  - resources/js/components/responder/ClosureSummary.vue
  - resources/js/pages/responder/Station.vue
autonomous: false
requirements: [RSPDR-01, RSPDR-02, RSPDR-03, RSPDR-04, RSPDR-09, RSPDR-10, RSPDR-11]

must_haves:
  truths:
    - "Full-screen assignment notification takes over when AssignmentPushed arrives"
    - "ACKNOWLEDGE button shows 90-second countdown and sends ack on tap"
    - "After acknowledging, auto-transitions to Nav tab"
    - "Priority-matched audio tones play when assignment arrives"
    - "Nav tab shows MapLibre mini-map with unit position and incident location"
    - "Nav tab has large Google Maps deep-link button"
    - "Assignment tab shows full incident summary with caller info, notes, timeline, assigned units"
    - "Outcome selection bottom sheet appears when advancing to Resolving"
    - "5 outcome options as large touch-friendly cards"
    - "Transported to Hospital expands hospital picker (SearchableSelect)"
    - "Resource request modal offers 6 resource types as large buttons"
    - "Post-closure summary screen shows incident outcome with Done button"
    - "Done button returns to standby screen"
  artifacts:
    - path: "resources/js/components/responder/AssignmentNotification.vue"
      provides: "Full-screen takeover with ACKNOWLEDGE button and countdown"
    - path: "resources/js/components/responder/AssignmentTab.vue"
      provides: "Incident summary card with resource request button"
    - path: "resources/js/components/responder/NavTab.vue"
      provides: "MapLibre mini-map with route line and Google Maps deep-link"
    - path: "resources/js/components/responder/OutcomeSheet.vue"
      provides: "Bottom sheet with 5 outcome options and hospital picker"
    - path: "resources/js/components/responder/ResourceRequestModal.vue"
      provides: "Modal with 6 resource type buttons"
    - path: "resources/js/components/responder/ClosureSummary.vue"
      provides: "Post-closure summary with Done button"
  key_links:
    - from: "AssignmentNotification ACKNOWLEDGE button"
      to: "responder.acknowledge endpoint"
      via: "POST fetch on tap, closes ack timer"
      pattern: "acknowledge|acknowledged_at"
    - from: "NavTab MapLibre"
      to: "maplibre-gl + useGpsTracking.position"
      via: "Map instance with unit marker, incident pulse, straight-line polyline"
      pattern: "maplibre|addSource|addLayer"
    - from: "OutcomeSheet"
      to: "responder.resolve endpoint"
      via: "POST with outcome + optional hospital + closure_notes"
      pattern: "resolve|outcome|hospital"
    - from: "ResourceRequestModal"
      to: "responder.request-resource endpoint"
      via: "POST with resource type and notes"
      pattern: "requestResource|ResourceType"
    - from: "ClosureSummary Done button"
      to: "useResponderSession.resetAfterClosure"
      via: "Resets state to standby"
      pattern: "resetAfterClosure|StandbyScreen"
---

<objective>
Assignment notification, navigation, closure, and full wiring: AssignmentNotification with audio + countdown, AssignmentTab with incident summary, NavTab with MapLibre mini-map + Google Maps link, OutcomeSheet with hospital picker, ResourceRequestModal, ClosureSummary, and final Station.vue wiring with visual verification.

Purpose: Complete all remaining responder UI components and wire everything together for the full responder workflow: receive assignment -> acknowledge -> navigate -> document scene -> resolve -> return to standby.

Output: Fully functional responder workflow from assignment notification through closure, visually verified.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-responder-workflow/05-CONTEXT.md
@.planning/phases/05-responder-workflow/05-RESEARCH.md
@.planning/phases/05-responder-workflow/05-01-SUMMARY.md
@.planning/phases/05-responder-workflow/05-02-SUMMARY.md

<interfaces>
<!-- Types and composables from Plan 02 consumed here -->

From resources/js/types/responder.ts:
```typescript
export interface ResponderIncident {
    id: number; incident_no: string; priority: IncidentPriority; status: IncidentStatus;
    incident_type: { id: number; name: string; code: string; category: string };
    location_text: string | null;
    barangay: { id: number; name: string } | null;
    coordinates: { latitude: number; longitude: number } | null;
    notes: string | null;
    caller_name: string | null; caller_contact: string | null;
    assigned_units: Array<{ id: string; callsign: string; type: string }>;
    timeline: IncidentTimelineEntry[];
    // ... other fields
}
export type IncidentOutcome = 'TREATED_ON_SCENE' | 'TRANSPORTED_TO_HOSPITAL' | 'REFUSED_TREATMENT' | 'DECLARED_DOA' | 'FALSE_ALARM';
export type ResourceType = 'ADDITIONAL_AMBULANCE' | 'FIRE_UNIT' | 'POLICE_BACKUP' | 'RESCUE_BOAT' | 'MEDICAL_OFFICER' | 'MEDEVAC';
export interface Hospital { id: string; name: string; }
export interface AssignmentPayload { /* ... from Plan 02 */ }
```

From resources/js/composables/useResponderSession.ts:
```typescript
// Exposes: activeIncident, currentStatus, messages, unreadCount, activeTab,
//          showAssignmentNotification, showOutcomeSheet, showClosureSummary,
//          acknowledgeAssignment(), advanceStatus(), resetAfterClosure()
```

From resources/js/composables/useGpsTracking.ts:
```typescript
// Exposes: position: Ref<{lat, lng} | null>, start(), stop(), isTracking
```

From resources/js/composables/useAlertSystem.ts:
```typescript
export function useAlertSystem(): {
    playPriorityTone(priority: string): void;
    flashScreen(priority: string): void;
    stopAll(): void;
};
```

From resources/js/composables/useAckTimer.ts:
```typescript
export function useAckTimer(durationMs, onExpiry): {
    remaining: Ref<number>; formattedTime: ComputedRef<string>;
    isActive: ComputedRef<boolean>; start(): void; stop(): void;
};
```

From app/Events/AssignmentPushed.php broadcastWith():
```
// { id, incident_no, priority, status, incident_type, location_text, barangay, coordinates: {lat, lng}, notes, unit_id }
```

From existing SearchableSelect component (Phase 9, at report-app/src/components/SearchableSelect.vue):
```vue
<!-- Props: options (array of {id, name}), modelValue, placeholder, label -->
<!-- Used for hospital picker dropdown -->
```
Note: The SearchableSelect pattern needs to be replicated in the main app if not already available there.
Check `resources/js/components/` for an existing SearchableSelect or create one following the report-app pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AssignmentNotification, AssignmentTab, NavTab, and ResourceRequestModal</name>
  <files>
    resources/js/components/responder/AssignmentNotification.vue,
    resources/js/components/responder/AssignmentTab.vue,
    resources/js/components/responder/NavTab.vue,
    resources/js/components/responder/ResourceRequestModal.vue
  </files>
  <action>
    1. Create `AssignmentNotification.vue` — full-screen takeover on assignment arrival:
    - Full-screen overlay (fixed inset-0, z-50, bg matches dark surface)
    - Screen border colored by priority: P1 red pulse animation (border-4 + CSS pulsing), P2 orange, P3 amber, P4 green
    - Content centered vertically:
      - Priority badge (PriBadge component from existing components)
      - Incident type name (large, DM Sans bold)
      - Location text + barangay
      - Notes (if present, truncated with expandable)
      - Incident number (Space Mono)
    - Large ACKNOWLEDGE button at bottom: min-h-[64px], full-width, accent color
    - 90-second countdown timer displayed on the button: "ACKNOWLEDGE (1:28)" using useAckTimer composable
    - Priority-matched audio tones via useAlertSystem: play on mount, loop every 15s until acknowledged. Use setInterval for the 15s loop, clear on unmount or acknowledge
    - On ACKNOWLEDGE tap: POST to `responder.acknowledge` endpoint via fetch/Wayfinder, then emit 'acknowledged' to parent (which hides notification, auto-switches to Nav tab, stops audio)
    - On timer expiry: show "Timer expired" state with a "LATE ACKNOWLEDGE" button (still allows acknowledgement)
    - Props: `incident: AssignmentPayload`, `userId: number`
    - Emits: `acknowledged`

    2. Create `AssignmentTab.vue` — incident summary card:
    - Full incident information displayed as a card:
      - Header: incident number (Space Mono) + priority badge + incident type
      - Status pipeline: current status highlighted (similar to dispatch StatusPipeline but simplified for mobile)
      - Location: location_text + barangay name
      - Caller info: caller_name, caller_contact (if present)
      - Notes section
      - Assigned units list: each unit shown as a chip with callsign
      - Timeline: chronological list of key events (status changes, assignments) — last 10 entries
    - Resource Request button at bottom of card: large touch-friendly button (44px) labeled "Request Resource"
    - On button tap: emits 'show-resource-modal'
    - Scrollable content area
    - Props: `incident: ResponderIncident`
    - Emits: `show-resource-modal`

    3. Create `NavTab.vue` — navigation with MapLibre mini-map:
    - Large "OPEN IN GOOGLE MAPS" button at top: min-h-[52px], prominent blue, opens Google Maps deep-link in new tab. URL: `https://www.google.com/maps/dir/?api=1&destination={lat},{lng}&travelmode=driving`
    - MapLibre mini-map below (takes remaining height):
      - Initialize maplibre-gl map centered between responder position and incident position
      - Zoom level: auto-fit bounds to show both points with padding
      - Dark/light style matching existing dispatch map style URLs
      - Incident marker: red pulse ring (GeoJSON point + circle layer with animation, simplified from dispatch)
      - Unit position marker: blue circle (GeoJSON point + circle layer, updated reactively from useGpsTracking.position)
      - Straight-line polyline between unit and incident (GeoJSON line source + line layer, blue dashed)
      - Live ETA countdown: computed from straight-line distance at 30km/h (matches existing dispatch ETA calculation). Displayed as overlay on map corner (e.g., "ETA: 4 min")
      - Update unit marker position when GPS updates
    - If no GPS position available: show "GPS unavailable" message where map would be, keep Google Maps button functional
    - Props: `incident: ResponderIncident`, `gpsPosition: {lat: number, lng: number} | null`, `unitCallsign: string`
    - Reuse maplibre-gl (already installed). Follow useDispatchMap patterns for map initialization and GeoJSON source management

    4. Create `ResourceRequestModal.vue` — resource request overlay:
    - Modal overlay (fixed inset-0 with dark backdrop)
    - Title: "Request Additional Resource"
    - 6 resource types as large touch-friendly buttons (min-h-[56px]), arranged in 2-column grid:
      - Additional Ambulance (Ambulance icon or text)
      - Fire Unit
      - Police Backup
      - Rescue Boat
      - Medical Officer
      - Medevac
    - Optional notes textarea below buttons
    - On resource type tap: POST to `responder.request-resource` endpoint with type + notes, close modal, show brief success feedback
    - Cancel button at bottom
    - Props: `incidentId: number`, `isOpen: boolean`
    - Emits: `close`, `requested`

    5. Run `npm run lint`, `npm run format`
  </action>
  <verify>
    <automated>npm run types:check && npm run build</automated>
  </verify>
  <done>
    - AssignmentNotification takes over screen with priority-colored border, countdown, audio
    - ACKNOWLEDGE button sends POST and emits acknowledged
    - AssignmentTab shows full incident summary with resource request button
    - NavTab renders MapLibre mini-map with unit/incident markers and straight-line route
    - Google Maps deep-link opens correct navigation URL
    - ResourceRequestModal shows 6 resource types with POST on selection
    - All components compile and build clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OutcomeSheet, ClosureSummary, wire all components into Station.vue, and finalize full workflow</name>
  <files>
    resources/js/components/responder/OutcomeSheet.vue,
    resources/js/components/responder/ClosureSummary.vue,
    resources/js/pages/responder/Station.vue
  </files>
  <action>
    1. Create `OutcomeSheet.vue` — bottom sheet for outcome selection:
    - CSS bottom sheet: fixed inset-x-0 bottom-0, rounded-t-2xl, transform translateY transition (show/hide)
    - Dark backdrop overlay when open
    - Handle bar at top (small rounded rectangle, visual affordance)
    - Title: "Select Outcome"
    - 5 outcome cards in a vertical stack, each min-h-[56px] with large touch target:
      - Treated On Scene (green accent) — Heart/Pulse icon
      - Transported to Hospital (blue accent) — Truck icon
      - Patient Refused Treatment (amber accent) — XCircle icon
      - Declared DOA (gray/muted) — AlertTriangle icon
      - False Alarm / Stand Down (orange accent) — ShieldOff icon
    - When "Transported to Hospital" is selected: expand section below with hospital picker
      - Check if a SearchableSelect component exists in the main app (resources/js/components/). If not, create a simplified one following the report-app/src/components/SearchableSelect.vue pattern — a dropdown with text filter input
      - Hospital list received as prop (from Station.vue, passed from Inertia props)
    - Optional closure notes textarea (max 2000 chars)
    - "Confirm & Close Incident" button at bottom: POST to `responder.resolve` endpoint with outcome + hospital (if applicable) + closure_notes
    - If backend returns 422 (e.g., missing vitals for medical outcome): show error toast/message indicating vitals required
    - On successful resolution: close sheet, show ClosureSummary
    - Props: `incidentId: number`, `isOpen: boolean`, `hospitals: Hospital[]`
    - Emits: `close`, `resolved`

    2. Create `ClosureSummary.vue` — post-closure summary screen:
    - Full-screen summary overlaying the station content
    - Success header: checkmark icon + "Incident Resolved"
    - Summary card with:
      - Incident number (Space Mono)
      - Outcome (human-readable label)
      - Hospital name (if transported)
      - Scene time (formatted from scene_time_sec: e.g., "12 min 34 sec")
      - Checklist completion % (progress bar)
      - Vitals summary (if recorded): BP, HR, SpO2, GCS values
      - Assessment tags (chip list)
    - "Done" button at bottom: large, full-width, min-h-[52px]
    - On "Done" tap: calls session.resetAfterClosure() which clears active incident, returns to StandbyScreen
    - Props: `incident: ResponderIncident` (the just-resolved incident snapshot)
    - Emits: `done`

    3. Update `Station.vue` — final wiring of all components:
    - Replace remaining placeholder divs with actual components:
      - 'assignment' tab: `<AssignmentTab>` component
      - 'nav' tab: `<NavTab>` component with gpsPosition from useGpsTracking
    - Wire AssignmentNotification:
      - Show when `showAssignmentNotification` is true
      - On 'acknowledged' emit: hide notification, stop audio, auto-switch to 'nav' tab, start GPS tracking
    - Wire OutcomeSheet:
      - Show when `showOutcomeSheet` is true (triggered by StatusButton 'show-outcome-sheet' emit)
      - Pass hospitals from Inertia props
      - On 'resolved' emit: hide sheet, show ClosureSummary
    - Wire ClosureSummary:
      - Show when `showClosureSummary` is true
      - On 'done' emit: call resetAfterClosure(), stop GPS tracking
    - Wire ResourceRequestModal:
      - Show when resource modal flag is true (toggled by AssignmentTab 'show-resource-modal' emit)
    - Wire MessageBanner (from Plan 03): ensure it shows on all tabs except chat
    - Wire StatusButton advance to call the correct session methods:
      - For normal status advances: POST to responder.advance-status, then update local state
      - For RESOLVING transition: show OutcomeSheet instead
    - Ensure GPS tracking starts on acknowledge and stops on resolve/done
    - Ensure audio tones stop after acknowledge

    4. Run `npm run lint`, `npm run format`, `npm run types:check`, `npm run build`

    5. Run `php artisan test --compact` to verify no regressions
  </action>
  <verify>
    <automated>npm run types:check && npm run build && php artisan test --compact</automated>
  </verify>
  <done>
    - OutcomeSheet slides up with 5 outcome cards and hospital picker
    - ClosureSummary shows incident summary with Done button
    - Station.vue renders all tab content and modal/sheet overlays
    - Full workflow works: standby -> notification -> acknowledge -> nav -> scene -> resolve -> summary -> standby
    - GPS tracking starts on acknowledge, stops on closure
    - Audio plays on assignment, stops on acknowledge
    - TypeScript compiles, Vite builds, all tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete responder workflow</name>
  <files></files>
  <action>
    Human verifies the complete responder workflow end-to-end:
    Complete responder workflow: mobile-first station at /responder with assignment notification (priority audio + countdown), tab navigation (Assignment/Nav/Scene/Chat), scene documentation (checklists, vitals, assessment tags), messaging (quick-reply chips + free text), status transitions, outcome selection with hospital picker, resource requests, GPS tracking, and post-closure summary. All backed by 10 tested API endpoints with PDF generation on closure.

    Steps to verify:
    1. Log in as a responder user at irms.test
    2. Verify you see the standby screen with unit callsign and "Standing By"
    3. From another browser tab, log in as a dispatcher and assign a unit to an incident
    4. Verify the responder sees a full-screen assignment notification with priority-colored border and countdown
    5. Tap ACKNOWLEDGE — verify the notification closes, nav tab auto-opens
    6. Check the Nav tab: MapLibre mini-map shows incident location, Google Maps button is present
    7. Tap the status button to advance to EN ROUTE, then ON SCENE
    8. When ON SCENE, the middle tab should switch from Nav to Scene
    9. Open Scene tab: verify checklist, vitals form, assessment tags accordion
    10. Toggle some checklist items and assessment tags — verify they save (no explicit save button for tags)
    11. Fill in some vitals and tap Save
    12. Switch to Chat tab: tap a quick-reply chip, verify message appears in history
    13. Type a free-text message and send
    14. On Assignment tab, tap "Request Resource" and select a type
    15. Advance status to RESOLVING — verify outcome bottom sheet appears
    16. Select an outcome (e.g., Treated On Scene), add optional notes, tap Confirm
    17. Verify closure summary screen shows, tap Done to return to standby
    18. Verify dispatch console shows all status changes in real-time throughout
  </action>
  <verify>
    <automated>npm run types:check && npm run build && php artisan test --compact</automated>
  </verify>
  <done>
    - Human confirms complete responder workflow works end-to-end
    - All 18 verification steps pass
    - Type "approved" or describe issues
  </done>
</task>

</tasks>

<verification>
- `npm run types:check` — TypeScript compiles
- `npm run build` — Vite builds clean
- `npm run lint` — ESLint passes
- `php artisan test --compact` — full suite passes
- Visual verification: complete responder workflow from assignment to closure
</verification>

<success_criteria>
- Full-screen assignment notification with audio, countdown, priority border
- ACKNOWLEDGE -> auto-transition to Nav tab
- MapLibre mini-map with unit/incident markers and straight-line route
- Google Maps deep-link opens correct URL
- All status transitions work in forward-only order
- Outcome selection bottom sheet with 5 options and hospital picker
- Resource request modal with 6 types
- Post-closure summary with Done returning to standby
- Complete workflow verified by human tester
</success_criteria>

<output>
After completion, create `.planning/phases/05-responder-workflow/05-04-SUMMARY.md`
</output>
