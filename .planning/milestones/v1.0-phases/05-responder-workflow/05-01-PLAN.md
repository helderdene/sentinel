---
phase: 05-responder-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/ResponderController.php
  - app/Enums/IncidentOutcome.php
  - app/Enums/ResourceType.php
  - app/Events/ChecklistUpdated.php
  - app/Events/ResourceRequested.php
  - app/Jobs/GenerateIncidentReport.php
  - app/Http/Requests/AcknowledgeAssignmentRequest.php
  - app/Http/Requests/AdvanceResponderStatusRequest.php
  - app/Http/Requests/UpdateVitalsRequest.php
  - app/Http/Requests/UpdateChecklistRequest.php
  - app/Http/Requests/UpdateAssessmentTagsRequest.php
  - app/Http/Requests/SendMessageRequest.php
  - app/Http/Requests/RequestResourceRequest.php
  - app/Http/Requests/ResolveIncidentRequest.php
  - app/Http/Requests/UpdateLocationRequest.php
  - database/seeders/HospitalSeeder.php
  - resources/views/pdf/incident-report.blade.php
  - routes/web.php
  - app/Providers/AppServiceProvider.php
  - database/seeders/DatabaseSeeder.php
  - tests/Feature/Responder/AcknowledgeAssignmentTest.php
  - tests/Feature/Responder/StatusTransitionTest.php
  - tests/Feature/Responder/MessagingTest.php
  - tests/Feature/Responder/VitalsTest.php
  - tests/Feature/Responder/AssessmentTagsTest.php
  - tests/Feature/Responder/ChecklistTest.php
  - tests/Feature/Responder/ResolutionTest.php
  - tests/Feature/Responder/ResourceRequestTest.php
  - tests/Feature/Responder/LocationUpdateTest.php
  - tests/Feature/Responder/PdfGenerationTest.php
autonomous: true
requirements: [RSPDR-01, RSPDR-02, RSPDR-03, RSPDR-04, RSPDR-05, RSPDR-06, RSPDR-07, RSPDR-08, RSPDR-09, RSPDR-10, RSPDR-11]
user_setup: []

must_haves:
  truths:
    - "Responder can acknowledge an assignment and the dispatch timer closes"
    - "Responder can transition through all 5 statuses in forward-only order"
    - "Responder can send messages to dispatch and receive messages"
    - "Responder can save vitals with validated ranges"
    - "Responder can toggle assessment tags with auto-save"
    - "Responder can update checklist progress and dispatch sees percentage"
    - "Responder must select an outcome before resolving"
    - "Transported to Hospital outcome requires hospital selection"
    - "Resolving an incident queues a PDF generation job"
    - "Responder can request additional resources from the field"
    - "Responder can broadcast GPS location updates"
  artifacts:
    - path: "app/Http/Controllers/ResponderController.php"
      provides: "All 10 responder endpoints"
      exports: ["show", "acknowledge", "advanceStatus", "updateLocation", "sendMessage", "updateChecklist", "updateVitals", "updateAssessmentTags", "resolve", "requestResource"]
    - path: "app/Enums/IncidentOutcome.php"
      provides: "5 outcome values"
      contains: "enum IncidentOutcome"
    - path: "app/Jobs/GenerateIncidentReport.php"
      provides: "Queued PDF generation"
      contains: "class GenerateIncidentReport implements ShouldQueue"
    - path: "resources/views/pdf/incident-report.blade.php"
      provides: "DomPDF template for incident reports"
    - path: "database/seeders/HospitalSeeder.php"
      provides: "Butuan City hospitals for outcome picker"
  key_links:
    - from: "ResponderController::acknowledge"
      to: "IncidentUnit pivot + IncidentStatusChanged event"
      via: "updateExistingPivot acknowledged_at + dispatch event"
      pattern: "acknowledged_at.*now|IncidentStatusChanged::dispatch"
    - from: "ResponderController::resolve"
      to: "GenerateIncidentReport job"
      via: "dispatch after setting outcome"
      pattern: "GenerateIncidentReport::dispatch"
    - from: "ResponderController::advanceStatus"
      to: "IncidentStatusChanged event"
      via: "forward-only allowedTransitions map"
      pattern: "allowedTransitions.*IncidentStatusChanged"
    - from: "ResponderController::updateLocation"
      to: "UnitLocationUpdated event"
      via: "POST endpoint updating unit coordinates"
      pattern: "UnitLocationUpdated::dispatch"
---

<objective>
Responder backend foundation: ResponderController with 10 endpoints, IncidentOutcome enum, ResourceType enum, 2 new broadcast events, queued PDF generation job, DomPDF install, hospital seeder, PDF Blade template, and comprehensive Pest tests covering all 11 RSPDR requirements.

Purpose: Establish all backend API endpoints the responder frontend consumes. Every responder action (acknowledge, status transition, messaging, scene documentation, resolution, resource request, GPS update) has a tested endpoint before the UI is built.

Output: Fully tested ResponderController with 10 actions, DomPDF installed, PDF template, hospital seeder, 10 test files.
</objective>

<execution_context>
@/Users/helderdene/.claude/get-shit-done/workflows/execute-plan.md
@/Users/helderdene/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-responder-workflow/05-CONTEXT.md
@.planning/phases/05-responder-workflow/05-RESEARCH.md
@.planning/phases/04-dispatch-console/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/Enums/IncidentStatus.php:
```php
enum IncidentStatus: string
{
    case Pending = 'PENDING';
    case Triaged = 'TRIAGED';
    case Dispatched = 'DISPATCHED';
    case Acknowledged = 'ACKNOWLEDGED';
    case EnRoute = 'EN_ROUTE';
    case OnScene = 'ON_SCENE';
    case Resolving = 'RESOLVING';
    case Resolved = 'RESOLVED';
}
```

From app/Models/Incident.php (relevant fillable fields):
```php
// Fillable includes: outcome, hospital, scene_time_sec, checklist_pct, vitals, assessment_tags, report_pdf_url
// Casts: vitals => 'array', assessment_tags => 'array', scene_time_sec => 'integer', checklist_pct => 'integer'
```

From app/Models/IncidentUnit.php:
```php
class IncidentUnit extends Pivot {
    protected $table = 'incident_unit';
    // Casts: acknowledged_at => 'datetime'
}
```

From app/Models/IncidentMessage.php:
```php
// Fillable: incident_id, sender_type, sender_id, body, message_type, is_quick_reply, read_at
// Casts: read_at => 'datetime', is_quick_reply => 'boolean'
```

From app/Events/AssignmentPushed.php:
```php
// Broadcasts on: PrivateChannel('user.' . $this->userId)
// Payload: id, incident_no, priority, status, incident_type, location_text, barangay, coordinates, notes, unit_id
```

From app/Events/MessageSent.php:
```php
// Broadcasts on: PrivateChannel('user.' . recipientId)
// Payload: id, incident_id, body, message_type, is_quick_reply, sender (id, name, role), created_at
```

From app/Http/Controllers/DispatchConsoleController.php:
```php
// advanceStatus uses allowedTransitions map:
// DISPATCHED => [ACKNOWLEDGED], ACKNOWLEDGED => [EN_ROUTE], EN_ROUTE => [ON_SCENE], etc.
// Pattern: check current status, validate next is allowed, update incident, create timeline entry, broadcast
```

From app/Providers/AppServiceProvider.php:
```php
Gate::define('respond-incidents', fn (User $user): bool => $user->role === UserRole::Responder);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install DomPDF, create enums, events, hospital seeder, PDF template, form requests, and ResponderController with 10 endpoints</name>
  <files>
    app/Enums/IncidentOutcome.php,
    app/Enums/ResourceType.php,
    app/Events/ChecklistUpdated.php,
    app/Events/ResourceRequested.php,
    app/Jobs/GenerateIncidentReport.php,
    app/Http/Controllers/ResponderController.php,
    app/Http/Requests/AcknowledgeAssignmentRequest.php,
    app/Http/Requests/AdvanceResponderStatusRequest.php,
    app/Http/Requests/UpdateVitalsRequest.php,
    app/Http/Requests/UpdateChecklistRequest.php,
    app/Http/Requests/UpdateAssessmentTagsRequest.php,
    app/Http/Requests/SendMessageRequest.php,
    app/Http/Requests/RequestResourceRequest.php,
    app/Http/Requests/ResolveIncidentRequest.php,
    app/Http/Requests/UpdateLocationRequest.php,
    database/seeders/HospitalSeeder.php,
    database/seeders/DatabaseSeeder.php,
    resources/views/pdf/incident-report.blade.php,
    routes/web.php,
    app/Providers/AppServiceProvider.php
  </files>
  <behavior>
    - Acknowledge: POST /responder/{incident}/acknowledge sets IncidentUnit.acknowledged_at, updates incident status to ACKNOWLEDGED, creates timeline entry, broadcasts IncidentStatusChanged
    - Acknowledge: returns 422 if user has no unit, 403 if unit not assigned to incident
    - Status advance: POST /responder/{incident}/advance-status transitions forward-only (ACKNOWLEDGED->EN_ROUTE->ON_SCENE->RESOLVING); Resolving is NOT allowed here (use resolve endpoint)
    - Status advance: updates lifecycle timestamps (en_route_at, on_scene_at, resolving_at) on the incident
    - Status advance: returns 422 for backward or invalid transitions
    - Update location: POST /responder/location accepts {latitude, longitude}, updates user's unit coordinates, broadcasts UnitLocationUpdated
    - Send message: POST /responder/{incident}/message creates IncidentMessage with polymorphic sender, broadcasts MessageSent to all dispatchers assigned to the incident (or dispatch.incidents channel)
    - Send message: supports is_quick_reply flag for preset chips
    - Update checklist: PATCH /responder/{incident}/checklist accepts {items: Record<string,boolean>}, stores in incident checklist_data JSONB or similar approach, computes and stores checklist_pct, broadcasts ChecklistUpdated
    - Update vitals: PATCH /responder/{incident}/vitals accepts {systolic_bp, diastolic_bp, heart_rate, spo2, gcs} with validation ranges (systolic 50-300, diastolic 20-200, hr 20-300, spo2 0-100, gcs 3-15), stores in incident vitals JSONB
    - Update assessment tags: PATCH /responder/{incident}/assessment-tags accepts {assessment_tags: string[]}, stores in incident assessment_tags column
    - Request resource: POST /responder/{incident}/request-resource accepts {type: ResourceType, notes: string?}, creates timeline entry, broadcasts ResourceRequested to dispatch
    - Resolve: POST /responder/{incident}/resolve accepts {outcome: IncidentOutcome, hospital?: string, closure_notes?: string}, requires outcome; hospital required if outcome=TRANSPORTED_TO_HOSPITAL; sets status=RESOLVED, resolved_at, scene_time_sec (computed from on_scene_at), releases unit back to AVAILABLE, creates timeline entry, broadcasts IncidentStatusChanged + UnitStatusChanged, dispatches GenerateIncidentReport job
    - Resolve: vitals required only for medical outcomes (TREATED_ON_SCENE, TRANSPORTED_TO_HOSPITAL)
    - PDF generation: GenerateIncidentReport job loads incident with relations, renders pdf.incident-report Blade template via DomPDF, stores file, updates report_pdf_url on incident
    - All endpoints require auth + respond-incidents gate check
    - Responder show: GET /responder renders responder/Station page with active incident (if any), unit info, messages, hospitals list
  </behavior>
  <action>
    1. Run `composer require barryvdh/laravel-dompdf` to install DomPDF.

    2. Create `IncidentOutcome` enum with 5 cases: TreatedOnScene, TransportedToHospital, RefusedTreatment, DeclaredDOA, FalseAlarm (string-backed). Include a `label(): string` method returning human-readable labels.

    3. Create `ResourceType` enum with 6 cases: AdditionalAmbulance, FireUnit, PoliceBackup, RescueBoat, MedicalOfficer, Medevac (string-backed). Include a `label(): string` method.

    4. Create `ChecklistUpdated` broadcast event implementing ShouldBroadcast + ShouldDispatchAfterCommit. Broadcasts on `dispatch.incidents` channel with incident_id, incident_no, checklist_pct. Constructor takes Incident.

    5. Create `ResourceRequested` broadcast event implementing ShouldBroadcast + ShouldDispatchAfterCommit. Broadcasts on `dispatch.incidents` channel with incident_id, incident_no, resource_type, resource_label, notes, requested_by (user name). Constructor takes Incident, ResourceType, ?string notes, User requester.

    6. Create `HospitalSeeder` with Butuan City hospitals as a simple array config returned by the controller (no Hospital model needed — just a static list): Butuan City District Hospital (BCDH), Manuel J. Santos Hospital, RTR Medical Center, Butuan Doctors Hospital, Agusan del Norte Provincial Hospital. Store as a config file at `config/hospitals.php` returning an indexed array of `['id' => string, 'name' => string]`.

    7. Create all 9 Form Request classes with array-style validation rules (matching existing convention in app/Http/Requests/):
       - `AcknowledgeAssignmentRequest` — no body params, just auth
       - `AdvanceResponderStatusRequest` — `status: required|string|in:EN_ROUTE,ON_SCENE,RESOLVING`
       - `UpdateVitalsRequest` — systolic_bp (nullable|integer|between:50,300), diastolic_bp (nullable|integer|between:20,200), heart_rate (nullable|integer|between:20,300), spo2 (nullable|integer|between:0,100), gcs (nullable|integer|between:3,15)
       - `UpdateChecklistRequest` — `items: required|array`, `items.*: required|boolean`
       - `UpdateAssessmentTagsRequest` — `assessment_tags: required|array`, `assessment_tags.*: string`
       - `SendMessageRequest` — `body: required|string|max:1000`, `is_quick_reply: boolean`
       - `RequestResourceRequest` — `type: required|string` + validation via ResourceType enum, `notes: nullable|string|max:500`
       - `ResolveIncidentRequest` — `outcome: required|string` + validation via IncidentOutcome enum, `hospital: required_if:outcome,TRANSPORTED_TO_HOSPITAL|nullable|string`, `closure_notes: nullable|string|max:2000`
       - `UpdateLocationRequest` — `latitude: required|numeric|between:-90,90`, `longitude: required|numeric|between:-180,180`

    8. Create `ResponderController` with 10 public methods following the existing DispatchConsoleController pattern:
       - `show()` — Inertia::render responder/Station with: active incident (user's unit's active incident via incident_unit pivot where unassigned_at is null, with incidentType, barangay, timeline, messages eager loaded), unit info, hospitals from config, and messages
       - `acknowledge(AcknowledgeAssignmentRequest, Incident)` — verify user has unit, verify unit is assigned to this incident, set IncidentUnit.acknowledged_at, update incident status to ACKNOWLEDGED + acknowledged_at timestamp, create timeline entry, broadcast IncidentStatusChanged
       - `advanceStatus(AdvanceResponderStatusRequest, Incident)` — forward-only transitions: ACKNOWLEDGED->EN_ROUTE, EN_ROUTE->ON_SCENE, ON_SCENE->RESOLVING. Update lifecycle timestamps (en_route_at, on_scene_at, resolving_at). Create timeline entry, broadcast IncidentStatusChanged
       - `updateLocation(UpdateLocationRequest)` — update user's unit coordinates (use Point from Magellan), broadcast UnitLocationUpdated
       - `sendMessage(SendMessageRequest, Incident)` — create IncidentMessage with sender_type=User::class, sender_id=auth user, broadcast MessageSent. For dispatch visibility, also broadcast on dispatch.incidents channel
       - `updateChecklist(UpdateChecklistRequest, Incident)` — store items in incident vitals or a dedicated approach (use a `checklist_data` key within the vitals JSONB, or add a migration for checklist_data column if vitals JSONB is not appropriate). Compute checklist_pct = (completed items / total items) * 100. Broadcast ChecklistUpdated
       - `updateVitals(UpdateVitalsRequest, Incident)` — store validated vitals in incident vitals JSONB column
       - `updateAssessmentTags(UpdateAssessmentTagsRequest, Incident)` — store tags array in incident assessment_tags column. Broadcast IncidentStatusChanged so dispatch sees updated tags
       - `resolve(ResolveIncidentRequest, Incident)` — validate outcome required, hospital required for TRANSPORTED_TO_HOSPITAL. If outcome is medical (TREATED_ON_SCENE or TRANSPORTED_TO_HOSPITAL), check incident has vitals (return 422 with message if missing). Set status=RESOLVED, resolved_at=now(), outcome, hospital, closure_notes, scene_time_sec (now()->diffInSeconds(on_scene_at) if on_scene_at exists). Release unit: set unit status to AVAILABLE, broadcast UnitStatusChanged. Create timeline entry. Broadcast IncidentStatusChanged. Dispatch GenerateIncidentReport job
       - `requestResource(RequestResourceRequest, Incident)` — create timeline entry with event_type='resource_requested', event_data including type and notes. Broadcast ResourceRequested

    9. Register routes in routes/web.php under the existing responder middleware group. Replace the placeholder route. Use route names: responder.station, responder.acknowledge, responder.advance-status, responder.update-location, responder.send-message, responder.update-checklist, responder.update-vitals, responder.update-assessment-tags, responder.resolve, responder.request-resource.

    10. Create `GenerateIncidentReport` job implementing ShouldQueue with $tries=3, $timeout=60. Load incident with incidentType, barangay, assignedUnits, timeline. Render `pdf.incident-report` Blade view via DomPDF. Store file to `storage/app/incident-reports/{incident_no}.pdf`. Update incident report_pdf_url.

    11. Create `resources/views/pdf/incident-report.blade.php` — structured report with inline CSS (DomPDF doesn't support Tailwind): CDRRMO header, incident ID, type, priority, all lifecycle timestamps, location + barangay, assigned units, checklist completion %, vitals table, assessment tags list, outcome, hospital (if applicable), notes. Target single page.

    12. Add HospitalSeeder to DatabaseSeeder. Create config/hospitals.php.

    13. Run `vendor/bin/pint --dirty --format agent` after all PHP files created.

    Note: Use the `respond-incidents` gate (already defined in AppServiceProvider) for authorization. The responder must be the one assigned to the incident for acknowledge/advance/resolve actions — verify via user->unit->activeIncidents relationship.
  </action>
  <verify>
    <automated>php artisan test --compact --filter=Responder</automated>
  </verify>
  <done>
    - All 10 ResponderController endpoints return correct responses
    - IncidentOutcome and ResourceType enums exist with correct cases
    - ChecklistUpdated and ResourceRequested broadcast events fire correctly
    - Forward-only status transitions enforced (backward transitions return 422)
    - Acknowledge updates pivot acknowledged_at and incident status
    - Resolve requires outcome, requires hospital for TRANSPORTED_TO_HOSPITAL, requires vitals for medical outcomes
    - Resolve releases unit to AVAILABLE and dispatches PDF job
    - GenerateIncidentReport job produces a PDF file
    - Hospital seeder creates Butuan City hospitals config
    - All 10 test files pass
    - `vendor/bin/pint --dirty --format agent` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add checklist_data column migration and update Incident model casts</name>
  <files>
    database/migrations/xxxx_add_checklist_data_to_incidents_table.php,
    app/Models/Incident.php
  </files>
  <action>
    1. Check if the incidents table already has a column suitable for storing checklist item state (the vitals JSONB is for medical vitals, not checklists). Create a migration to add `checklist_data` JSONB column (nullable) to incidents table if it doesn't exist. This stores the individual checklist item state (e.g., {"scene_secured": true, "patient_assessed": false}).

    2. Add `'checklist_data' => 'array'` to Incident model casts() method. Add `checklist_data` to $fillable.

    3. Run migration: `php artisan migrate`

    4. Run `vendor/bin/pint --dirty --format agent`

    5. Re-run responder tests to ensure nothing broke: `php artisan test --compact --filter=Responder`
  </action>
  <verify>
    <automated>php artisan test --compact --filter=Responder</automated>
  </verify>
  <done>
    - checklist_data JSONB column exists on incidents table
    - Incident model casts checklist_data as array
    - All responder tests still pass
  </done>
</task>

</tasks>

<verification>
- `php artisan test --compact --filter=Responder` — all responder tests pass
- `php artisan test --compact` — full suite passes (no regressions)
- `vendor/bin/pint --dirty --format agent` — no formatting issues
- `php artisan route:list --name=responder` — shows all 10 responder routes
</verification>

<success_criteria>
- 10 working ResponderController endpoints behind auth + respond-incidents gate
- IncidentOutcome (5 cases) and ResourceType (6 cases) enums created
- ChecklistUpdated and ResourceRequested broadcast events implemented
- GenerateIncidentReport queued job produces PDF via DomPDF
- PDF Blade template renders incident report with all data fields
- Hospital config file with 5 Butuan City hospitals
- All 10 test files pass; full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-responder-workflow/05-01-SUMMARY.md`
</output>
